# Agents (Victoria, Veronica, MCP, Nightly Learner)
fastapi>=0.115.0
psutil>=5.9.0
uvicorn[standard]>=0.30.0
starlette>=0.49.1
httpx>=0.28.0
socksio>=1.0.0
pydantic>=2.0
fastmcp>=0.1.0
aiohttp>=3.9.0
asyncpg>=0.29.0
python-dotenv>=1.0.0
watchdog>=4.0.0

# Vision (Knowledge OS / Victoria) — клиент для Moondream Station
moondream>=0.1.0
Pillow>=10.0.0

# PDF (Victoria Telegram Bot)
pypdf>=4.0.0

# Redis для кэша и очередей (Singularity 10.0)
redis>=5.0.1

# Prometheus exporters (expert-workers / smart-worker / orchestrator scrape)
prometheus-client>=0.19.0

# Docker SDK для SandboxManager (Victoria Enhanced)
docker>=7.0.0

# MLX (Singularity 14.0: God Mode)
# Примечание: В Docker на macOS MLX работает в режиме CPU-only или fallback,
# но библиотека нужна для устранения ошибок импорта и работы API-сервера.
mlx-lm>=0.20.0
mlx>=0.18.0

# RAG: Hybrid Search v2 & Cross-Encoder Re-ranking (Singularity 16.0)
sentence-transformers>=3.0.0
torch>=2.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Аналитика данных (Parquet, DuckDB)
duckdb>=1.1.0
lancedb>=0.17.0
pyarrow>=14.0.0
pandas>=2.0.0
