# Normalize API URLs: remove trailing slash for API endpoints
if ($request_uri ~ ^(/api/.+)/$) {
    return 301 $scheme://$host$1$is_args$args;
}

# Trailing-slash canonical redirect disabled to avoid mass 301 responses.
# Keep page URLs as requested.
