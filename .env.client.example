# Пример .env для проекта-клиента корпорации (новый проект)
# Скопировать в репо нового проекта и задать значения.

# Slug проекта (должен быть зарегистрирован в реестре: scripts/register_project.py или POST /api/projects/register)
PROJECT_CONTEXT=my-project

# Адреса корпорации (хост или контейнер, где запущены Victoria, Veronica, Knowledge OS)
VICTORIA_URL=http://localhost:8010
VERONICA_URL=http://localhost:8011
KNOWLEDGE_OS_API_URL=http://localhost:8002

# Если у проекта свой backend, который вызывает Victoria
BACKEND_URL=http://localhost:8080

# Опционально: основной проект корпорации (для fallback)
# MAIN_PROJECT=atra-web-ide
