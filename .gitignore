# Env & secrets
.env
.env.local
.env.webmaster
.env.direct
*.pem
*.key
*.crt
*.csr
certs/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# Node
node_modules/
dist/
.npm

# IDE & OS
.DS_Store
.idea/
.vscode/
.cursor/
.cursor_chats_backup/

# Logs & backups
logs/
*.log
backups/
victoria_bot.log

# Generated
latency_benchmark.json
latency_dashboard.html
auto_optimizer_dashboard.html
quality_dashboard.html
quality_report.html
backend/auto_optimizer_report.json
backend/validation_report.json
coherence_issues_analysis.json
problematic_queries_analysis.json

# SQLite
*.db
feedback.db

# Large AI learning data (exceeds GitHub 100MB)
knowledge_os/ai_learning_data/

# Rust / C build artifacts
target/
*.o
*.rlib
*.dylib
*.a

# Backup / temp (мировая практика: не коммитить)
*.backup
*.bak
*.swp
*.tmp

# Lock / большие логи (чтобы не раздувать «active changes» в Cursor)
.victoria_bot.lock
full_logs.txt

# Playwright / frontend test output
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/
frontend/playwright/.cache/

# Curator reports (генерируемые)
docs/curator_reports/
docs/curator_findings.jsonl
docs/CHANGES_FROM_OTHER_CHATS.md
scripts/curator_tasks.txt

# Skills procedural artifacts (все кроме базовых)
knowledge_os/app/skills/procedural/*/
!knowledge_os/app/skills/procedural/README.md
!knowledge_os/app/skills/procedural/skills.json

# Local DB migrations
knowledge_os/app/db/migrations/
knowledge_os/app/knowledge_os/

# Артефакты сборки / зеркала (чтобы не раздувать число «active changes» в Cursor)
fused_model/
mirror/

# Объёмные данные обучения (десятки GB — не в git)
training_data/

# Rust gateway (лог не в репо)
rust_core/gateway.log

# Allow cursor rules for cross-project sharing
!.cursor/rules/
