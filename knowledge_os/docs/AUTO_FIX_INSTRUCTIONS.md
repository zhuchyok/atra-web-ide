# 🚀 АВТОМАТИЧЕСКОЕ ИСПРАВЛЕНИЕ ПОЗИЦИЙ

## ✅ ВСЕ ГОТОВО К ЗАПУСКУ

Все скрипты созданы, код обновлен в git. Теперь нужно выполнить на боевом сервере.

---

## 📋 КОМАНДЫ ДЛЯ СЕРВЕРА

### **Вариант 1: Автоматический скрипт (рекомендуется)**

```bash
# На боевом сервере:
cd /path/to/atra
git pull  # Обновить код до последней версии
python3 scripts/auto_fix_all_positions.py
```

Этот скрипт выполнит все шаги автоматически:

1. ✅ Синхронизация позиций с биржи
2. ✅ Экстренное исправление PUMPUSDT
3. ✅ Общее исправление всех позиций

---

### **Вариант 2: Bash скрипт**

```bash
# На боевом сервере:
cd /path/to/atra
git pull
bash scripts/run_all_fixes.sh
```

---

### **Вариант 3: Пошагово (если нужен контроль)**

```bash
# На боевом сервере:
cd /path/to/atra
git pull

# Шаг 1: Синхронизация
python3 scripts/sync_positions_with_exchange.py

# Шаг 2: PUMPUSDT
python3 scripts/emergency_fix_pumpusdt.py

# Шаг 3: Все позиции
python3 scripts/fix_open_positions_tp_sl.py
```

---

## 🔍 ПРОВЕРКА РЕЗУЛЬТАТОВ

После выполнения проверьте:

1. **На бирже:**
   - PUMPUSDT имеет TP1, TP2, SL
   - Все другие позиции защищены

2. **В БД:**

   ```sql
   -- Проверить позиции
   SELECT * FROM active_positions WHERE symbol = 'PUMPUSDT';

   -- Проверить ордера
   SELECT * FROM order_audit_log
   WHERE symbol LIKE '%PUMP%'
     AND datetime(created_at) > datetime('now', '-1 hour')
   ORDER BY created_at DESC;
   ```

3. **В логах:**
   - Должны быть записи о создании TP1/TP2/SL
   - Нет критических ошибок

---

## ⚠️ ВАЖНО

- Скрипты **идемпотентны** - безопасно запускать несколько раз
- Если TP/SL уже существуют, скрипты их пропустят
- Все действия логируются в `order_audit_log`

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После выполнения:

- ✅ PUMPUSDT добавлена в БД
- ✅ TP1 установлен: 0.003175 (50% объема)
- ✅ TP2 установлен: 0.003110 (50% объема)
- ✅ SL проверен/установлен: 0.003293
- ✅ Все другие позиции защищены

---

## 📞 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

1. Проверьте логи скриптов
2. Убедитесь, что ключи Bitget подключены
3. Проверьте доступность биржи
4. Запустите скрипты повторно (они безопасны)
