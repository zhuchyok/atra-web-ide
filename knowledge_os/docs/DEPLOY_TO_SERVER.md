# 🚀 РАЗВЕРТЫВАНИЕ РОЛЕЙ АДМИНИСТРАТОРОВ НА СЕРВЕРЕ

## 📋 Быстрое развертывание

### Вариант 1: Автоматическая отправка (рекомендуется)

```bash
# Замените параметры на ваши данные сервера
python3 deploy_admin_roles.py deploy <user> <host> <path>

# Пример:
python3 deploy_admin_roles.py deploy root 192.168.1.100 /opt/atra
python3 deploy_admin_roles.py deploy ubuntu your-server.com /home/ubuntu/atra
```

### Вариант 2: Ручная отправка файлов

```bash
# 1. Отправьте файлы на сервер
scp admin_roles.json user@server:/path/to/atra/
scp apply_admin_roles_server.py user@server:/path/to/atra/
scp deploy_admin_roles.sh user@server:/path/to/atra/

# 2. Подключитесь к серверу и выполните
ssh user@server
cd /path/to/atra
chmod +x deploy_admin_roles.sh
./deploy_admin_roles.sh
```

### Вариант 3: Выполнение на сервере

```bash
# Подключитесь к серверу
ssh user@server
cd /path/to/atra

# Скопируйте файлы admin_roles.json и apply_admin_roles_server.py
# Затем выполните:
python3 apply_admin_roles_server.py
```

## 📁 Файлы для отправки

1. **`admin_roles.json`** - роли администраторов
2. **`apply_admin_roles_server.py`** - скрипт применения ролей
3. **`deploy_admin_roles.sh`** - скрипт развертывания

## 🎯 Результат

После выполнения на сервере:

```
🔧 ПРИМЕНЕНИЕ РОЛЕЙ АДМИНИСТРАТОРОВ НА СЕРВЕРЕ
==================================================
✅ Супер администратор - полные права: 556251171
✅ Администратор - ограниченные права: 958930260

🎉 Применено ролей: 2/2

📊 ПРОВЕРКА РЕЗУЛЬТАТА:
🔥 СУПЕР АДМИН: 556251171 (super_admin)
👑 АДМИН: 958930260 (admin)
```

## 🛡️ Безопасность

- ✅ Автоматический бэкап базы данных перед изменениями
- ✅ Проверка существования пользователей
- ✅ Логирование всех операций
- ✅ Откат в случае ошибок

## 🔄 Проверка на сервере

После развертывания проверьте:

```bash
# На сервере выполните:
python3 -c "
from db import Database
db = Database()
admin_ids = db.get_admin_ids()
for admin_id in admin_ids:
    admin_data = db.get_user_data(admin_id)
    if admin_data:
        role = admin_data.get('role', 'user')
        is_super = admin_data.get('is_super_admin', False)
        admin_type = '🔥 СУПЕР АДМИН' if is_super else '👑 АДМИН'
        print(f'{admin_type}: {admin_id} ({role})')
"
```

## 🆘 Восстановление

Если что-то пошло не так:

```bash
# Восстановите из бэкапа
cp backups/trading_backup_*.db trading.db

# Или примените роли заново
python3 apply_admin_roles_server.py
```

---

**🎉 Готово! Роли администраторов развернуты на сервере!**
