# Отчет об обновлении иконок и отображения плеча в торговых сигналах

## Внесенные изменения

### 1. Убрана дублирующая иконка в TP1

**Проблема:** В новых торговых сигналах у TP1 было две иконки 🎯 (одна в `tp_info`, вторая в отображении)

**Исправление:** Убрана иконка из `tp_info`, оставлена только в отображении

**Было:**

```python
tp_info = f"🎯 TP1: {fmt.format(tp1)} (+{tp1_pct:.1f}%)\n🚀 TP2: {fmt.format(tp2)} (+{tp2_pct:.1f}%)"
```

**Стало:**

```python
tp_info = f"TP1: {fmt.format(tp1)} (+{tp1_pct:.1f}%)\n🚀 TP2: {fmt.format(tp2)} (+{tp2_pct:.1f}%)"
```

### 2. Добавлено отображение динамического плеча для фьючерсов

**Новая функциональность:** После риска теперь отображается динамическое плечо для пользователей с режимом фьючерсов

**Добавлен расчет плеча:**

```python
# Динамическое плечо для фьючерсов
dynamic_leverage = None
if trade_mode == 'futures':
    base_leverage = user_data.get('leverage', 1)
    dynamic_leverage = get_dynamic_leverage(df, current_index, base_leverage)
```

**Обновлено отображение:**

```python
f"⚠️ Риск: <code>{risk_pct:.2f}%</code>"
f"{f' | ⚡ Плечо: <code>x{dynamic_leverage}</code>' if dynamic_leverage else ''}\n"
```

### 3. Обновлены DCA сигналы

**DCA LONG и SHORT сигналы** также обновлены с новой иконкой плеча:

**Было:**

```python
leverage_info = f" | Плечо: x{leverage}" if trade_mode == 'futures' else ""
```

**Стало:**

```python
leverage_info = f" | ⚡ Плечо: x{leverage}" if trade_mode == 'futures' else ""
```

## Результат

### Новые торговые сигналы теперь отображают:

**Для SPOT режима:**

```
🟢 НОВЫЙ ТОРГОВЫЙ СИГНАЛ

📊 Символ: BTCUSDT
💰 Цена входа: 43250.00
📈 Сторона: LONG
🎯 TP1: 43682.50 (+1.0%)
🚀 TP2: 44115.00 (+2.0%)
⚠️ Риск: 2.00%
🔧 Режим: Строгий (spot)
```

**Для FUTURES режима:**

```
🟢 НОВЫЙ ТОРГОВЫЙ СИГНАЛ

📊 Символ: BTCUSDT
💰 Цена входа: 43250.00
📈 Сторона: LONG
🎯 TP1: 43682.50 (+1.0%)
🚀 TP2: 44115.00 (+2.0%)
⚠️ Риск: 2.00% | ⚡ Плечо: x1.5
🔧 Режим: Строгий (futures)
```

### DCA сигналы теперь отображают:

**Для фьючерсов:**

```
СИГНАЛ УСРЕДНЕНИЯ (LONG): BTCUSDT
Режим: FUTURES | ⚡ Плечо: x1.5
```

## Файлы изменены

- `signal_live.py` - обновлены функции формирования торговых сигналов

## Статус

✅ **ВЫПОЛНЕНО** - Иконки исправлены, отображение плеча добавлено
