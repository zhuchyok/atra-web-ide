# 🔄 Система обновления паттернов и параметров

## ✅ Текущее состояние системы

### Что работает автоматически:

1. **Добавление паттернов из сигналов:**
   - ✅ При принятии сигнала вызывается `record_signal_pattern()` в `ai_integration.py`
   - ✅ Паттерн создаётся со статусом `PENDING` или `ACCEPTED`
   - ✅ Сохраняется в `ai_learning_data/trading_patterns.json`

2. **Непрерывное обучение:**
   - ✅ `continuous_learning()` запускается каждые 60 минут
   - ✅ Автоматически оптимизирует параметры через `auto_optimize_parameters()`
   - ✅ Применяет оптимизированные параметры через `apply_optimized_parameters()`
   - ✅ Сохраняет паттерны, модель и метрики

3. **Автоматическое управление паттернами:**
   - ✅ При добавлении каждого 1000-го паттерна запускается `auto_manage_patterns()`
   - ✅ Умная очистка: сохраняет важные паттерны, удаляет старые/неважные
   - ✅ Лимит: 50,000 паттернов (из конфига)

4. **Обновление индивидуальных параметров:**
   - ✅ `SymbolSpecificOptimizer` проверяет новые паттерны каждые 6 часов
   - ✅ Если найдено ≥50 новых паттернов для символа, обновляет параметры
   - ✅ Сохраняет в `ai_learning_data/symbol_specific_params/{symbol}_params.json`

---

## 🆕 НОВОЕ: Обновление паттернов при закрытии сделок

### ✅ Реализовано:

1. **Функция `update_pattern_from_closed_trade()`:**
   - ✅ Создана в `ai_integration.py`
   - ✅ Обновляет паттерн результатом закрытой сделки (WIN/LOSS)
   - ✅ Добавляет profit_pct для обучения
   - ✅ Если паттерн не найден, создаёт новый из закрытой сделки

2. **Скрипт синхронизации:**
   - ✅ `scripts/sync_trades_to_patterns.py` - автоматический импорт из trading.db
   - ✅ Импортирует закрытые сделки за последние N часов
   - ✅ Обновляет паттерны результатами

### 📋 Как это работает:

```python
# При закрытии позиции:
await ai_integration.update_pattern_from_closed_trade(
    symbol="BTCUSDT",
    side="LONG",
    entry_price=50000.0,
    exit_price=51000.0,
    exit_reason="tp2",
    user_id=123,
    profit_pct=2.0,  # +2%
)

# Система:
# 1. Ищет паттерн с PENDING/ACCEPTED статусом
# 2. Обновляет result = "WIN" или "LOSS"
# 3. Обновляет profit_pct
# 4. Добавляет обновлённый паттерн в ai_learning
```

---

## 🔧 Интеграция в систему

### Где нужно вызывать `update_pattern_from_closed_trade()`:

1. **`auto_execution.py`** - при закрытии позиции по TP/SL
2. **`telegram_handlers.py`** - при закрытии через команды
3. **`risk_monitor.py`** - при закрытии по SL/TP через монитор
4. **`position_tracker.py`** - при закрытии позиции

### Автоматический импорт:

**Скрипт:** `scripts/sync_trades_to_patterns.py`

**Использование:**
```bash
# Синхронизация за последние 24 часа
python3 scripts/sync_trades_to_patterns.py --hours 24

# Только показать, что будет сделано (dry run)
python3 scripts/sync_trades_to_patterns.py --hours 24 --dry-run
```

**Рекомендуется добавить в cron:**
```bash
# Каждый час синхронизировать закрытые сделки
0 * * * * cd /path/to/atra && python3 scripts/sync_trades_to_patterns.py --hours 1
```

---

## 📊 Полный цикл обновления паттернов

### 1. Открытие сигнала:
```
Сигнал отправлен → record_signal_pattern_on_send() → Паттерн со статусом PENDING
Сигнал принят → record_signal_pattern() → Паттерн со статусом ACCEPTED
```

### 2. Закрытие позиции:
```
Позиция закрыта → update_pattern_from_closed_trade() → Паттерн обновлён результатом (WIN/LOSS)
```

### 3. Автоматический импорт:
```
Каждый час → sync_trades_to_patterns.py → Импорт закрытых сделок из trading.db
```

### 4. Непрерывное обучение:
```
Каждый час → continuous_learning() → Оптимизация параметров на основе новых паттернов
```

### 5. Обновление параметров:
```
Каждые 6 часов → SymbolSpecificOptimizer → Обновление индивидуальных параметров для символов
```

---

## 📈 Статистика обновления

- **Всего паттернов:** 50,000 (лимит)
- **Обновление параметров:** Каждые 6 часов (если ≥50 новых паттернов)
- **Непрерывное обучение:** Каждый час
- **Автоочистка:** Каждые 1000 паттернов
- **Синхронизация сделок:** Каждый час (рекомендуется)

---

## ✅ Итог

**Что работает:**
- ✅ Добавление паттернов при открытии сигналов
- ✅ Обновление паттернов при закрытии сделок (НОВОЕ)
- ✅ Автоматический импорт из trading.db (НОВОЕ)
- ✅ Непрерывное обучение (каждый час)
- ✅ Автоматическое управление паттернами
- ✅ Обновление индивидуальных параметров (каждые 6 часов)

**Система полностью автоматизирована:**
- 🟢 Паттерны обновляются автоматически
- 🟢 Параметры оптимизируются автоматически
- 🟢 Система учится на реальных результатах

---

**Версия:** 2.0  
**Дата:** 2025-11-13  
**Автор:** ATRA Team
