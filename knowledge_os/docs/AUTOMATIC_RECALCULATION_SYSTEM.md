# 🔄 АВТОМАТИЧЕСКИЙ ПЕРЕСЧЕТ ДАННЫХ

## 📅 Дата: 27 июля 2025

## 🎯 Обзор

Система автоматически пересчитывает все данные после закрытия позиций (полного или частичного). Это обеспечивает актуальность информации о балансе, рисках и доступных средствах.

## ✅ **ЧТО ПЕРЕСЧИТЫВАЕТСЯ АВТОМАТИЧЕСКИ**

### 💰 **Финансовые данные:**
- **Депозит** - обновляется с учетом прибыли/убытков
- **Общая прибыль** - сумма всех закрытых сделок
- **Свободные средства** - доступные для новых сделок
- **Занятые рисками средства** - по открытым позициям

### 📊 **Риски и позиции:**
- **Risk amount** каждой открытой позиции
- **Общий риск** по всем позициям
- **Количество открытых позиций**
- **Стоимость позиций** с учетом плеча

### 📈 **Статистика:**
- **История сделок** - добавляется закрытая сделка
- **P&L** - прибыль/убыток по закрытой позиции
- **Время закрытия** - точное время закрытия

## 🔄 **ПРОЦЕСС АВТОМАТИЧЕСКОГО ПЕРЕСЧЕТА**

### 📋 **Шаг 1: Закрытие позиции**
```python
# Закрываем позицию (полностью или частично)
if actual_qty >= qty:
    # Закрываем всю позицию
    open_positions = [p for p in open_positions if p["symbol"] != symbol]
else:
    # Закрываем часть позиции
    remaining_qty = qty - actual_qty
    pos["qty"] = remaining_qty
```

### 📊 **Шаг 2: Сохранение в историю**
```python
user_data["trade_history"].append({
    "symbol": symbol,
    "side": side,
    "entry_price": entry_price,
    "close_price": close_price,
    "qty": actual_qty,
    "profit": profit,
    "close_time": datetime.now().isoformat(),
    "trade_mode": user_data.get("trade_mode", "spot"),
    "leverage": pos.get("leverage", 1)
})
```

### 🔄 **Шаг 3: Пересчет баланса и рисков**
```python
# Автоматический вызов функции пересчета
balance_update = recalculate_balance_and_risks(user_data)
```

### 💾 **Шаг 4: Сохранение данных**
```python
save_user_data(context)
```

## 🎯 **ФУНКЦИЯ `recalculate_balance_and_risks`**

### 📊 **Что делает функция:**

```python
def recalculate_balance_and_risks(user_data):
    """
    Пересчитывает баланс и риски после закрытия позиций
    """
    # 1. Получаем текущие данные
    deposit = user_data.get("deposit", 0)
    open_positions = user_data.get("open_positions", [])
    trade_history = user_data.get("trade_history", [])

    # 2. Пересчитываем общий баланс
    total_profit = sum(trade.get("profit", 0) for trade in trade_history)
    updated_deposit = deposit + total_profit

    # 3. Пересчитываем риски по открытым позициям
    total_risk_amount = 0
    for pos in open_positions:
        pos_qty = pos.get("qty", 0)
        pos_entry_price = pos.get("entry_price", 0)
        pos_risk_pct = pos.get("risk_pct", 2.0)
        pos_leverage = pos.get("leverage", 1)

        # Стоимость позиции
        position_value = pos_qty * pos_entry_price

        # Risk amount с учетом плеча
        if pos_leverage > 1:
            risk_amount = position_value / pos_leverage * pos_risk_pct / 100
        else:
            risk_amount = position_value * pos_risk_pct / 100

        pos["risk_amount"] = risk_amount
        total_risk_amount += risk_amount

    # 4. Рассчитываем свободные средства
    free_deposit = max(updated_deposit - total_risk_amount, 0)

    # 5. Обновляем данные пользователя
    user_data["deposit"] = updated_deposit
    user_data["total_risk_amount"] = total_risk_amount
    user_data["free_deposit"] = free_deposit
    user_data["total_profit"] = total_profit

    return {
        "updated_deposit": updated_deposit,
        "total_risk_amount": total_risk_amount,
        "free_deposit": free_deposit,
        "total_profit": total_profit,
        "open_positions_count": len(open_positions)
    }
```

## 📊 **ПРИМЕР АВТОМАТИЧЕСКОГО ПЕРЕСЧЕТА**

### 🎯 **До закрытия позиции:**
```
💰 БАЛАНС:
Депозит: 1000.00 USDT
Общая прибыль: 0.00 USDT
Открытых позиций: 2
Занято рисками: 200.00 USDT
Свободно: 800.00 USDT

📊 ПОЗИЦИИ:
1. BTCUSDT (LONG) - 1.0000 BTC @ 44000.00
2. ETHUSDT (LONG) - 2.0000 ETH @ 2400.00
```

### 🔴 **Закрытие BTC позиции с прибылью:**
```
/close BTCUSDT
→ Закрытие по цене 45000.00
→ Прибыль: +1000.00 USDT
```

### ✅ **После автоматического пересчета:**
```
🟢 ПОЗИЦИЯ ЗАКРЫТА

СИМВОЛ: BTCUSDT
Сторона: LONG
Цена входа: 44000.00
Цена закрытия: 45000.00
Закрыто: 1.0000
Прибыль: 1000.00 USDT

💰 ОБНОВЛЕННЫЙ БАЛАНС:
Депозит: 2000.00 USDT (+1000.00)
Общая прибыль: 1000.00 USDT
Открытых позиций: 1
Занято рисками: 100.00 USDT
Свободно: 1900.00 USDT
```

## 🎯 **ЧАСТИЧНОЕ ЗАКРЫТИЕ**

### 📊 **До частичного закрытия:**
```
📊 ПОЗИЦИЯ:
ETHUSDT (LONG) - 2.0000 ETH @ 2400.00
P&L: +200.00 USDT
```

### 💰 **Частичное закрытие 50%:**
```
/close ETHUSDT 2500 1.0
→ Закрытие 1.0000 ETH по цене 2500.00
→ Прибыль: +100.00 USDT
```

### ✅ **После автоматического пересчета:**
```
🟢 ПОЗИЦИЯ ЗАКРЫТА

СИМВОЛ: ETHUSDT
Сторона: LONG
Цена входа: 2400.00
Цена закрытия: 2500.00
Закрыто: 1.0000
Прибыль: 100.00 USDT

💰 ОБНОВЛЕННЫЙ БАЛАНС:
Депозит: 2100.00 USDT (+100.00)
Общая прибыль: 1100.00 USDT
Открытых позиций: 1
Занято рисками: 50.00 USDT (уменьшился)
Свободно: 2050.00 USDT (увеличился)

📊 ОСТАВШАЯСЯ ПОЗИЦИЯ:
ETHUSDT (LONG) - 1.0000 ETH @ 2400.00
```

## 🛡️ **БЕЗОПАСНОСТЬ ПЕРЕСЧЕТА**

### ✅ **Проверки и валидация:**
- **Проверка существования** позиции перед закрытием
- **Валидация количества** - нельзя закрыть больше, чем есть
- **Обработка ошибок** - try-catch блоки
- **Сохранение данных** - атомарные операции

### 🔒 **Защита от ошибок:**
```python
try:
    balance_update = recalculate_balance_and_risks(user_data)
    if balance_update:
        # Успешный пересчет
        save_user_data(context)
    else:
        # Ошибка пересчета
        print(f"[recalculate_balance_and_risks] Ошибка: {e}")
except Exception as e:
    # Обработка исключений
    print(f"[close_position] Ошибка: {e}")
```

## 📈 **ПРЕИМУЩЕСТВА АВТОМАТИЧЕСКОГО ПЕРЕСЧЕТА**

### ✅ **Точность:**
- 🎯 **Реальное время** - данные обновляются мгновенно
- 📊 **Точные расчеты** - с учетом всех параметров
- 💰 **Актуальный баланс** - всегда соответствует реальности

### ✅ **Удобство:**
- 🔄 **Автоматически** - не требует ручных действий
- 📱 **Информативно** - показывает все изменения
- ⚡ **Быстро** - пересчет происходит мгновенно

### ✅ **Безопасность:**
- 🛡️ **Проверки** - валидация всех данных
- 🔒 **Сохранение** - атомарные операции
- ❌ **Обработка ошибок** - защита от сбоев

## 🎯 **КОГДА ПРОИСХОДИТ ПЕРЕСЧЕТ**

### 🔄 **Автоматически при:**
- ✅ Закрытии всей позиции
- ✅ Частичном закрытии позиции
- ✅ Закрытии всех позиций
- ✅ Усреднении позиции (DCA)

### 📊 **Что пересчитывается:**
- 💰 **Депозит** - с учетом прибыли/убытков
- 📊 **Риски** - по оставшимся позициям
- 🆓 **Свободные средства** - для новых сделок
- 📈 **Статистика** - общая прибыль, количество позиций

## 🎉 **ЗАКЛЮЧЕНИЕ**

**Автоматический пересчет обеспечивает:**

- 🔄 **Мгновенное обновление** всех данных после закрытия
- 📊 **Точные расчеты** баланса, рисков и свободных средств
- 🛡️ **Безопасность** - проверки и обработка ошибок
- 💰 **Актуальность** - данные всегда соответствуют реальности

**Система работает полностью автоматически - пользователю не нужно ничего пересчитывать вручную!** 🚀