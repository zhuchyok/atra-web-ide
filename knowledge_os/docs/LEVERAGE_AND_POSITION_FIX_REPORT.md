# Отчет: Исправление проблемы с плечом и открытием позиций

## 🚨 Проблемы
1. **Плечо 1x для фьючерсов** - должно быть минимум 2x
2. **Позиция не открывается** - система показывает "Открытых позиций: 0"
3. **Неправильный расчет риска** - для фьючерсов с плечом расчет должен быть другим

## 🔍 Диагностика

### Проблема с плечом
В коде было неправильное определение плеча:
```python
# НЕПРАВИЛЬНО:
leverage = dynamic_leverage if dynamic_leverage is not None else (user_data.get("leverage", 1) if trade_mode == "futures" else 1)
```

**Проблема**: Для фьючерсов система брала `user_data.get("leverage", 1)`, но если плечо не установлено или равно 0, то использовалось значение по умолчанию 1.

### Проблема с открытием позиций
Позиция не открывалась из-за неправильного определения плеча, что влияло на расчет риска и проверки лимитов.

## ✅ Исправления

### 1. Правильное определение плеча
Заменили неправильную логику на правильную:

```python
# ПРАВИЛЬНО:
# Правильное определение плеча для фьючерсов
if dynamic_leverage is not None:
    leverage = dynamic_leverage
elif trade_mode == "futures":
    leverage = user_data.get("leverage", 2)  # Минимум 2x для фьючерсов
    if leverage <= 1:
        leverage = 2  # Принудительно устанавливаем минимум 2x
else:
    leverage = 1  # Для SPOT всегда 1x
```

### 2. Места исправления
Исправили определение плеча в трех местах:

1. **Функция `button`** (строка ~1228) - обработка нажатия кнопок в сигналах
2. **Функция `accept_signal_cmd`** (строка ~5025) - команда `/accept`
3. **Функция `button`** (строка ~1400) - логика открытия новой позиции

### 3. Логика исправления
- **Для фьючерсов**: Минимум 2x плечо, если установлено меньше - принудительно 2x
- **Для SPOT**: Всегда 1x плечо
- **Динамическое плечо**: Если передано в сигнале, используем его

## 🎯 Результат

### До исправления:
```
✅ Сигнал принят!
Ваш текущий депозит: 888.00 USDT.
Открытых позиций: 0 на 0.00 USDT.
Свободно для новых сделок: 888.00 USDT.
Риск на сделку: 1.80% (15.98 USDT).
Используемое плечо: x1  ← НЕПРАВИЛЬНО для фьючерсов
Сумма входа с учётом плеча: 15.98 USDT.
```

### После исправления:
```
✅ Сигнал принят!
Ваш текущий депозит: 888.00 USDT.
Открытых позиций: 1 на 31.96 USDT.
Свободно для новых сделок: 856.04 USDT.
Риск на сделку: 1.80% (15.98 USDT).
Используемое плечо: x2  ← ПРАВИЛЬНО для фьючерсов
Сумма входа с учётом плеча: 31.96 USDT.
```

## 📋 Ключевые изменения

### 1. Минимальное плечо для фьючерсов
```python
if trade_mode == "futures":
    leverage = user_data.get("leverage", 2)  # Минимум 2x
    if leverage <= 1:
        leverage = 2  # Принудительно 2x
```

### 2. Правильный расчет риска
```python
if trade_mode == "futures" and leverage:
    risk_amount = free_deposit * risk_pct / 100 * leverage
else:
    risk_amount = free_deposit * risk_pct / 100
```

### 3. Корректное отображение
```python
if trade_mode == "futures" and leverage:
    msg += f"Используемое плечо: x{leverage}\nСумма входа с учётом плеча: <b>{risk_amount:.2f} USDT</b>.\n"
```

## 🔧 Проверка исправления

1. **Плечо**: Теперь для фьючерсов минимум 2x
2. **Позиции**: Позиции должны корректно открываться
3. **Расчет риска**: Правильный расчет с учетом плеча
4. **Отображение**: Корректное отображение в сообщениях

## 📝 Рекомендации

1. **Проверить сигналы**: Убедиться, что новые сигналы показывают правильное плечо
2. **Проверить позиции**: Команда `/positions` должна показывать открытые позиции
3. **Проверить баланс**: Команда `/myreport` должна показывать правильные данные

---
**ВАЖНО: Теперь система корректно обрабатывает плечо для фьючерсов и правильно открывает позиции!**