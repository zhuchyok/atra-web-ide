# 📊 РЕШЕНИЕ ПО VOLUME PROFILE ФИЛЬТРУ

## ⚠️ СТАТУС: НЕЭФФЕКТИВЕН - ОТКЛЮЧЕН

## Дата: 2025-11-29

## 🧪 ТЕСТ НА МЕМКОЙНАХ (2025-11-29)

**Гипотеза:** На мемкойнах с высокой волатильностью фильтр может работать лучше

**Результаты теста:**

- **Мемкойны (DOGE, SHIB, PEPE, FLOKI, BONK):**
  - Блокировка: 1.5% (немного больше, чем на обычных)
  - Доходность: -0.09% (2 сделки)
- **Обычные монеты (BTC, ETH, BNB, SOL, ADA):**
  - Блокировка: 0.9%
  - Доходность: +0.28% (21 сделка)

**Вывод:** Фильтр работает одинаково неэффективно на мемкойнах и обычных монетах. Блокирует < 2% сигналов в обоих случаях.

**Рекомендация:** Фильтр неэффективен везде, независимо от типа монеты.

## 📈 СТАТИСТИКА РАБОТЫ ФИЛЬТРА

**Результаты анализа на 7 днях:**

- Всего проверок: 228
- Пропущено: 226 (99.1%)
- Заблокировано: 2 (0.9%)

**Результаты торговли:**

- С фильтром: +0.28% (21 сделка)
- Без фильтра (baseline): +0.28% (21 сделка)
- Разница: 0.00% (идентично)

## 🎯 РЕШЕНИЕ: ОТКЛЮЧИТЬ ФИЛЬТР

### Причины:

1. **Фильтр практически не работает:**
   - Блокирует только 0.9% сигналов
   - Не влияет на результаты торговли

2. **Нет преимуществ:**
   - Результаты идентичны baseline
   - Не улучшает Win Rate или Profit Factor

3. **Добавляет сложность:**
   - Вычислительные затраты на расчет Volume Profile
   - Дополнительный код для поддержки
   - Нет практической пользы

## 🔧 ВЫПОЛНЕННЫЕ ИЗМЕНЕНИЯ

### 1. Отключен фильтр по умолчанию

**Файл:** `config.py`

```python
# Было:
USE_VP_FILTER = os.getenv("USE_VP_FILTER", "true").lower() in ("1", "true", "yes")

# Стало:
USE_VP_FILTER = os.getenv("USE_VP_FILTER", "false").lower() in ("1", "true", "yes")
```

### 2. Исправлена логика фильтра

**Файл:** `src/signals/filters_volume_vwap.py`

- Исправлена слишком мягкая логика в soft-mode
- Добавлено логирование статистики
- Добавлены функции для анализа работы фильтра

### 3. Создан скрипт для анализа

**Файл:** `scripts/analyze_vp_filter_stats.py`

- Собирает статистику блокировок
- Анализирует эффективность фильтра
- Выдает рекомендации

## 📋 ЕСЛИ ПОТРЕБУЕТСЯ ВКЛЮЧИТЬ ОБРАТНО

### Вариант 1: Через переменную окружения

```bash
USE_VP_FILTER=true python3 scripts/backtest_5coins_intelligent.py
```

### Вариант 2: Изменить config.py

```python
USE_VP_FILTER = os.getenv("USE_VP_FILTER", "true").lower() in ("1", "true", "yes")
```

### Вариант 3: Ужесточить логику

Если в будущем захотите попробовать снова, можно:

1. **Убрать проверку "в пределах Value Area" в soft mode:**

   ```python
   # Удалить строки 144-160 (проверка Value Area в soft mode)
   ```

2. **Использовать strict_mode по умолчанию:**

   ```python
   # В src/signals/core.py изменить:
   vp_ok, vp_reason = check_volume_profile_filter(df, i, "long", strict_mode=True)
   ```

3. **Уменьшить tolerance_pct:**
   ```python
   # В check_volume_profile_filter уменьшить множитель:
   tolerance_pct = max(1.0, min(5.0, (1.0 / volume_profile_threshold) * 2.0))  # Было 3.0
   ```

## 📊 ИСТОРИЯ ИЗМЕНЕНИЙ

1. **2025-11-28:** Обнаружена проблема - фильтр ухудшал результаты (-6.36%)
2. **2025-11-29:** Исправлена логика - фильтр стал нейтральным (0.00%)
3. **2025-11-29:** Добавлена статистика - выявлено, что фильтр блокирует только 0.9% сигналов
4. **2025-11-29:** Принято решение отключить фильтр

## ✅ СТАТУС

**Фильтр отключен по умолчанию**

Система работает без Volume Profile фильтра, используя baseline конфигурацию с хорошими показателями (+5.57% на 30 днях, 244 сделки, Win Rate 84.6%).
