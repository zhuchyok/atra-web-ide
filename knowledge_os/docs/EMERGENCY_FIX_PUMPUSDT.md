# 🚨 ЭКСТРЕННОЕ ИСПРАВЛЕНИЕ PUMPUSDT

## 📋 ПРОБЛЕМА

- **Позиция PUMPUSDT (SHORT)** открыта на бирже от 17.11.2025 17:46
- **В БД:** Нет записи в `active_positions` и `accepted_signals`
- **Защита:** Только SL, отсутствуют TP1 и TP2
- **Параметры сигнала:**
  - Entry: 0.003241
  - TP1: 0.003175 (+1.69%)
  - TP2: 0.003110 (+3.37%)
  - SL: 0.003293 (-1.60%)

---

## 🚀 РЕШЕНИЕ

### **ЭТАП 1: Синхронизация позиций**

```bash
# На боевом сервере:
cd /path/to/atra
python scripts/sync_positions_with_exchange.py
```

**Что делает:**

- Находит все реальные позиции на Bitget
- Добавляет недостающие в `active_positions` (включая PUMPUSDT)

---

### **ЭТАП 2: Экстренное исправление PUMPUSDT**

```bash
# На боевом сервере:
python scripts/emergency_fix_pumpusdt.py
```

**Что делает:**

- Находит позицию PUMPUSDT на бирже
- Добавляет в БД, если отсутствует
- Устанавливает TP1/TP2/SL по параметрам сигнала
- Логирует все действия в `order_audit_log`

---

### **ЭТАП 3: Общее исправление всех позиций**

```bash
# После экстренного исправления:
python scripts/fix_open_positions_tp_sl.py
```

**Что делает:**

- Проверяет защиту всех открытых позиций
- Довешивает TP/SL для позиций без защиты

---

## ✅ ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После выполнения скриптов:

1. ✅ **PUMPUSDT в БД:** Позиция добавлена в `active_positions`
2. ✅ **TP1 установлен:** 0.003175 (50% объема)
3. ✅ **TP2 установлен:** 0.003110 (50% объема)
4. ✅ **SL установлен:** 0.003293 (уже был, проверен)
5. ✅ **Логи:** Все действия записаны в `order_audit_log`

---

## 🔍 ПРОВЕРКА

### **На бирже:**

1. Открыть Bitget приложение
2. Найти позицию PUMPUSDT
3. Проверить наличие TP1, TP2, SL
4. Убедиться в распределении объемов 50/50

### **В БД:**

```sql
-- Проверить позицию
SELECT * FROM active_positions WHERE symbol = 'PUMPUSDT';

-- Проверить ордера
SELECT * FROM order_audit_log
WHERE symbol LIKE '%PUMP%'
  AND datetime(created_at) > datetime('now', '-1 hour')
ORDER BY created_at DESC;
```

---

## 📝 ПРИМЕЧАНИЯ

- Скрипт **идемпотентен** - безопасно запускать несколько раз
- Если TP/SL уже существуют, скрипт их пропустит
- Все действия логируются для аудита

---

## 🛡️ ПРЕДОТВРАЩЕНИЕ ПОВТОРЕНИЯ

После исправления рекомендуется:

1. ✅ Внедрить регулярную синхронизацию (каждые 5 минут)
2. ✅ Добавить валидацию при старте бота
3. ✅ Настроить мониторинг незащищенных позиций
