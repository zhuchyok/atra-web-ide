# 👥 РЕАЛИЗАЦИЯ: Система мониторинга рисков портфеля

## ✅ ВЫПОЛНЕНО

### 1. **Расширен CorrelationRiskManager**

**Добавлены методы:**

#### `check_portfolio_correlation_risk()`

- Проверяет корреляционный риск портфеля к SOL
- Вычисляет среднюю корреляцию активных позиций
- Определяет уровень риска (LOW/MEDIUM/HIGH/CRITICAL)
- Генерирует предупреждения

**Возвращает:**

```python
{
    'correlation_to_sol': float,      # Средняя корреляция к SOL
    'sol_positions_count': int,        # Количество позиций в SOL_HIGH
    'risk_level': str,                 # Уровень риска
    'alerts': List[str],               # Список предупреждений
    'timestamp': int                   # Время проверки
}
```

#### `calculate_dynamic_limit()`

- Вычисляет динамический лимит на основе волатильности
- Высокая волатильность (>0.15) → уменьшает лимит до 60%
- Низкая волатильность (<0.05) → полный лимит
- Средняя волатильность → 80% лимита

#### `get_risk_alerts()`

- Получает список алертов по рискам
- Уровни: CRITICAL, WARNING, INFO, ERROR
- Типы: CORRELATION, CONCENTRATION, PORTFOLIO, SYSTEM

### 2. **Создан скрипт мониторинга**

**`scripts/monitor_portfolio_risk.py`**

**Функционал:**

- Периодическая проверка рисков портфеля
- Форматированный отчет в консоль
- Сохранение JSON отчетов в `data/reports/`
- Проверка критических алертов
- Exit code 1 при критических алертах

**Пороги:**

```python
ALERT_THRESHOLDS = {
    "max_sol_positions": 8,           # Предупреждение при 8+ позициях
    "max_concurrent_loss": 5,         # Алерт при 5+ убыточных позициях
    "max_drawdown": 12.0,             # Алерт при просадке >12%
    "sol_correlation": 0.85,          # Алерт при корреляции >0.85
    "critical_correlation": 0.9       # Критический алерт при корреляции >0.9
}
```

## 📊 ИСПОЛЬЗОВАНИЕ

### Запуск мониторинга:

```bash
# Разовый запуск
python3 scripts/monitor_portfolio_risk.py

# Периодический запуск (каждые 30 минут)
*/30 * * * * cd /path/to/atra && python3 scripts/monitor_portfolio_risk.py >> logs/risk_monitor.log 2>&1
```

### Пример вывода:

```
================================================================================
📊 ОТЧЕТ МОНИТОРИНГА РИСКОВ ПОРТФЕЛЯ
================================================================================
Время: 2024-11-14T12:00:00

📈 ОСНОВНЫЕ МЕТРИКИ:
  Активных сигналов: 7
  Позиций в SOL_HIGH: 7
  Корреляция к SOL: 0.82
  Уровень риска: MEDIUM

⚠️ АЛЕРТЫ:
  ⚠️ [WARNING] Высокая корреляция: 0.82 к SOL
     → MONITOR: Требуется диверсификация

🎯 ПРОВЕРКА КРИТИЧЕСКИХ ПОРОГОВ:
  ✅ Позиций SOL_HIGH: 7 < 8 (в норме)
  ⚠️ Высокая корреляция: 0.820 >= 0.85 (порог)
================================================================================
```

## 🔄 СЛЕДУЮЩИЕ ШАГИ

### Немедленные действия (День 0-7):

1. ✅ **Мониторинг корреляции** - РЕАЛИЗОВАНО
2. ⏳ **Интеграция в signal_live.py** - В процессе
3. ⏳ **Настройка cron для периодических проверок** - Ожидает
4. ⏳ **Telegram уведомления при критических алертах** - Ожидает

### Среднесрочные действия (День 8-30):

1. ⏳ **Динамические лимиты на основе волатильности** - Метод готов, нужна интеграция
2. ⏳ **Автоматическое снижение лимитов при превышении рисков** - Ожидает
3. ⏳ **Диверсификация портфеля** - Ожидает

## 📝 ИНТЕГРАЦИЯ В signal_live.py

**План интеграции:**

1. Периодический вызов `check_portfolio_correlation_risk()` (каждые 30 минут)
2. Логирование критических алертов
3. Автоматическое снижение лимита при критических условиях
4. Telegram уведомления администратору

**Пример кода:**

```python
# В signal_live.py добавить:

async def periodic_risk_check():
    """Периодическая проверка рисков портфеля"""
    while True:
        try:
            if correlation_manager:
                portfolio_risk = await correlation_manager.check_portfolio_correlation_risk()
                alerts = await correlation_manager.get_risk_alerts()

                # Критические алерты → логируем и уведомляем
                critical = [a for a in alerts if a['level'] == 'CRITICAL']
                if critical:
                    logger.warning("🚨 КРИТИЧЕСКИЕ РИСКИ ПОРТФЕЛЯ:")
                    for alert in critical:
                        logger.warning("  %s", alert['message'])
                        # Отправляем в Telegram администратору

        except Exception as e:
            logger.error("Ошибка проверки рисков: %s", e)

        await asyncio.sleep(1800)  # 30 минут
```

## ✅ СТАТУС

- ✅ Мониторинг корреляции реализован
- ✅ Система алертов готова
- ✅ Скрипт мониторинга создан
- ⏳ Интеграция в signal_live.py - в процессе
- ⏳ Автоматические действия - ожидает

**Готово к тестированию!** 🚀
