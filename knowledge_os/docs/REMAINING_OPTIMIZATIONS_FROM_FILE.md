# Оставшиеся оптимизации из performance_optimization.mdc

## Дата: 2025-01-09

## Статус: Анализ завершен, готово к реализации

---

## ✅ Уже реализовано (из файла):

1. ✅ **Redis кэширование** (пункт 54) - `src/database/redis_cache.py`
2. ✅ **Отключение индексов при массовой загрузке** (пункт 46) - `executemany_optimized()`
3. ✅ **VACUUM и ANALYZE автоматизация** (пункт 56) - `vacuum_if_needed()`, `analyze_if_needed()`
4. ✅ **Numba JIT компиляция** (пункт 70) - `src/data/technical.py`
5. ✅ **Батчинг запросов** (пункт 75) - `src/database/batch_queries.py`
6. ✅ **Параллельная обработка** (пункт 34) - `src/optimization/parallel_processing.py`
7. ✅ **Fetchmany оптимизация** (пункт 48) - `src/database/fetch_optimizer.py`
8. ✅ **uvloop** (пункт 33) - `main.py`
9. ✅ **Async generators** (пункт 35) - `src/database/async_loaders.py`
10. ✅ **Prepared statements** (пункт 9) - `src/database/db.py`
11. ✅ **Query caching** (пункт 37) - `src/database/query_cache.py`
12. ✅ **PGO для Rust** (пункт 22) - `scripts/build_rust_pgo.sh`
13. ✅ **LTO для Rust** (пункт 23) - `rust-atra/Cargo.toml`
14. ✅ **target-cpu=native** (пункт 25) - `Makefile`

---

## 🎯 Что еще можно реализовать:

### Высокий приоритет (рекомендуется):

#### 1. CHECK constraints на уровне БД (пункт 6)

**Статус:** ❌ Не реализовано  
**Приоритет:** Высокий  
**Описание:** Валидация OHLC данных на уровне БД  
**Преимущества:** Предотвращение некорректных данных, ускорение на 5-10%  
**Применимость для SQLite:** ✅ Да

**Что нужно:**

```sql
-- Добавить в таблицу quotes/candles
ALTER TABLE quotes ADD CONSTRAINT chk_ohlc_valid CHECK (
    high >= low AND
    open >= low AND open <= high AND
    close >= low AND close <= high AND
    open > 0 AND high > 0 AND low > 0 AND close > 0 AND
    volume >= 0
);
```

**Файлы для изменения:**

- `src/database/db.py` - добавить CHECK constraints при создании таблиц

---

#### 2. Суррогатные ключи для временных меток (пункт 2)

**Статус:** ❌ Не реализовано  
**Приоритет:** Высокий  
**Описание:** Добавить INTEGER колонку для временных меток вместо DATETIME в запросах  
**Преимущества:** Ускорение на 20-40%, экономия места на 30-60%  
**Применимость для SQLite:** ✅ Да

**Что нужно:**

```sql
-- Добавить колонку time_surrogate INTEGER
ALTER TABLE signals ADD COLUMN time_surrogate INTEGER;
-- Заполнить: time_surrogate = CAST(strftime('%s', ts) AS INTEGER)
-- Создать индекс на time_surrogate
CREATE INDEX idx_signals_time_surrogate ON signals(time_surrogate);
```

**Файлы для изменения:**

- `src/database/db.py` - добавить колонку и триггер
- Обновить запросы для использования time_surrogate

---

#### 3. Частичные индексы (пункт 5)

**Статус:** ❌ Не реализовано  
**Приоритет:** Средний  
**Описание:** Создавать индексы только для приоритетных таймфреймов/символов  
**Преимущества:** Уменьшение размера индексов → быстрее обновление  
**Применимость для SQLite:** ✅ Да

**Что нужно:**

```sql
-- Индекс только для активных символов
CREATE INDEX idx_signals_active_symbols ON signals(symbol, ts)
WHERE symbol IN ('BTCUSDT', 'ETHUSDT', ...);
```

**Файлы для изменения:**

- `src/database/db.py` - добавить частичные индексы
- `scripts/analyze_and_create_indexes.py` - автоматическое создание

---

#### 4. Архивация старых данных (пункт 53)

**Статус:** ❌ Не реализовано  
**Приоритет:** Средний  
**Описание:** Перемещение данных старше 2 лет в архивные таблицы  
**Преимущества:** Снижение размера активной БД на 30-80%  
**Применимость для SQLite:** ✅ Да

**Что нужно:**

- Создать скрипт `scripts/archive_old_data.py`
- Автоматизировать через `system_tasks.py`
- Создать архивные таблицы с суффиксом `_archive`

**Файлы для создания:**

- `scripts/archive_old_data.py` - скрипт архивации
- `src/database/archive_manager.py` - менеджер архивации

---

### Средний приоритет:

#### 5. Query profiling улучшения (пункт 16)

**Статус:** ⚠️ Частично реализовано  
**Приоритет:** Средний  
**Описание:** Расширить существующий `query_profiler.py` для автоматического анализа  
**Преимущества:** Автоматическое выявление медленных запросов  
**Применимость для SQLite:** ✅ Да

**Что нужно:**

- Добавить автоматический анализ через `EXPLAIN QUERY PLAN`
- Интегрировать в `Database.execute_with_retry()` для медленных запросов
- Логировать запросы > 1 секунды

**Файлы для изменения:**

- `src/database/query_profiler.py` - расширить функциональность
- `src/database/db.py` - интегрировать автоматический профилинг

---

#### 6. Оптимизация chunking размеров (пункт 48)

**Статус:** ⚠️ Частично реализовано  
**Приоритет:** Средний  
**Описание:** Автоматический выбор оптимального размера chunk  
**Преимущества:** Улучшение cache locality, экономия памяти  
**Применимость для SQLite:** ✅ Да

**Что нужно:**

- Добавить эмпирический расчет оптимального размера chunk
- Адаптивный chunking на основе размера данных

**Файлы для изменения:**

- `src/database/async_loaders.py` - добавить адаптивный chunking
- `src/database/fetch_optimizer.py` - оптимизация размера батча

---

### Низкий приоритет (для максимальной производительности):

#### 7. SIMD в Rust (пункт 28)

**Статус:** ❌ Не реализовано  
**Приоритет:** Низкий  
**Описание:** Использование SIMD для векторной обработки больших массивов  
**Преимущества:** Ускорение на порядки для больших массивов  
**Применимость:** ✅ Да, для технических индикаторов

**Что нужно:**

- Добавить SIMD в `rust-atra` для расчетов индикаторов
- Использовать `packed_simd` или `rayon/simd`

**Файлы для изменения:**

- `rust-atra/Cargo.toml` - добавить зависимости
- `rust-atra/src/indicators.rs` - добавить SIMD версии

---

#### 8. jemalloc для Rust (пункт 30)

**Статус:** ❌ Не реализовано  
**Приоритет:** Низкий  
**Описание:** Использование jemalloc вместо system allocator  
**Преимущества:** Ускорение на 5-15% для частых аллокаций  
**Применимость:** ✅ Да

**Что нужно:**

```toml
# rust-atra/Cargo.toml
[dependencies]
jemallocator = "0.5"
```

```rust
// rust-atra/src/lib.rs
#[global_allocator]
static ALLOC: jemallocator::Jemalloc = jemallocator::Jemalloc;
```

**Файлы для изменения:**

- `rust-atra/Cargo.toml` - добавить зависимость
- `rust-atra/src/lib.rs` - настроить global allocator

---

#### 9. Memory alignment (пункт 27)

**Статус:** ❌ Не реализовано  
**Приоритет:** Низкий  
**Описание:** Выравнивание данных по кэш-линии (64 байта)  
**Преимущества:** Ускорение на 10-30% за счет лучшего использования кэша  
**Применимость:** ✅ Да, для hot paths в Rust

**Что нужно:**

```rust
#[repr(align(64))]  // Выравнивание по кэш-линии
struct HotData {
    // Часто используемые данные
}
```

**Файлы для изменения:**

- `rust-atra/src/indicators.rs` - добавить выравнивание для hot paths

---

#### 10. Cython для критичных участков (пункт 69)

**Статус:** ❌ Не реализовано  
**Приоритет:** Низкий  
**Описание:** Компиляция Python в C для ускорения  
**Преимущества:** Ускорение на 10-100x для вычислительно интенсивных задач  
**Применимость:** ✅ Да, для технических индикаторов

**Что нужно:**

- Выделить критичные функции из `src/data/technical.py`
- Переписать на Cython
- Компилировать в .so модули

**Файлы для создания:**

- `src/data/technical_cython.pyx` - Cython версия
- `setup.py` - скрипт компиляции

---

#### 11. Модуль array для оптимизации массивов (пункт 71)

**Статус:** ❌ Не реализовано  
**Приоритет:** Низкий  
**Описание:** Использование `array.array` вместо списков для численных операций  
**Преимущества:** Ускорение на 12-15x для численных операций  
**Применимость:** ✅ Да, но NumPy уже используется

**Примечание:** NumPy уже используется, поэтому эффект будет минимальным

---

## 📊 Приоритизация реализации:

### Немедленно (высокий приоритет):

1. **CHECK constraints** - предотвращение некорректных данных
2. **Суррогатные ключи** - значительное ускорение запросов
3. **Частичные индексы** - оптимизация для приоритетных данных

### В ближайшее время (средний приоритет):

4. **Архивация старых данных** - снижение размера БД
5. **Query profiling улучшения** - автоматический мониторинг
6. **Оптимизация chunking** - улучшение обработки больших данных

### В будущем (низкий приоритет):

7. **SIMD в Rust** - для максимальной производительности
8. **jemalloc** - небольшой эффект
9. **Memory alignment** - требует глубокого понимания
10. **Cython** - увеличивает сложность разработки

---

## 🎯 Рекомендуемый план реализации:

### Этап 1: Валидация и оптимизация БД (1-2 дня)

- ✅ CHECK constraints для OHLC данных
- ✅ Суррогатные ключи для временных меток
- ✅ Частичные индексы для приоритетных символов

### Этап 2: Оптимизация обработки данных (2-3 дня)

- ✅ Архивация старых данных
- ✅ Query profiling улучшения
- ✅ Оптимизация chunking размеров

### Этап 3: Максимальная производительность (опционально)

- ✅ SIMD в Rust
- ✅ jemalloc
- ✅ Memory alignment
- ✅ Cython

---

## 📈 Ожидаемый эффект:

### Высокий приоритет:

- **CHECK constraints:** Предотвращение ошибок, ускорение на 5-10%
- **Суррогатные ключи:** Ускорение на 20-40%, экономия места на 30-60%
- **Частичные индексы:** Ускорение обновления на 30-50%

### Средний приоритет:

- **Архивация:** Снижение размера БД на 30-80%
- **Query profiling:** Автоматическое выявление узких мест
- **Chunking:** Улучшение обработки больших данных на 20-30%

### Низкий приоритет:

- **SIMD:** Ускорение на порядки для больших массивов
- **jemalloc:** Ускорение на 5-15%
- **Memory alignment:** Ускорение на 10-30%
- **Cython:** Ускорение на 10-100x для критичных участков

---

## Итоговая сводка

**Всего оптимизаций в файле:** 81  
**Уже реализовано:** ~14  
**Можно реализовать:** ~11  
**Рекомендуется реализовать:** 6 (высокий + средний приоритет)

**Ожидаемый общий эффект от рекомендуемых оптимизаций:**

- Ускорение запросов: 20-50%
- Снижение размера БД: 30-80%
- Предотвращение ошибок: 100% (CHECK constraints)
- Улучшение мониторинга: Автоматический профилинг
