# 🎮 РУКОВОДСТВО ПО ИНТЕРАКТИВНОМУ УПРАВЛЕНИЮ СЕРВЕРОМ

## 🎯 РЕШЕНИЕ ПРОБЛЕМЫ

**Проблема:** Раньше Ctrl+C останавливал процесс и позволял вводить команды, а сейчас этого не происходит.

**Решение:** Создана система интерактивного управления сервером с поддержкой команд в реальном времени.

## 🚀 СПОСОБЫ ЗАПУСКА

### 1. Быстрый запуск (рекомендуется)
```bash
./start_interactive.sh
```

### 2. Прямой запуск интерактивного сервера
```bash
python3 interactive_server.py
```

### 3. Запуск с интерактивным управлением
```bash
python3 server_with_interactive.py
```

### 4. Тестовый режим
```bash
python3 test_interactive.py
```

## 📋 ДОСТУПНЫЕ КОМАНДЫ

### 🎮 Основные команды
- `help` - Показать справку по командам
- `status` - Статус сервера и системы
- `clear` - Очистить экран
- `exit` - Выйти из программы

### 🔧 Управление сервером
- `restart` - Перезапустить сервер
- `stop` - Остановить сервер
- `resume` - Вернуться к обычному режиму (для server_with_interactive.py)

### 📊 Мониторинг
- `logs` - Показать последние логи
- `health` - Проверить здоровье системы
- `users` - Список пользователей бота
- `balance` - Показать баланс системы
- `signals` - Последние сигналы
- `config` - Показать конфигурацию

## 🎯 КАК ИСПОЛЬЗОВАТЬ

### Способ 1: Интерактивный сервер (рекомендуется)
1. Запустите: `python3 interactive_server.py`
2. Сервер запустится автоматически
3. Используйте команды для управления

### Способ 2: Сервер с переключением режимов
1. Запустите: `python3 server_with_interactive.py`
2. Нажмите **Ctrl+C** для переключения в интерактивный режим
3. Введите команды для управления
4. Введите `resume` для возврата к обычному режиму

### Способ 3: Тестовый режим
1. Запустите: `python3 test_interactive.py`
2. Нажмите **Ctrl+C** для переключения в интерактивный режим
3. Протестируйте команды

## 🔧 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Запуск и управление
```bash
# Запуск интерактивного сервера
python3 interactive_server.py

# В интерактивном режиме:
ATRA [running]> status
ATRA [running]> logs
ATRA [running]> health
ATRA [running]> restart
ATRA [running]> exit
```

### Переключение режимов
```bash
# Запуск сервера с переключением
python3 server_with_interactive.py

# В обычном режиме нажмите Ctrl+C
# Переключение в интерактивный режим
TEST [interactive]> help
TEST [interactive]> status
TEST [interactive]> resume
```

## 🛠️ НАСТРОЙКА

### Переменные окружения
```bash
export ATRA_ENV=dev  # или prod
export TELEGRAM_TOKEN=your_token
```

### Файлы конфигурации
- `config.py` - основная конфигурация
- `atra.lock` - файл блокировки
- `system_improved.log` - основной лог

## 🔍 ДИАГНОСТИКА

### Проверка статуса
```bash
# В интерактивном режиме
ATRA [running]> status

# Или через команду
ps aux | grep python3
```

### Просмотр логов
```bash
# В интерактивном режиме
ATRA [running]> logs

# Или напрямую
tail -f system_improved.log
```

### Проверка здоровья
```bash
# В интерактивном режиме
ATRA [running]> health
```

## 🚨 УСТРАНЕНИЕ ПРОБЛЕМ

### Проблема: Сервер не запускается
```bash
# Проверьте блокировку
rm -f atra.lock

# Остановите все процессы
pkill -f python3

# Запустите заново
python3 interactive_server.py
```

### Проблема: Команды не работают
```bash
# Проверьте режим
ATRA [interactive]> status

# Перезапустите сервер
ATRA [running]> restart
```

### Проблема: Нет доступа к базе данных
```bash
# Проверьте конфигурацию
ATRA [running]> config

# Проверьте здоровье системы
ATRA [running]> health
```

## 📚 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### Файлы системы
- `interactive_server.py` - основной интерактивный сервер
- `server_with_interactive.py` - сервер с переключением режимов
- `test_interactive.py` - тестовый сервер
- `start_interactive.sh` - скрипт запуска

### Логи
- `system_improved.log` - основной лог системы
- `bot.log` - лог бота (если используется nohup)

### Блокировки
- `atra.lock` - файл блокировки для предотвращения множественного запуска

## ✅ ПРЕИМУЩЕСТВА НОВОЙ СИСТЕМЫ

1. **Интерактивное управление** - команды в реальном времени
2. **Безопасная остановка** - корректное завершение процессов
3. **Мониторинг** - проверка статуса и здоровья системы
4. **Гибкость** - переключение между режимами
5. **Диагностика** - встроенные инструменты отладки

## 🎯 РЕКОМЕНДАЦИИ

1. **Для разработки:** используйте `test_interactive.py`
2. **Для продакшена:** используйте `interactive_server.py`
3. **Для отладки:** используйте `server_with_interactive.py`
4. **Для быстрого запуска:** используйте `./start_interactive.sh`

Теперь у вас есть полный контроль над сервером через интерактивные команды! 🎉
