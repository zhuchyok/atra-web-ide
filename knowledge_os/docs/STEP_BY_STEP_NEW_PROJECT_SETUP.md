# 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ: ДОБАВЛЕНИЕ КОМАНДЫ ЭКСПЕРТОВ В НОВОЙ ПРОЕКТ

**Дата:** 2025-12-14  
**Статус:** ✅ **ГОТОВО К ИСПОЛЬЗОВАНИЮ**

---

## 🎯 ЦЕЛЬ

Добавить команду из 22 экспертов в новый проект Cursor, чтобы они автоматически активировались при работе.

---

## ⚡ БЫСТРЫЙ СПОСОБ (3 ШАГА)

> **⚠️ ВАЖНО:** `.team_data` — это **ОПЦИОНАЛЬНО**! Для работы команды достаточно только `.cursorrules`.  
> Если видите сообщение про отсутствие `.team_data` — это нормально, команда работает!  
> См. `docs/TEAM_DATA_EXPLANATION.md` для подробностей.

### **Шаг 1: Скопировать файл `.cursorrules`**

```bash
# Из проекта ATRA скопируйте файл в корень нового проекта
cp /path/to/atra/.cursorrules /path/to/new-project/.cursorrules
```

**Этого достаточно!** Команда экспертов (более 40 сотрудников) будет работать автоматически.

### **Шаг 2: Открыть новый чат в Cursor**

Просто откройте новый чат в Cursor IDE.

### **Шаг 3: Начать работать**

Опишите любую задачу - Виктория автоматически активирует нужных экспертов!

> **💡 Примечание:** Если видите сообщение про `.team_data` — это информационное сообщение, не ошибка.  
> Команда работает без `.team_data`. Настроить синхронизацию можно позже (см. "ПОЛНЫЙ СПОСОБ" ниже).

---

## 📖 ПОЛНЫЙ СПОСОБ (С СИНХРОНИЗАЦИЕЙ ДАННЫХ)

> **💡 Этот способ нужен ТОЛЬКО если вы хотите синхронизировать данные команды между несколькими проектами.**  
> Для одного проекта достаточно "БЫСТРОГО СПОСОБА" выше.

### **Шаг 1: Клонировать или открыть новый проект**

```bash
# Если проект еще не клонирован
git clone https://github.com/your-org/new-project.git
cd new-project

# Или просто перейдите в существующий проект
cd /path/to/new-project
```

---

### **Шаг 2: Скопировать скрипты синхронизации**

Скопируйте из проекта ATRA:

```bash
# Создать директорию scripts если нет
mkdir -p scripts

# Скопировать скрипт синхронизации
cp /path/to/atra/scripts/sync_team_data.py scripts/
cp /path/to/atra/scripts/setup_team_sync.sh scripts/
cp /path/to/atra/scripts/auto_sync_team_data.sh scripts/

# Сделать исполняемыми
chmod +x scripts/sync_team_data.py
chmod +x scripts/setup_team_sync.sh
chmod +x scripts/auto_sync_team_data.sh
```

---

### **Шаг 3: Настроить синхронизацию данных команды**

#### **Вариант A: Если есть центральный репозиторий**

```bash
# Установить переменные окружения
export TEAM_DATA_REPO="https://github.com/your-org/team-data.git"
export TEAM_DATA_DIR=".team_data"

# Получить данные команды
python scripts/sync_team_data.py pull
```

#### **Вариант B: Если нет центрального репозитория**

```bash
# Использовать интерактивную настройку
bash scripts/setup_team_sync.sh
```

Или скопировать данные напрямую из проекта ATRA:

```bash
# Создать директорию
mkdir -p .team_data

# Скопировать данные из проекта ATRA
cp -r /path/to/atra/.team_data/* .team_data/
```

---

### **Шаг 4: Скопировать правила Cursor**

```bash
# Скопировать .cursorrules из синхронизированных данных
cp .team_data/.cursorrules .cursorrules

# Или напрямую из проекта ATRA
cp /path/to/atra/.cursorrules .cursorrules
```

---

### **Шаг 5: Проверить настройку**

```bash
# Проверить статус синхронизации
python scripts/sync_team_data.py status

# Проверить наличие .cursorrules
ls -la .cursorrules
```

Должно показать:
```
📊 Статус синхронизации:
  Локальный репозиторий: ✅
  Git инициализирован: ✅
  Файлов: 51
```

---

### **Шаг 6: Открыть Cursor и протестировать**

1. **Откройте проект в Cursor IDE**
2. **Откройте новый чат**
3. **Напишите любую задачу**, например:
   ```
   Создать новый корпоративный сайт
   ```
4. **Виктория автоматически:**
   - ✅ Определит нужных экспертов
   - ✅ Обратится к каждому с вопросами
   - ✅ Получит детальные ответы
   - ✅ Представит результат

---

## ✅ ПРОВЕРКА РАБОТЫ

### **Тест 1: Проверка правил**

В Cursor напишите:
```
Какие эксперты из команды должны обсудить создание нового сайта?
```

**Ожидаемый результат:** Виктория должна автоматически выбрать экспертов (Анастасия, София, Андрей, Дарья, Марина, Юлия) и обратиться к каждому.

---

### **Тест 2: Проверка синхронизации**

```bash
# Проверить статус
python scripts/sync_team_data.py status

# Обновить данные
python scripts/sync_team_data.py pull
```

---

## 🔄 ОБНОВЛЕНИЕ ДАННЫХ КОМАНДЫ

### **Автоматическое обновление**

Добавьте в cron (опционально):

```bash
# Каждый час
0 * * * * cd /path/to/new-project && python scripts/sync_team_data.py pull
```

### **Ручное обновление**

```bash
# Получить последние изменения
python scripts/sync_team_data.py pull

# Скопировать обновленные правила
cp .team_data/.cursorrules .cursorrules
```

---

## 📋 ЧЕКЛИСТ НАСТРОЙКИ

- [ ] Проект открыт в Cursor
- [ ] Файл `.cursorrules` скопирован в корень проекта
- [ ] Скрипты синхронизации скопированы (опционально)
- [ ] Данные команды синхронизированы (опционально)
- [ ] Проверен статус синхронизации
- [ ] Протестирована работа команды в Cursor

---

## 🚨 РЕШЕНИЕ ПРОБЛЕМ

### **Проблема: Команда не активируется**

**Решение:**
1. Проверьте наличие `.cursorrules` в корне проекта
2. Перезапустите Cursor IDE
3. Откройте новый чат (не продолжайте старый)

---

### **Проблема: Данные не синхронизируются**

**Решение:**
```bash
# Проверить переменные окружения
echo $TEAM_DATA_REPO
echo $TEAM_DATA_DIR

# Инициализировать заново
rm -rf .team_data
python scripts/sync_team_data.py sync
```

---

### **Проблема: Скрипт не найден**

**Решение:**
```bash
# Проверить путь
which python3

# Использовать полный путь
python3 /path/to/scripts/sync_team_data.py status
```

---

## 📝 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### **Пример 1: Веб-разработка**

**Запрос:**
```
Создать новый корпоративный сайт
```

**Виктория автоматически обращается к:**
- Анастасия (Product Manager) - roadmap
- София (UI/UX Designer) - дизайн
- Андрей (Frontend Developer) - реализация
- Дарья (SEO Specialist) - видимость
- Марина (Content Manager) - контент
- Юлия (Legal Counsel) - compliance

---

### **Пример 2: Торговая стратегия**

**Запрос:**
```
Разработать новую торговую стратегию
```

**Виктория автоматически обращается к:**
- Павел (Trading Strategy Developer) - стратегия
- Мария (Risk Manager) - риски
- Максим (Data Analyst) - метрики
- Дмитрий (ML Engineer) - ML модели

---

### **Пример 3: Техническая проблема**

**Запрос:**
```
Исправить проблему с производительностью базы данных
```

**Виктория автоматически обращается к:**
- Роман (Database Engineer) - БД
- Ольга (Performance Engineer) - производительность
- Игорь (Backend Developer) - код

---

## 🎯 ИТОГОВАЯ СТРУКТУРА ПРОЕКТА

После настройки структура должна быть:

```
new-project/
├── .cursorrules              # ✅ Правила команды (ОБЯЗАТЕЛЬНО)
├── .team_data/              # ✅ Данные команды (опционально)
│   ├── .git/
│   ├── scripts/
│   │   ├── *_knowledge.md
│   │   └── learning_programs/
│   ├── .cursorrules
│   └── observability/
├── scripts/                  # ✅ Скрипты синхронизации (опционально)
│   ├── sync_team_data.py
│   ├── setup_team_sync.sh
│   └── auto_sync_team_data.sh
└── ... (остальные файлы проекта)
```

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

- **Полная документация:** `docs/TEAM_DATA_SYNC_SYSTEM.md`
- **Быстрый старт:** `docs/QUICK_START_TEAM_SYNC.md`
- **Использование экспертов:** `docs/HOW_TO_USE_EXPERTS_IN_NEW_PROJECT.md`

---

## ✅ ГОТОВО!

Теперь команда экспертов (более 40 сотрудников) автоматически активируется в вашем новом проекте!

**Просто опишите задачу - Виктория сделает всё остальное!** 🚀

---

**Автор:** Виктория (Team Lead)  
**Дата:** 2025-12-14  
**Статус:** ✅ **ГОТОВО К ИСПОЛЬЗОВАНИЮ**

