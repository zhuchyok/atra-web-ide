# 🛡️ ИСПРАВЛЕНИЕ: Преждевременное закрытие позиций при синхронизации

**Дата:** 2025-11-06  
**Проблема:** Позиции закрывались сразу после открытия на бирже из-за задержки появления в API

## 🔴 ПРОБЛЕМА

Пользователь сообщил, что:

1. **19:36** - Позиция SYRUPUSDT SHORT открылась на бирже
2. **19:38:12** - Система отправила алерт "Позиция закрыта на бирже (автосинхронизация)"
3. Но позиция на бирже была открыта и закрылась на бирже

**Причина:** Система синхронизации проверяет позиции на бирже и закрывает локальные позиции, если они не найдены на бирже. Но есть задержка между открытием позиции на бирже и её появлением в API `fetch_positions()`.

## ✅ РЕШЕНИЕ

### 1. Защита от преждевременного закрытия новых позиций

Добавлена проверка времени создания позиции:

- **Не закрываем позиции**, которые были открыты **менее 5 минут назад**
- Это защита от задержки появления позиции в API биржи
- Позиции старше 5 минут закрываются, если не найдены на бирже

### 2. Подробное логирование

Добавлено логирование для отладки:

- Количество позиций, полученных с биржи
- Детали каждой позиции (symbol, contracts)
- Сравнение local_open vs remote_open
- Предупреждение перед закрытием позиции
- Информация о времени создания позиции

## 📝 ИЗМЕНЕНИЯ В КОДЕ

### `main.py` - Функция `_sync_positions_periodically`

```python
# 🛡️ ЗАЩИТА: Не закрываем позиции, которые были открыты менее 5 минут назад
# Это защита от преждевременного закрытия из-за задержки появления позиции на бирже
try:
    from datetime import datetime, timedelta
    import sqlite3

    # Получаем время создания позиции из БД
    with sqlite3.connect(adb_local.db_path) as conn:
        cursor = conn.cursor()
        cursor.execute(
            """
            SELECT entry_time, created_at FROM active_positions
            WHERE symbol = ? AND accepted_by = ?
              AND UPPER(IFNULL(status,'open')) LIKE 'OPEN%'
            ORDER BY created_at DESC LIMIT 1
            """,
            (sym, str(uid))
        )
        result = cursor.fetchone()

        if result:
            entry_time_str = result[0] or result[1]
            if entry_time_str:
                # Парсим время создания
                if isinstance(entry_time_str, str):
                    entry_time = datetime.fromisoformat(entry_time_str.replace('Z', '+00:00'))
                else:
                    entry_time = datetime.fromtimestamp(entry_time_str)

                # Проверяем, прошло ли 5 минут
                time_since_entry = datetime.now() - entry_time.replace(tzinfo=None) if entry_time.tzinfo else datetime.now() - entry_time

                if time_since_entry < timedelta(minutes=5):
                    logger.warning(
                        "⏸️ [SYNC] Позиция %s для пользователя %d слишком новая "
                        "(открыта %s назад, требуется минимум 5 минут). "
                        "Пропускаем закрытие.",
                        sym, uid, time_since_entry
                    )
                    continue
except Exception as e:
    logger.debug(
        "⚠️ [SYNC] Ошибка проверки времени создания позиции %s: %s",
        sym, e
    )
```

### Логирование

```python
# Логирование количества позиций с биржи
logger.debug(
    "🔍 [SYNC] Пользователь %d: получено %d позиций с биржи",
    uid, len(positions or [])
)

# Логирование деталей каждой позиции
logger.debug(
    "🔍 [SYNC] Позиция с биржи: symbol=%s, contracts=%.6f",
    symbol, contracts
)

# Логирование сравнения
logger.info(
    "🔍 [SYNC] Пользователь %d: local_open=%s, remote_open=%s, to_close=%s",
    uid, local_open, open_symbols_remote, to_close
)

# Логирование перед закрытием
logger.warning(
    "⚠️ [SYNC] Пользователь %d: Позиция %s есть локально, но НЕТ на бирже. "
    "Режим: %s. Проверяем время создания...",
    uid, sym, user_mode
)
```

## 🎯 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### До исправления:

- Позиции закрывались сразу после открытия (через 2-3 минуты)
- Пользователи получали ложные алерты о закрытии позиций
- Позиции, которые были открыты на бирже, закрывались локально

### После исправления:

- Позиции не закрываются, если они открыты менее 5 минут назад
- Позиции старше 5 минут закрываются только если действительно не найдены на бирже
- Подробное логирование помогает понять, что происходит при синхронизации

## 🔍 МОНИТОРИНГ

### Ключевые метрики для отслеживания:

1. **Логи синхронизации:**
   - Количество позиций с биржи
   - Сравнение local_open vs remote_open
   - Предупреждения о новых позициях (<5 минут)

2. **Алерты о закрытии:**
   - Позиции не должны закрываться сразу после открытия
   - Позиции старше 5 минут должны закрываться только если действительно не найдены на бирже

3. **Время появления позиций на бирже:**
   - Обычно позиции появляются в API через 1-3 минуты после открытия
   - Защита в 5 минут должна покрывать большинство случаев

## 📋 СЛЕДУЮЩИЕ ШАГИ

1. **Мониторинг 1-2 часа:**
   - Проверить, что новые позиции не закрываются преждевременно
   - Проверить, что старые позиции закрываются корректно
   - Проверить логи синхронизации

2. **Корректировки (при необходимости):**
   - Если 5 минут недостаточно → увеличить до 7-10 минут
   - Если слишком много ложных закрытий → проверить формат символов
   - Если все ок → оставить текущие параметры

---

**Время исправления:** 2025-11-06 19:40 MSK  
**Статус:** ✅ Исправлено и готово к тестированию
