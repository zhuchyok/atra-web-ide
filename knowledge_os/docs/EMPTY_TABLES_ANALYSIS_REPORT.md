# ОТЧЕТ: АНАЛИЗ ПУСТЫХ ТАБЛИЦ В БАЗЕ ДАННЫХ

## 📊 **ОБЩАЯ СТАТИСТИКА**

**Найдено 10 пустых таблиц из 24:**

### ✅ **Таблицы с данными (14):**
- `fees`: 2400 записей
- `quotes`: 11 записей (исправлено!)
- `pairs`: 2400 записей
- `active_signals`: 28 записей
- `signals_log`: 36 записей
- `users_data`: 2 записи
- `app_cache`: 37 записей
- `system_settings`: 20 записей
- `whitelist`: 275 записей
- `blacklist`: 25 записей
- `telemetry_api`: 7204 записей
- `telemetry_cycles`: 27 записей
- `audit_dynamic_params`: 75 записей

### ❌ **Пустые таблицы (10):**

## 🔍 **ДЕТАЛЬНЫЙ АНАЛИЗ ПУСТЫХ ТАБЛИЦ**

### 1. **arbitrage_events: 0 записей**
**Назначение:** Арбитражные события - заполняются при обнаружении арбитража
**Структура:** 10 колонок (id, ts, symbol, buy_exchange, sell_exchange, buy_price, sell_price, amount, net_profit, net_profit_pct)
**Связанные функции:** save_arbitrage, insert_arbitrage

**💡 Возможные причины:**
- Арбитраж не обнаружен
- Функция save_arbitrage не вызывается
- Слишком высокие комиссии
- Нет подходящих пар для арбитража

### 2. **manual_trades: 0 записей**
**Назначение:** Ручные сделки - заполняются при ручной торговле
**Структура:** 20 колонок (детальная информация о сделках)
**Связанные функции:** save_manual_trade, insert_manual_trade

**💡 Возможные причины:**
- Ручная торговля не используется
- Функция save_manual_trade не вызывается
- Пользователи не совершают сделки
- Интерфейс ручной торговли недоступен

### 3. **signals: 0 записей**
**Назначение:** Торговые сигналы - заполняются при генерации сигналов
**Структура:** 8 колонок (id, ts, exchange, symbol, rsi, ema_fast, ema_slow, price)
**Связанные функции:** insert_signal, save_signal

**💡 Возможные причины:**
- Функция insert_signal не вызывается
- Сигналы не генерируются
- Фильтры слишком строгие
- Проблемы с анализом данных

### 4. **signal_accum_events: 0 записей**
**Назначение:** Накопительные события сигналов - для анализа трендов
**Структура:** 7 колонок (id, ts, symbol, event, weight, ttl_sec, meta)
**Связанные функции:** add_signal_event, insert_accum_event

**💡 Возможные причины:**
- Накопительная система не активна
- Функция add_signal_event не вызывается
- События не генерируются
- TTL событий истек

### 5. **market_cap_blacklist: 0 записей**
**Назначение:** Черный список по капитализации - фильтрация монет
**Структура:** 6 колонок (symbol, market_cap, blacklisted_at, last_checked, unfreeze_date, reason)
**Связанные функции:** add_to_blacklist, blacklist_coin

**💡 Возможные причины:**
- Фильтрация по капитализации отключена
- Функция add_to_blacklist не вызывается
- Все монеты проходят фильтр
- Настройки фильтрации не заданы

### 6. **backtest_results: 0 записей**
**Назначение:** Результаты бэктестов - тестирование стратегий
**Структура:** 16 колонок (детальная статистика бэктестов)
**Связанные функции:** save_backtest, insert_backtest

**💡 Возможные причины:**
- Бэктесты не запускались
- Функция save_backtest не вызывается
- Ошибки при выполнении бэктестов
- Нет данных для тестирования

### 7. **pending_check: 0 записей**
**Назначение:** Ожидающие проверки - временные записи
**Структура:** 4 колонки (symbol, attempts, last_check, status)
**Связанные функции:** add_pending, check_pending

**💡 Возможные причины:**
- Система проверок не активна
- Функция add_pending не вызывается
- Все проверки выполнены
- Временные записи очищены

### 8. **audit_strategy_pauses: 0 записей**
**Назначение:** Аудит пауз стратегии - мониторинг системы
**Структура:** 7 колонок (id, ts, action, reason, window_hours, sl_count, net_profit_sum)
**Связанные функции:** log_strategy_pause, audit_pause

**💡 Возможные причины:**
- Аудит стратегий отключен
- Функция log_strategy_pause не вызывается
- Стратегии не останавливались
- Система мониторинга не активна

### 9. **audit_soft_blocklist: 0 записей**
**Назначение:** Аудит мягкого блэклиста - фильтрация
**Структура:** 6 колонок (id, ts, action, symbol, votes, reason)
**Связанные функции:** log_soft_block, audit_block

**💡 Возможные причины:**
- Мягкий блэклист не используется
- Функция log_soft_block не вызывается
- Нет монет для мягкой блокировки
- Система фильтрации не активна

### 10. **audit_active_coins: 0 записей**
**Назначение:** Аудит активных монет - мониторинг
**Структура:** 5 колонок (id, ts, action, symbol, note)
**Связанные функции:** log_active_coin, audit_coin

**💡 Возможные причины:**
- Аудит активных монет отключен
- Функция log_active_coin не вызывается
- Нет активных монет для аудита
- Система мониторинга монет не работает

## 🎯 **КРИТИЧЕСКИЕ ПРОБЛЕМЫ**

### **1. Таблица signals пуста - КРИТИЧНО!**
- **Проблема:** Основные торговые сигналы не сохраняются
- **Влияние:** Нет истории сигналов для анализа
- **Решение:** Проверить вызовы `insert_signal` в коде

### **2. Таблица arbitrage_events пуста**
- **Проблема:** Арбитражные возможности не отслеживаются
- **Влияние:** Упущенная прибыль от арбитража
- **Решение:** Включить мониторинг арбитража

### **3. Таблицы аудита пусты**
- **Проблема:** Нет мониторинга системы
- **Влияние:** Сложно отследить проблемы
- **Решение:** Включить систему аудита

## 💡 **РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ**

### **Немедленные действия:**
1. **Проверить вызовы функций сохранения** в коде
2. **Убедиться, что API бирж работает** корректно
3. **Проверить настройки фильтрации** и порогов
4. **Включить отладочное логирование** для пустых таблиц

### **Долгосрочные улучшения:**
1. **Запустить тестовые функции** для заполнения таблиц
2. **Проверить права доступа** к базе данных
3. **Убедиться, что все зависимости** установлены
4. **Создать систему мониторинга** заполнения таблиц

## 📈 **ПОЛОЖИТЕЛЬНЫЕ МОМЕНТЫ**

### ✅ **Что работает хорошо:**
- **quotes**: 11 записей (исправлено!)
- **signals_log**: 36 записей (основные сигналы записываются)
- **active_signals**: 28 записей (активные сигналы отслеживаются)
- **telemetry_api**: 7204 записей (телеметрия работает)
- **audit_dynamic_params**: 75 записей (динамические параметры отслеживаются)

## 🎯 **ЗАКЛЮЧЕНИЕ**

**Основная проблема:** Некоторые функции сохранения не вызываются или не работают корректно.

**Критично:** Таблица `signals` пуста, что означает потерю важных данных о торговых сигналах.

**Рекомендация:** Провести аудит кода и убедиться, что все функции сохранения вызываются корректно.

**Статус:** ⚠️ **ТРЕБУЕТСЯ ВНИМАНИЕ** - некоторые таблицы не заполняются
