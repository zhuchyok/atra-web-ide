# ✅ АВТОМАТИЧЕСКОЕ ОБНОВЛЕНИЕ .CURSORRULES

**Дата:** 2025-12-14  
**Статус:** ✅ **РЕАЛИЗОВАНО**

---

## 🎯 ПРОБЛЕМА

`.cursorrules` был статическим файлом, который не обновлялся автоматически, когда:
- Добавлялись новые эксперты в БД
- Обновлялись знания экспертов
- Менялись роли и ответственности
- Обновлялись learning progress и другие метрики

---

## ✅ РЕШЕНИЕ

Создана система автоматического обновления `.cursorrules` на основе данных из базы знаний Knowledge OS.

---

## 📦 КОМПОНЕНТЫ

### **1. `knowledge_os/app/cursorrules_generator.py`**

Модуль для генерации `.cursorrules` из БД:

**Функционал:**
- ✅ Чтение экспертов из БД (таблица `experts`)
- ✅ Получение метрик каждого эксперта:
  - Количество знаний
  - Средняя уверенность
  - Количество взаимодействий
  - Средний feedback
- ✅ Получение доменов знаний эксперта
- ✅ Получение топ знаний эксперта
- ✅ Определение уровня эксперта (Guru/Expert/Advanced/Intermediate/Beginner)
- ✅ Генерация секций для каждого эксперта
- ✅ Обновление `.cursorrules` файла

**Уровни экспертов:**
- ⭐⭐⭐⭐⭐ **Guru**: 100+ знаний, confidence ≥ 0.9, 50+ взаимодействий
- ⭐⭐⭐⭐ **Expert**: 50+ знаний, confidence ≥ 0.8, 25+ взаимодействий
- ⭐⭐⭐ **Advanced**: 20+ знаний, confidence ≥ 0.7
- ⭐⭐ **Intermediate**: 10+ знаний
- ⭐ **Beginner**: менее 10 знаний

---

### **2. Интеграция в Nightly Learner**

**ФАЗА 9: UPDATE CURSORRULES** добавлена в `nightly_learner.py`:

```python
# --- ФАЗА 9: UPDATE CURSORRULES (ОБНОВЛЕНИЕ .CURSORRULES) ---
print("📝 Updating .cursorrules from database...")
try:
    from cursorrules_generator import update_cursorrules_file
    await update_cursorrules_file()
except Exception as e:
    print(f"⚠️ .cursorrules update error: {e}")
```

**Расписание:** Обновление происходит каждую ночь после всех других фаз обучения.

---

### **3. Cron задача**

Отдельная задача в cron для обновления `.cursorrules`:

- **Время:** Ежедневно в 4:00 (после nightly_learner)
- **Команда:** `python3 app/cursorrules_generator.py`
- **Лог:** `logs/cursorrules_update.log`

---

## 📊 ЧТО ОБНОВЛЯЕТСЯ

### **Секция каждого эксперта:**

```markdown
### **1. Виктория - Team Lead**
- Уровень: ⭐⭐⭐⭐⭐ Guru
- Департамент: Management
- Знаний в базе: 150
- Средняя уверенность: 0.95
- Взаимодействий: 75
- Средний feedback: 0.92
- Домены: Trading, Architecture, Leadership

- Координация, архитектура, принятие решений
- Анализ задачи и декомпозиция
- Распределение работы между экспертами
- Финальные решения и рекомендации

**Топ знания:**
- 1. Лучшие практики алгоритмической торговли...
- 2. Архитектурные паттерны для высоконагруженных систем...
- 3. Управление командой экспертов...
```

### **Общая статистика:**

- Всего экспертов: X
- Активных: Y
- Всего знаний: Z

---

## 🚀 ИСПОЛЬЗОВАНИЕ

### **Автоматическое обновление:**

`.cursorrules` обновляется автоматически:
1. Каждую ночь через `nightly_learner.py` (ФАЗА 9)
2. Отдельно в cron (ежедневно в 4:00)

### **Ручное обновление:**

```bash
cd /root/knowledge_os
python3 app/cursorrules_generator.py
```

Или локально:

```bash
cd knowledge_os/app
python3 cursorrules_generator.py
```

---

## ⚙️ НАСТРОЙКА

### **Установка на сервере:**

```bash
bash knowledge_os/scripts/setup_cursorrules_auto_update.sh
```

Или вручную:

```bash
ssh root@185.177.216.15
crontab -e

# Добавить:
0 4 * * * cd /root/knowledge_os && python3 app/cursorrules_generator.py >> logs/cursorrules_update.log 2>&1
```

---

## 📝 ЛОГИ

**Логирование:**
- Автоматические обновления: `logs/cursorrules_update.log`
- Обновления через nightly_learner: в основном логе nightly_learner

**Просмотр логов:**

```bash
ssh root@185.177.216.15
tail -f /root/knowledge_os/logs/cursorrules_update.log
```

---

## ✅ ПРОВЕРКА

### **Проверка обновления:**

```bash
# Посмотреть когда последний раз обновлялся
grep "Последнее обновление" .cursorrules

# Проверить количество экспертов
grep "Всего экспертов" .cursorrules

# Проверить конкретного эксперта
grep -A 10 "Виктория - Team Lead" .cursorrules
```

---

## 🔄 ПРОЦЕСС ОБНОВЛЕНИЯ

1. **Чтение данных из БД:**
   - Эксперты (`experts` table)
   - Знания (`knowledge_nodes` table)
   - Взаимодействия (`interaction_logs` table)
   - Домены (`domains` table)

2. **Вычисление метрик:**
   - Количество знаний
   - Средняя уверенность
   - Количество взаимодействий
   - Средний feedback
   - Уровень эксперта

3. **Генерация секций:**
   - Для каждого активного эксперта
   - С актуальными данными
   - С топ знаниями
   - С доменами

4. **Обновление файла:**
   - Сохранение статических разделов
   - Замена секции экспертов
   - Добавление метаданных обновления

---

## 🎯 РЕЗУЛЬТАТ

**Теперь `.cursorrules` всегда актуален:**

- ✅ Автоматически обновляется каждую ночь
- ✅ Отражает актуальное состояние экспертов
- ✅ Включает метрики и уровень каждого эксперта
- ✅ Показывает топ знания экспертов
- ✅ Отображает домены экспертизы
- ✅ Синхронизирован с БД Knowledge OS

---

**Автор:** Виктория (Team Lead)  
**Дата:** 2025-12-14

