# 🟢🔴 ТРЕНД BITCOIN И РЕЖИМЫ ФИЛЬТРОВ - ПОЛНАЯ ЛОГИКА

## 🎯 **ОБЩАЯ СХЕМА:**

### **📊 Настройки тренда Bitcoin:**

```python
# В shared_utils.py
USE_BTC_TREND_FILTER = True  # Включить/выключить фильтр тренда биткоина
BTC_TREND_FILTER_SOFT = True  # True = мягкий фильтр, False = строгий фильтр
```

### **🔍 Типы фильтров тренда Bitcoin:**

#### **🟢 Мягкий фильтр (BTC_TREND_FILTER_SOFT = True):**

```python
def btc_trend_filter_soft(df_btc):
    """Мягкий фильтр тренда биткоина: только цена > EMA200"""
    df_btc["ema200"] = ta.trend.EMAIndicator(df_btc["close"], window=200).ema_indicator()
    df_btc["trend"] = df_btc["close"] > df_btc["ema200"]
    return df_btc["trend"]
```

#### **🔴 Строгий фильтр (BTC_TREND_FILTER_SOFT = False):**

```python
def btc_trend_filter(df_btc):
    """Строгий фильтр тренда биткоина: цена > EMA200 И EMA25 растёт"""
    df_btc["ema200"] = ta.trend.EMAIndicator(df_btc["close"], window=200).ema_indicator()
    df_btc["ema25"] = ta.trend.EMAIndicator(df_btc["close"], window=25).ema_indicator()
    df_btc["trend"] = (df_btc["close"] > df_btc["ema200"]) & (df_btc["ema25"].diff() > 0)
    return df_btc["trend"]
```

---

## 🔄 **ЛОГИКА ГЕНЕРАЦИИ СИГНАЛОВ:**

### **📈 LONG сигналы:**

```python
if signal_type == "LONG" and btc_trend_status:  # Бычий тренд BTC
    print(f"Условия для LONG выполнены + бычий тренд BTC, формируем сигнал!")
    # Генерируем LONG сигнал
```

### **📉 SHORT сигналы:**

```python
elif signal_type == "SHORT" and not btc_trend_status:  # Медвежий тренд BTC
    print(f"Условия для SHORT выполнены + медвежий тренд BTC, формируем сигнал!")
    # Генерируем SHORT сигнал
```

### **❌ Блокировка сигналов:**

```python
else:
    print(f"Сигнал {signal_type} не соответствует тренду BTC")
    # Сигнал НЕ генерируется
```

---

## 🎯 **СВЯЗЬ С РЕЖИМАМИ ФИЛЬТРОВ:**

### **🔍 ВАЖНО: Тренд Bitcoin НЕ привязан к режимам фильтров!**

#### **📊 Режимы фильтров (balanced/soft):**

- **Влияют на технические условия** входа в позицию
- **Определяют строгость** технических индикаторов
- **НЕ влияют** на фильтр тренда Bitcoin

#### **🟢🔴 Фильтр тренда Bitcoin:**

- **Глобальная настройка** для всех пользователей
- **Применяется ко всем** режимам фильтров
- **Независим** от режимов balanced/soft

---

## 📋 **ТАБЛИЦА ПОВЕДЕНИЯ:**

| Режим фильтра | Тренд BTC   | LONG сигналы    | SHORT сигналы   |
| ------------- | ----------- | --------------- | --------------- |
| **balanced**  | 🟢 БЫЧИЙ    | ✅ Генерируются | ❌ Блокируются  |
| **balanced**  | 🔴 МЕДВЕЖИЙ | ❌ Блокируются  | ✅ Генерируются |
| **soft**      | 🟢 БЫЧИЙ    | ✅ Генерируются | ❌ Блокируются  |
| **soft**      | 🔴 МЕДВЕЖИЙ | ❌ Блокируются  | ✅ Генерируются |

---

## 🎯 **ПРИМЕРЫ СЦЕНАРИЕВ:**

### **Сценарий 1: Бычий тренд Bitcoin**

```
Текущий тренд: 🟢 БЫЧИЙ (цена BTC > EMA200)

Пользователь 556251171 (SPOT + soft):
• Технические условия: ✅ Выполнены
• Тренд BTC: ✅ Соответствует LONG
• Результат: ✅ Получает LONG сигнал

Пользователь 958930260 (FUTURES + soft):
• Технические условия: ✅ Выполнены
• Тренд BTC: ✅ Соответствует LONG
• Результат: ✅ Получает LONG сигнал
```

### **Сценарий 2: Медвежий тренд Bitcoin**

```
Текущий тренд: 🔴 МЕДВЕЖИЙ (цена BTC < EMA200)

Пользователь 556251171 (SPOT + soft):
• Технические условия: ✅ Выполнены
• Тренд BTC: ❌ Не соответствует LONG
• Результат: ❌ НЕ получает сигнал

Пользователь 958930260 (FUTURES + soft):
• Технические условия: ✅ Выполнены
• Тренд BTC: ✅ Соответствует SHORT
• Результат: ✅ Получает SHORT сигнал
```

---

## 🔧 **ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ:**

### **1. Определение тренда:**

```python
btc_trend_status = get_btc_trend_status(btc_df, use_soft_filter=BTC_TREND_FILTER_SOFT)
print(f"Тренд BTCUSDT: {'БЫЧИЙ' if btc_trend_status else 'МЕДВЕЖИЙ'} ({'мягкий' if BTC_TREND_FILTER_SOFT else 'строгий'} фильтр)")
```

### **2. Применение фильтра:**

```python
# Для каждого пользователя и режима фильтра
for user_id, user_data in user_data_dict.items():
    for filter_mode in ["balanced", "soft"]:
        signal_type, signal_price = get_entry_signal_by_mode(df, current_index, filter_mode)

        if signal_type == "LONG" and btc_trend_status:
            # Генерируем LONG сигнал
        elif signal_type == "SHORT" and not btc_trend_status:
            # Генерируем SHORT сигнал
        else:
            # Блокируем сигнал
```

### **3. Команды управления:**

```python
# Включить/выключить фильтр
/btc_filter_on  - включить фильтр тренда Bitcoin
/btc_filter_off - выключить фильтр тренда Bitcoin

# Изменить тип фильтра
/btc_filter_soft   - мягкий фильтр (только EMA200)
/btc_filter_strict - строгий фильтр (EMA200 + EMA25)
```

---

## 📊 **ВЛИЯНИЕ НА СИСТЕМУ:**

### **✅ Преимущества:**

- **Защита от ложных сигналов** в неподходящем тренде
- **Улучшение качества сигналов** за счет тренда Bitcoin
- **Глобальная настройка** для всех пользователей

### **⚠️ Особенности:**

- **Может блокировать сигналы** в боковом тренде
- **Не зависит от режимов** фильтров пользователей
- **Применяется ко всем** символам

---

## 🎯 **ИТОГ:**

### **🔑 Ключевые принципы:**

1. **Тренд Bitcoin - глобальный фильтр** для всех пользователей
2. **НЕ зависит от режимов** balanced/soft
3. **LONG сигналы** только при бычьем тренде Bitcoin
4. **SHORT сигналы** только при медвежьем тренде Bitcoin

### **📊 Текущие настройки:**

- **Фильтр включен:** `USE_BTC_TREND_FILTER = True`
- **Мягкий режим:** `BTC_TREND_FILTER_SOFT = True`
- **Логика:** цена BTC > EMA200 = бычий тренд

### **🎯 Результат:**

**Тренд Bitcoin работает как дополнительный фильтр качества сигналов, независимо от режимов фильтров пользователей!**
