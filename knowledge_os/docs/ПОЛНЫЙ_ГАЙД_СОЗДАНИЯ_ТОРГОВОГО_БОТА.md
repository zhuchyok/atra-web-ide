# 🚀 ПОЛНЫЙ ГАЙД СОЗДАНИЯ ТОРГОВОГО БОТА ATRA

## 📋 СОДЕРЖАНИЕ

1. [Обзор системы](#обзор-системы)
2. [Структура проекта](#структура-проекта)
3. [Установка и настройка](#установка-и-настройка)
4. [Основные компоненты](#основные-компоненты)
5. [Конфигурация](#конфигурация)
6. [Telegram бот](#telegram-бот)
7. [Система сигналов](#система-сигналов)
8. [База данных](#база-данных)
9. [API интеграции](#api-интеграции)
10. [Запуск и мониторинг](#запуск-и-мониторинг)

---

## 🎯 ОБЗОР СИСТЕМЫ

### Основные возможности:

- **Автоматическая генерация торговых сигналов** на основе технического анализа
- **Telegram бот** для уведомлений и управления
- **Множественные стратегии**: пробой, возврат к средней, сжатие полос Боллинджера
- **Фильтры риска** и новостные фильтры
- **Отслеживание китов** и анализ объемных блоков
- **Система накапливания сигналов** (DCA)
- **Автоматическая оптимизация** параметров
- **Поддержка SPOT и фьючерсов**

### Технические индикаторы:

- Bollinger Bands (20, 2.0)
- EMA (12, 39, 50)
- RSI (14)
- ATR (15)
- Volume analysis
- Fear & Greed Index

---

## 📁 СТРУКТУРА ПРОЕКТА

```
trading_bot/
├── main.py                 # Главный файл запуска
├── config.py              # Конфигурация системы
├── telegram_bot.py        # Telegram бот
├── signal_live.py         # Генерация сигналов
├── db.py                  # База данных
├── state.py               # Глобальные переменные
├── exchange_api.py        # API бирж
├── ohlc_utils.py          # Получение OHLC данных
├── shared_utils.py        # Общие утилиты
├── user_utils.py          # Управление пользователями
├── requirements.txt       # Зависимости
├── .env                   # Переменные окружения
├── trading.db             # SQLite база данных
├── user_data.json         # Данные пользователей
├── logs/                  # Логи
├── backups/               # Резервные копии
└── system_cache/          # Кэш системы
```

---

## ⚙️ УСТАНОВКА И НАСТРОЙКА

### 1. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Создание файла .env

```env
# Telegram настройки
TELEGRAM_TOKEN=your_bot_token_here
TELEGRAM_TOKEN_DEV=your_dev_bot_token_here
TELEGRAM_CHAT_IDS=[123456789,987654321]

# API ключи (опционально)
ETHERSCAN_API_KEY=your_etherscan_key
BSCSCAN_API_KEY=your_bscscan_key
POLYGONSCAN_API_KEY=your_polygonscan_key
ARBISCAN_API_KEY=your_arbiscan_key

# Новостные API
CRYPTOPANIC_API_KEY=390212cf54403e087e19347f4f3e4a2f4459c79c
NEWSDATA_API_KEY=pub_9259f5b0818a4d40baabae05a908af4f
```

---

## 🔧 ОСНОВНЫЕ КОМПОНЕНТЫ

### 1. main.py - Главный файл

```python
#!/usr/bin/env python3
import asyncio
import logging
import signal
import sys

# Настройка логирования
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('system.log'),
        logging.StreamHandler()
    ]
)

async def main():
    """Основная функция запуска"""
    logger = logging.getLogger(__name__)
    logger.info("🚀 Запуск торговой системы ATRA...")

    # Запуск всех компонентов
    await asyncio.gather(
        run_telegram_bot(),
        run_signals(),
        run_optimization()
    )

if __name__ == "__main__":
    asyncio.run(main())
```

### 2. config.py - Конфигурация

```python
import os
from dotenv import load_dotenv

load_dotenv()

# Основные настройки
DATABASE = "trading.db"
TELEGRAM_TOKEN = os.getenv("TELEGRAM_TOKEN")
TELEGRAM_TOKEN_DEV = os.getenv("TELEGRAM_TOKEN_DEV", "")
TELEGRAM_CHAT_IDS = os.getenv("TELEGRAM_CHAT_IDS")

# Фильтры риска
RISK_FILTERS = {
    "min_volume_24h": 50_000_000,
    "max_spread_pct": 2.0,
    "min_price": 0.01,
    "max_price": 100_000,
    "max_volatility_pct": 15.0,
    "min_profit_pct": 0.5,
    "max_profit_pct": 5.0,
    "enable_risk_filters": True,
}

# Настройки стратегии
ENHANCED_STRATEGY_CONFIG = {
    "bb_window": 20,
    "bb_std": 2.0,
    "ema_fast": 12,
    "ema_slow": 39,
    "ema_trend": 50,
    "rsi_window": 14,
    "rsi_overbought": 90,
    "rsi_oversold": 10,
    "atr_window": 15,
    "atr_multiplier_sl": 1.7,
}
```

---

## 🤖 TELEGRAM БОТ

### Основные команды:

- `/start` - Запуск бота
- `/help` - Справка
- `/balance` - Показать баланс
- `/deposit <amount>` - Установить депозит
- `/positions` - Активные позиции
- `/trade_history` - История сделок
- `/settings` - Настройки
- `/myreport` - Персональный отчет

### Кнопки:

- **Принять** - Принять сигнал
- **Отклонить** - Отклонить сигнал
- **Закрыть** - Закрыть позицию
- **DCA** - Докупить позицию

### Структура telegram_bot.py:

```python
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import Application, CommandHandler, CallbackQueryHandler

class TradingBot:
    def __init__(self):
        self.app = Application.builder().token(TOKEN).build()
        self.setup_handlers()

    def setup_handlers(self):
        """Настройка обработчиков команд"""
        self.app.add_handler(CommandHandler("start", self.start_command))
        self.app.add_handler(CommandHandler("balance", self.balance_command))
        self.app.add_handler(CommandHandler("deposit", self.deposit_command))
        self.app.add_handler(CallbackQueryHandler(self.button_handler))

    async def start_command(self, update: Update, context):
        """Обработка команды /start"""
        keyboard = [
            [InlineKeyboardButton("💰 Баланс", callback_data="balance")],
            [InlineKeyboardButton("⚙️ Настройки", callback_data="settings")],
            [InlineKeyboardButton("📊 Отчет", callback_data="report")]
        ]
        reply_markup = InlineKeyboardMarkup(keyboard)

        await update.message.reply_text(
            "🚀 Добро пожаловать в торговую систему ATRA!\n"
            "Выберите действие:",
            reply_markup=reply_markup
        )
```

---

## 📊 СИСТЕМА СИГНАЛОВ

### Основные стратегии:

#### 1. Пробой полос Боллинджера

```python
def breakout_strategy(df):
    """Стратегия пробоя полос Боллинджера"""
    bb_upper = df['bb_upper'].iloc[-1]
    bb_lower = df['bb_lower'].iloc[-1]
    close = df['close'].iloc[-1]

    # LONG сигнал
    if close > bb_upper and df['volume'].iloc[-1] > df['volume'].rolling(20).mean().iloc[-1]:
        return "LONG"

    # SHORT сигнал
    if close < bb_lower and df['volume'].iloc[-1] > df['volume'].rolling(20).mean().iloc[-1]:
        return "SHORT"

    return None
```

#### 2. Возврат к средней

```python
def mean_reversion_strategy(df):
    """Стратегия возврата к средней"""
    bb_middle = df['bb_middle'].iloc[-1]
    close = df['close'].iloc[-1]
    rsi = df['rsi'].iloc[-1]

    # LONG сигнал
    if close < bb_middle and rsi < 30:
        return "LONG"

    # SHORT сигнал
    if close > bb_middle and rsi > 70:
        return "SHORT"

    return None
```

### Генерация сигналов:

```python
async def generate_signals():
    """Генерация торговых сигналов"""
    # Получаем список монет
    symbols = await get_filtered_top_usdt_pairs()

    signals = []
    for symbol in symbols:
        # Получаем OHLC данные
        ohlc_data = await get_ohlc_data(symbol)
        df = create_dataframe(ohlc_data)

        # Добавляем индикаторы
        df = add_indicators(df)

        # Проверяем стратегии
        signal = check_strategies(df, symbol)
        if signal:
            signals.append(signal)

    return signals
```

---

## 🗄️ БАЗА ДАННЫХ

### Структура SQLite:

```sql
-- Таблица сигналов
CREATE TABLE signals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ts TEXT,
    symbol TEXT,
    signal_type TEXT,
    entry_price REAL,
    tp1 REAL,
    tp2 REAL,
    sl REAL,
    status TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Таблица сделок
CREATE TABLE trades (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    signal_id INTEGER,
    user_id INTEGER,
    symbol TEXT,
    side TEXT,
    entry_price REAL,
    exit_price REAL,
    quantity REAL,
    profit REAL,
    status TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Таблица пользователей
CREATE TABLE users (
    id INTEGER PRIMARY KEY,
    username TEXT,
    deposit REAL DEFAULT 0,
    balance REAL DEFAULT 0,
    settings TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Класс Database:

```python
class Database:
    def __init__(self, db_path="trading.db"):
        self.conn = sqlite3.connect(db_path)
        self.cursor = self.conn.cursor()
        self.init_tables()

    def init_tables(self):
        """Инициализация таблиц"""
        self.cursor.execute("""
            CREATE TABLE IF NOT EXISTS signals (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                ts TEXT,
                symbol TEXT,
                signal_type TEXT,
                entry_price REAL,
                tp1 REAL,
                tp2 REAL,
                sl REAL,
                status TEXT,
                created_at DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        self.conn.commit()

    def save_signal(self, signal_data):
        """Сохранение сигнала"""
        self.cursor.execute("""
            INSERT INTO signals (ts, symbol, signal_type, entry_price, tp1, tp2, sl, status)
            VALUES (?, ?, ?, ?, ?, ?, ?, ?)
        """, (
            signal_data['timestamp'],
            signal_data['symbol'],
            signal_data['type'],
            signal_data['entry_price'],
            signal_data['tp1'],
            signal_data['tp2'],
            signal_data['sl'],
            'active'
        ))
        self.conn.commit()
```

---

## 🔌 API ИНТЕГРАЦИИ

### 1. Binance API

```python
class BinanceAPI:
    BASE_URL = "https://api.binance.com"

    @staticmethod
    async def get_ohlc(symbol, interval="1h", limit=300):
        """Получение OHLC данных"""
        url = f"{BinanceAPI.BASE_URL}/api/v3/klines"
        params = {
            "symbol": symbol,
            "interval": interval,
            "limit": limit
        }

        async with aiohttp.ClientSession() as session:
            async with session.get(url, params=params) as response:
                data = await response.json()
                return [
                    {
                        "timestamp": int(item[0]),
                        "open": float(item[1]),
                        "high": float(item[2]),
                        "low": float(item[3]),
                        "close": float(item[4]),
                        "volume": float(item[5])
                    }
                    for item in data
                ]

    @staticmethod
    async def get_prices(symbols):
        """Получение текущих цен"""
        url = f"{BinanceAPI.BASE_URL}/api/v3/ticker/bookTicker"

        async with aiohttp.ClientSession() as session:
            async with session.get(url) as response:
                data = await response.json()
                return {item["symbol"]: float(item["bidPrice"]) for item in data}
```

### 2. Новостные API

```python
class NewsAPI:
    def __init__(self):
        self.cryptopanic_key = os.getenv("CRYPTOPANIC_API_KEY")
        self.newsdata_key = os.getenv("NEWSDATA_API_KEY")

    async def get_crypto_news(self, symbol):
        """Получение новостей по криптовалюте"""
        url = f"https://cryptopanic.com/api/v1/posts/"
        params = {
            "auth_token": self.cryptopanic_key,
            "currencies": symbol.replace("USDT", ""),
            "filter": "hot"
        }

        async with aiohttp.ClientSession() as session:
            async with session.get(url, params=params) as response:
                data = await response.json()
                return data.get("results", [])
```

---

## 🚀 ЗАПУСК И МОНИТОРИНГ

### 1. Запуск системы

```bash
# Активация виртуального окружения
source venv/bin/activate

# Запуск бота
python main.py
```

### 2. Мониторинг логов

```bash
# Просмотр логов в реальном времени
tail -f system.log

# Поиск ошибок
grep "ERROR" system.log

# Поиск сигналов
grep "SIGNAL" system.log
```

### 3. Автоматический перезапуск

```bash
# Создание systemd сервиса
sudo nano /etc/systemd/system/trading-bot.service

[Unit]
Description=Trading Bot ATRA
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/trading_bot
Environment=PATH=/path/to/trading_bot/venv/bin
ExecStart=/path/to/trading_bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target

# Активация сервиса
sudo systemctl enable trading-bot
sudo systemctl start trading-bot
sudo systemctl status trading-bot
```

---

## 📈 ОПТИМИЗАЦИЯ И НАСТРОЙКА

### 1. Настройка фильтров

```python
# В config.py
ENHANCED_STRATEGY_CONFIG = {
    # Для агрессивной торговли
    "rsi_overbought": 85,  # Было 90
    "rsi_oversold": 15,    # Было 10

    # Для консервативной торговли
    "rsi_overbought": 95,  # Более строгий
    "rsi_oversold": 5,     # Более строгий
}
```

### 2. Настройка риск-менеджмента

```python
RISK_FILTERS = {
    "min_volume_24h": 100_000_000,  # Увеличить минимальный объем
    "max_volatility_pct": 10.0,     # Уменьшить максимальную волатильность
    "min_profit_pct": 1.0,          # Увеличить минимальную прибыль
}
```

### 3. Автоматическая оптимизация

```python
# Система автоматически оптимизирует параметры на основе:
# - Винрейт последних сделок
# - Средняя прибыль
# - Максимальная просадка
# - Количество сигналов
```

---

## 🔒 БЕЗОПАСНОСТЬ

### 1. Защита API ключей

- Все ключи хранятся в .env файле
- .env добавлен в .gitignore
- Используются переменные окружения

### 2. Валидация данных

```python
def validate_signal(signal_data):
    """Валидация торгового сигнала"""
    required_fields = ['symbol', 'type', 'entry_price', 'tp1', 'sl']

    for field in required_fields:
        if field not in signal_data:
            raise ValueError(f"Отсутствует обязательное поле: {field}")

    if signal_data['entry_price'] <= 0:
        raise ValueError("Цена входа должна быть положительной")

    return True
```

### 3. Лимиты риска

```python
GLOBAL_RISK_LIMIT_PCT = 30  # Максимум 30% депозита в позициях
MAX_POSITIONS_PER_SYMBOL = 1
MAX_DAILY_TRADES = 10
```

---

## 📊 АНАЛИТИКА И ОТЧЕТЫ

### 1. Ежедневные отчеты

```python
async def generate_daily_report():
    """Генерация ежедневного отчета"""
    today = datetime.now().strftime("%Y-%m-%d")

    # Статистика сигналов
    signals_count = db.get_signals_count(today)
    signals_profit = db.get_signals_profit(today)

    # Статистика сделок
    trades_count = db.get_trades_count(today)
    trades_profit = db.get_trades_profit(today)

    report = f"""
📊 ЕЖЕДНЕВНЫЙ ОТЧЕТ {today}

📈 СИГНАЛЫ:
• Всего: {signals_count}
• Прибыль: {signals_profit:.2f} USDT

💰 СДЕЛКИ:
• Всего: {trades_count}
• Прибыль: {trades_profit:.2f} USDT

📊 ОБЩАЯ ПРИБЫЛЬ: {signals_profit + trades_profit:.2f} USDT
    """

    return report
```

### 2. Анализ производительности

```python
def analyze_performance():
    """Анализ производительности системы"""
    # Время выполнения функций
    # Использование памяти
    # Количество API запросов
    # Успешность сигналов
    pass
```

---

## 🛠️ УСТРАНЕНИЕ НЕПОЛАДОК

### 1. Частые проблемы

#### Ошибка подключения к Telegram

```bash
# Проверка токена
curl "https://api.telegram.org/bot<YOUR_TOKEN>/getMe"

# Очистка webhook
curl "https://api.telegram.org/bot<YOUR_TOKEN>/deleteWebhook"
```

#### Ошибки API бирж

```python
# Добавить retry логику
async def get_ohlc_with_retry(symbol, max_retries=3):
    for attempt in range(max_retries):
        try:
            return await get_ohlc(symbol)
        except Exception as e:
            if attempt == max_retries - 1:
                raise e
            await asyncio.sleep(2 ** attempt)
```

#### Проблемы с базой данных

```bash
# Проверка целостности
sqlite3 trading.db "PRAGMA integrity_check;"

# Резервное копирование
sqlite3 trading.db ".backup backup_$(date +%Y%m%d_%H%M%S).db"
```

### 2. Логи и отладка

```python
# Включение подробного логирования
logging.basicConfig(
    level=logging.DEBUG,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)

# Логирование в файл
logging.FileHandler('debug.log')
```

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

### 1. Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Binance API](https://binance-docs.github.io/apidocs/spot/en/)
- [Technical Analysis Library](https://technical-analysis-library-in-python.readthedocs.io/)

### 2. Рекомендуемые книги

- "Technical Analysis of the Financial Markets" - John J. Murphy
- "Building Algorithmic Trading Systems" - Kevin J. Davey
- "Python for Finance" - Yves Hilpisch

### 3. Сообщества

- Telegram каналы по криптотрейдингу
- Reddit r/algotrading
- Stack Overflow для технических вопросов

---

## 🎉 ЗАКЛЮЧЕНИЕ

Этот гайд содержит полную информацию для создания торгового бота с нуля. Система включает:

✅ **Полную архитектуру** с модульной структурой
✅ **Telegram интеграцию** с кнопками и командами
✅ **Множественные стратегии** торговли
✅ **Систему риск-менеджмента**
✅ **Базу данных** для хранения данных
✅ **API интеграции** с биржами
✅ **Автоматическую оптимизацию**
✅ **Мониторинг и логирование**
✅ **Безопасность** и валидацию

Для запуска следуйте инструкциям по порядку, начиная с установки зависимостей и заканчивая настройкой автоматического перезапуска.

**Удачи в торговле! 🚀📈**
