# 🎯 АВТОМАТИЧЕСКАЯ ОПТИМИЗАЦИЯ ПАРАМЕТРОВ ATRA

## 📋 Обзор системы

Система автоматической оптимизации ATRA предназначена для постоянного улучшения параметров торговой стратегии на основе анализа исторических данных и текущей производительности.

### 🔧 Что делает система:

1. **Анализирует текущую производительность** стратегии
2. **Оптимизирует параметры** для топ-50 монет по объему
3. **Тестирует новые параметры** на исторических данных
4. **Применяет лучшие параметры** если они дают улучшение >5%
5. **Автоматически перезапускает бота** после применения новых параметров
6. **Ведет историю оптимизаций** в папке `optimization_history/`

## 🚀 Быстрый старт

### 1. Включение автоматической оптимизации

В файле `config.py` установите:
```python
"auto_optimization": True,   # Автоматическая оптимизация параметров
```

### 2. Запуск планировщика

```bash
# Запуск через демон (рекомендуется)
./start_auto_optimization_daemon.sh start

# Или прямой запуск
python3 auto_optimization_scheduler.py
```

### 3. Проверка статуса

```bash
./start_auto_optimization_daemon.sh status
```

## 📅 Расписание оптимизации

### Ежедневная оптимизация
- **Время:** 06:00 утра
- **Тип:** Быстрая оптимизация (25 испытаний)
- **Цель:** Поддержание производительности

### Еженедельная полная оптимизация
- **Время:** Воскресенье 08:00
- **Тип:** Полная оптимизация (100 испытаний)
- **Цель:** Глубокий анализ и улучшение

### Проверка производительности
- **Частота:** Каждые 4 часа
- **Действие:** Мониторинг без оптимизации
- **Автооптимизация:** При падении производительности ниже порога

## 🛠️ Управление системой

### Команды демона

```bash
# Запуск
./start_auto_optimization_daemon.sh start

# Остановка
./start_auto_optimization_daemon.sh stop

# Перезапуск
./start_auto_optimization_daemon.sh restart

# Статус
./start_auto_optimization_daemon.sh status

# Справка
./start_auto_optimization_daemon.sh help
```

### Ручное управление

```bash
# Разовая оптимизация
python3 start_auto_optimization.py

# Прямой запуск планировщика
python3 auto_optimization_scheduler.py
```

## 📊 Мониторинг и логи

### Файлы логов

- `auto_optimization.log` - Основные логи оптимизации
- `auto_optimization_scheduler.log` - Логи планировщика
- `auto_optimization_daemon.log` - Логи демона

### Просмотр логов

```bash
# Последние записи
tail -f auto_optimization.log

# Поиск ошибок
grep "ERROR" auto_optimization.log

# Статистика оптимизаций
grep "✅.*оптимизирован" auto_optimization.log
```

### Файлы результатов

- `optimized_parameters.json` - Текущие оптимизированные параметры
- `optimized_config.py` - Конфигурация с оптимизированными параметрами
- `optimization_history/` - История всех оптимизаций

## ⚙️ Настройка параметров

### Параметры оптимизации

В файле `auto_optimization_scheduler.py`:

```python
class AutoOptimizationScheduler:
    def __init__(self):
        self.performance_threshold = 0.6  # Минимальная оценка для применения
        self.config.optimization_trials = 25  # Количество испытаний
```

### Оптимизируемые параметры

- **EMA быстрая/медленная** - Периоды экспоненциальных скользящих средних
- **RSI период/максимум** - Параметры индикатора относительной силы
- **ATR период** - Период среднего истинного диапазона
- **TP/SL множители** - Множители для take profit и stop loss
- **Минимальный объем** - Фильтр по объему торгов

## 🔍 Анализ результатов

### Оценка производительности

Система использует комбинированную оценку:
```python
score = (
    win_rate * 0.3 +                    # Винрейт 30%
    min(profit_factor, 3) / 3 * 0.3 +   # Фактор прибыли 30%
    (1 - max_drawdown) * 0.2 +          # Максимальная просадка 20%
    max(0, sharpe_ratio) / 2 * 0.2      # Коэффициент Шарпа 20%
)
```

### Критерии применения параметров

Новые параметры применяются только если:
- Улучшение производительности > 5%
- Оценка выше порогового значения (0.6)
- Успешная оптимизация > 80% монет

## 🚨 Устранение неполадок

### Проблема: Планировщик не запускается

```bash
# Проверка зависимостей
pip3 install schedule optuna

# Проверка прав доступа
chmod +x start_auto_optimization_daemon.sh

# Проверка логов
tail -f auto_optimization_daemon.log
```

### Проблема: Ошибки оптимизации

```bash
# Проверка подключения к интернету
ping 8.8.8.8

# Проверка API ключей
python3 -c "from exchange_api import get_top_usdt_pairs_by_volume; print(get_top_usdt_pairs_by_volume(5))"
```

### Проблема: Параметры не применяются

```bash
# Проверка файлов конфигурации
ls -la optimized_parameters.json
ls -la config.py

# Проверка прав на запись
ls -la config.py
```

## 📈 Статистика и отчеты

### Просмотр статистики

```bash
# Через скрипт
python3 start_auto_optimization.py
# Выберите пункт 3 - "Показать статистику оптимизаций"

# Прямой вызов
python3 -c "from auto_optimization_scheduler import AutoOptimizationScheduler; scheduler = AutoOptimizationScheduler(); print(scheduler.get_optimization_stats())"
```

### Анализ истории

```bash
# Просмотр истории оптимизаций
ls -la optimization_history/

# Анализ конкретной оптимизации
cat optimization_history/optimization_20250807_193000.json
```

## 🔧 Расширенная настройка

### Изменение расписания

Отредактируйте файл `auto_optimization_scheduler.py`:

```python
def start_scheduler(self):
    # Ежедневная оптимизация в 6:00 утра
    schedule.every().day.at("06:00").do(self.run_optimization_cycle)

    # Еженедельная полная оптимизация в воскресенье в 8:00
    schedule.every().sunday.at("08:00").do(self.run_full_optimization)

    # Каждые 4 часа проверка производительности
    schedule.every(4).hours.do(self.performance_check)
```

### Настройка списка монет

```python
def __init__(self):
    # Получаем топ-50 монет по объему
    self.symbols = get_top_usdt_pairs_by_volume(limit=50)

    # Или используйте фиксированный список
    self.symbols = ['BTCUSDT', 'ETHUSDT', 'BNBUSDT', ...]
```

### Автоматический перезапуск бота

Система автоматически создает файл-сигнал для перезапуска:
```python
def _signal_bot_restart(self):
    restart_signal_file = Path("bot_restart_signal.txt")
    restart_signal_file.write_text(f"restart_requested_{datetime.now().isoformat()}")
```

## 📞 Поддержка

### Полезные команды

```bash
# Полная диагностика системы
./start_auto_optimization_daemon.sh status

# Проверка всех процессов
ps aux | grep -E "(auto_optimization|scheduler)" | grep -v grep

# Очистка логов
> auto_optimization.log
> auto_optimization_scheduler.log
```

### Контакты

При возникновении проблем:
1. Проверьте логи системы
2. Убедитесь в корректности настроек
3. Проверьте подключение к интернету
4. Убедитесь в наличии всех зависимостей

---

**🎯 Система автоматической оптимизации ATRA готова к работе!**

*Последнее обновление: 2025-08-07*