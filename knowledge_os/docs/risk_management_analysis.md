# 🛡️ АНАЛИЗ СИСТЕМЫ УПРАВЛЕНИЯ РИСКАМИ

## ✅ **ДА! СИСТЕМА УЧИТЫВАЕТ РИСКИ И СВОБОДНЫЙ БАЛАНС**

### 📊 **КАК СИСТЕМА УЧИТЫВАЕТ ОТКРЫТЫЕ/ЗАКРЫТЫЕ ПОЗИЦИИ:**

#### **1. РАСЧЕТ ДОСТУПНЫХ СРЕДСТВ** (`get_available_funds`)

```python
def get_available_funds(user_data: dict, trade_mode: str = "spot") -> float:
    """
    Возвращает доступные средства для торговли с учетом открытых позиций.
    """
    deposit = float(user_data.get("deposit", 0))
    if deposit <= 0:
        return 0.0

    # Рассчитываем занятые средства
    total_used = 0.0
    positions = user_data.get("positions", []) + user_data.get("open_positions", [])

    for position in positions:
        if position.get("status") == "open":
            if trade_mode == "futures":
                # Для фьючерсов - маржа (залоговые средства)
                total_used += position.get("margin", 0)
            else:
                # Для спота - полная стоимость позиции
                total_used += position.get("qty", 0) * position.get("entry_price", 0)

    # Резерв капитала
    reserve_pct = 0.15 if trade_mode == "spot" else 0.10
    reserve = deposit * reserve_pct

    available = deposit - total_used - reserve
    return max(0.0, available)
```

#### **2. ПРОВЕРКА ДОСТУПНЫХ СРЕДСТВ ПЕРЕД НОВЫМИ СИГНАЛАМИ**

```python
def get_available_funds_for_signal(user_data: dict, trade_mode: str = "spot") -> float:
    """
    Возвращает доступные средства для новых сигналов с учетом открытых позиций.
    """
    # Аналогичная логика, но с дополнительными проверками
    available_funds = get_available_funds(user_data, trade_mode)

    # Минимум 5% депозита должно быть свободным
    min_required = deposit * 0.05
    if available_funds < min_required:
        return 0.0

    return available_funds
```

### 🎯 **ЗАЩИТНЫЕ МЕХАНИЗМЫ:**

#### **1. РЕЗЕРВЫ КАПИТАЛА:**

- **Спот**: 15% депозита всегда свободно
- **Фьючерсы**: 10% депозита всегда свободно

#### **2. ОГРАНИЧЕНИЯ ПО ДОСТУПНЫМ СРЕДСТВАМ:**

- **Новые сигналы**: Максимум 80% от доступных средств
- **DCA**: Максимум 80% от доступных средств
- **Минимум**: 5% депозита должно быть свободным

#### **3. ПРОВЕРКА МАРЖИ (ФЬЮЧЕРСЫ):**

```python
# Для фьючерсов проверяем, что маржа не превышает доступные средства
min_margin_required = available_funds * 0.1  # Минимум 10% от доступных средств как маржа
if final_leverage > 1:
    effective_margin = available_funds / final_leverage
    if effective_margin < min_margin_required:
        final_leverage = max(1, int(available_funds / min_margin_required))
```

### 📈 **АДАПТИВНОЕ УПРАВЛЕНИЕ РИСКАМИ:**

#### **1. ПО ТИРУ ДЕПОЗИТА:**

- **$100-500**: Высокие риски (8-15%), но с защитой
- **$1000-5000**: Средние риски (2-10%)
- **$10000+**: Низкие риски (2-6%)

#### **2. ПО РЕЖИМУ РЫНКА:**

- **Bull market**: +10% к плечу, +5% к размеру входа
- **Bear market**: -20% к плечу, -30% к размеру входа
- **Neutral**: Стандартные параметры

#### **3. ПО ВОЛАТИЛЬНОСТИ:**

- **Высокая волатильность**: Снижение плеча и риска
- **Низкая волатильность**: Увеличение плеча и риска

### 🛡️ **ПРИМЕРЫ РАБОТЫ СИСТЕМЫ:**

#### **ПРИМЕР 1: МАЛЫЙ ДЕПОЗИТ ($500)**

```python
user_data = {
    "deposit": 500,
    "positions": [
        {"status": "open", "qty": 0.1, "entry_price": 50000, "margin": 0}
    ],
    "open_positions": []
}

# Результат:
# Доступные средства: 425.0 (500 - 50 - 25 резерв)
# Риск: 8.0% (ограничен доступными средствами)
# Плечо: 1x (спот)
# Сумма входа: 40.0 USD
```

#### **ПРИМЕР 2: СРЕДНИЙ ДЕПОЗИТ ($5000)**

```python
user_data = {
    "deposit": 5000,
    "positions": [
        {"status": "open", "qty": 0.5, "entry_price": 3000, "margin": 150},
        {"status": "open", "qty": 1.0, "entry_price": 2500, "margin": 200}
    ],
    "open_positions": []
}

# Результат:
# Доступные средства: 4150.0 (5000 - 350 - 500 резерв)
# Риск: 4.0% (адаптивный)
# Плечо: 6x (фьючерсы)
# Сумма входа: 200.0 USD
```

### 🎯 **КЛЮЧЕВЫЕ ПРИНЦИПЫ:**

#### **1. ЗАЩИТА КАПИТАЛА:**

- ✅ Резервы капитала (10-15%)
- ✅ Ограничения по доступным средствам
- ✅ Минимальные требования к свободным средствам

#### **2. АДАПТИВНОСТЬ:**

- ✅ Учет размера депозита
- ✅ Учет режима рынка
- ✅ Учет волатильности
- ✅ Учет открытых позиций

#### **3. КОНТРОЛЬ РИСКОВ:**

- ✅ Максимальные лимиты плеча
- ✅ Максимальные лимиты риска
- ✅ Проверка доступных средств
- ✅ Валидация перед каждой сделкой

## ✅ **ВЫВОД: СИСТЕМА ПОЛНОСТЬЮ УЧИТЫВАЕТ РИСКИ!**

**Система автоматически:**

1. **Рассчитывает доступные средства** с учетом открытых позиций
2. **Ограничивает размеры сделок** доступными средствами
3. **Создает резервы капитала** для защиты
4. **Адаптирует параметры** под условия рынка
5. **Предотвращает переторговлю** и превышение лимитов

**Система готова к безопасной торговле! 🛡️**
