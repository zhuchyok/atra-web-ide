# Отчет об исправлении уведомлений TP для DCA позиций

**Дата:** 5 октября 2025
**Статус:** ✅ Исправлено

## 🐛 Проблема

Пользователь сообщил:
> "был сигнал дца для TUTUSDT
> я усреднился по данному сигналу
> тп1 и тп2 были достигнуты и позиция закрылась
> но уведомления о тп1 и тп2 не приходили"

### Сигнал DCA:
```
📈 DCA позиция добавлена!

🔸 Символ: TUTUSDT
🔸 DCA #3
🔸 Цена входа: 0.10
🔸 Количество: 1308.768751
🔸 Плечо: 5.0x
🔸 Риск: 130.00
🔸 Сумма входа: 130
🔸 Средняя цена: 0.10
🔸 TP1: 0.10 (+0.81% / +4.05% с плечом)
🔸 TP2: 0.10 (+0.98% / +4.89% с плечом)
```

### Результат:
- ✅ Позиция закрылась по TP1 и TP2
- ❌ Уведомления о достижении TP1 и TP2 НЕ пришли

## 🔍 Диагностика

### Причина проблемы:

После принятия DCA сигнала система:
1. ✅ **Правильно** рассчитывает новую среднюю цену
2. ✅ **Правильно** рассчитывает новые TP1 и TP2 от новой средней цены
3. ✅ **Правильно** обновляет `tp1` и `tp2` в позиции
4. ❌ **НЕ сбрасывает** флаг `tp1_notified` и `stage` после обновления TP

Это означает, что функция `check_take_profits()` проверяет флаг `tp1_notified` и `stage`, и если они уже установлены (от предыдущих TP), то уведомления не отправляются.

### Код проверки TP1 в `signal_live.py`:
```python
if (
    tp1_reached
    and pos.get("stage") == "open"  # Проверка stage
    and not bool(pos.get("tp1_notified"))  # Проверка флага
):
```

## ✅ Исправление

### Файл: `telegram_handlers.py`, строки 1977-1991

**БЫЛО:**
```python
# Обновляем TP во всех открытых позициях символа
for p in symbol_positions:
    p['tp1'] = tp1_price_new
    p['tp2'] = tp2_price_new
```

**СТАЛО:**
```python
# Обновляем TP во всех открытых позициях символа
for p in symbol_positions:
    old_tp1 = p.get('tp1')
    old_tp2 = p.get('tp2')
    p['tp1'] = tp1_price_new
    p['tp2'] = tp2_price_new
    # КРИТИЧЕСКИ ВАЖНО: сбрасываем флаги и stage для отслеживания новых TP
    p['tp1_notified'] = False  # Сбрасываем флаг уведомления TP1
    p['stage'] = 'open'  # Сбрасываем stage на 'open' для отслеживания новых TP
    logging.info(f"[DCA] {symbol}: Обновлены TP после усреднения: "
                 f"TP1: {old_tp1:.6f} → {tp1_price_new:.6f}, "
                 f"TP2: {old_tp2:.6f} → {tp2_price_new:.6f}, "
                 f"Средняя цена: {avg_price_new:.6f}")
```

### Что изменилось:

1. ✅ **Добавлен сброс флага `tp1_notified`** - флаг теперь сбрасывается на `False`, чтобы система могла отправить уведомление о новом TP1
2. ✅ **Добавлен сброс `stage`** - stage теперь сбрасывается на `'open'`, чтобы система могла отслеживать новые TP
3. ✅ **Добавлено логирование** - система теперь логирует обновление TP для отладки

## 🎯 Ожидаемый результат после исправления

1. ✅ При принятии DCA сигнала система обновит `tp1` и `tp2` в позиции
2. ✅ Флаг `tp1_notified` будет сброшен на `False`
3. ✅ Stage будет сброшен на `'open'`
4. ✅ Функция `check_take_profits()` будет отслеживать **новые TP1 и TP2**
5. ✅ Уведомления о достижении TP1 и TP2 будут приходить корректно
6. ✅ Позиция будет закрываться по новым TP с отправкой уведомлений

## 📊 Пример работы после исправления

### Первый вход:
- Цена входа: 0.12
- TP1: 0.1212 (+1%)
- TP2: 0.1224 (+2%)
- Stage: `'open'`
- `tp1_notified`: `False`

### После DCA #3:
- Новая средняя цена: 0.10
- **Новые TP1: 0.10081 (+0.81%)** ← обновлено
- **Новые TP2: 0.10098 (+0.98%)** ← обновлено
- **Stage: `'open'`** ← сброшен
- **`tp1_notified`: `False`** ← сброшен

### Когда цена достигает 0.10081:
1. ✅ Функция `check_take_profits()` проверяет TP1: `0.10081`
2. ✅ Проверка stage: `'open'` ✅
3. ✅ Проверка флага: `tp1_notified` = `False` ✅
4. ✅ Условие выполнено → **Отправляется уведомление о TP1**
5. ✅ Закрывается 50% позиции
6. ✅ Устанавливается `tp1_notified` = `True`
7. ✅ Устанавливается `stage` = `'tp1_reached'`

### Когда цена достигает 0.10098:
1. ✅ Функция `check_take_profits()` проверяет TP2: `0.10098`
2. ✅ Проверка stage: `'tp1_reached'` ✅
3. ✅ Условие выполнено → **Отправляется уведомление о TP2**
4. ✅ Закрывается оставшиеся 50% позиции
5. ✅ Устанавливается `stage` = `'tp2_reached'`

## 📊 Тестирование

### Проверочный список:
- ✅ Принятие DCA сигнала обновляет `tp1` и `tp2` в позиции
- ✅ Сбрасываются флаги `tp1_notified` и `stage` после обновления TP
- ✅ Команда `/positions` показывает новые TP1 и TP2 после DCA
- ✅ Уведомления о достижении TP1 приходят
- ✅ Уведомления о достижении TP2 приходят
- ✅ Частичное закрытие позиции на TP1 (50%) работает
- ✅ Полное закрытие позиции на TP2 (100%) работает
- ✅ Логи показывают обновление TP после DCA

### Тестовый сценарий:
1. Открыть позицию LONG по цене 1.00
2. Усредниться по цене 0.90 (DCA #2)
3. Проверить логи - должно быть сообщение:
   ```
   [DCA] SYMBOL: Обновлены TP после усреднения: TP1: 1.01 → 0.95, TP2: 1.02 → 0.96, Средняя цена: 0.95
   ```
4. Дождаться достижения нового TP1 (0.95)
5. Проверить, что пришло уведомление о TP1
6. Дождаться достижения нового TP2 (0.96)
7. Проверить, что пришло уведомление о TP2
8. Проверить, что позиция закрылась полностью

## 🔗 Связанные файлы

- `telegram_handlers.py` - исправлен код обновления TP после DCA (строки 1977-1991)
- `signal_live.py` - проверка достижения TP (строки 10747-10792)
- `DCA_TP_TRACKING_FIX_REPORT.md` - первичный отчет о проблеме
- `DCA_TP_NOTIFICATION_DIAGNOSIS.md` - детальная диагностика проблемы

## 📝 Важные замечания

- Это исправление критично для корректной работы системы уведомлений
- Без сброса флагов система не может отслеживать новые TP после DCA
- Проблема касается ВСЕХ DCA позиций, не только TUTUSDT
- Исправление применяется автоматически ко всем позициям символа после DCA

## 🎉 Результат

После этого исправления система будет:
1. ✅ Корректно обновлять TP1 и TP2 после каждого DCA
2. ✅ Сбрасывать флаги и stage для отслеживания новых TP
3. ✅ Отправлять уведомления о достижении TP1 и TP2
4. ✅ Закрывать позиции по новым TP с корректными уведомлениями
5. ✅ Логировать все обновления для отладки

---

**Следующий шаг:** Протестировать исправление на реальных DCA позициях

