# 📊 СТАТУС МАССОВОГО СКРИНИНГА

## ✅ ЗАПУЩЕНО

**Дата запуска**: 2025-01-XX  
**Процесс**: `mass_screening_by_correlation_groups.py`  
**PID**: Проверьте через `ps aux | grep mass_screening`

## 📋 ЧТО ДЕЛАЕТ СКРИПТ

1. **Загружает все монеты** из `data/backtest_data/` (~101 монета)
2. **Вычисляет корреляцию** каждой монеты к BTC, ETH, SOL
3. **Группирует монеты** по корреляционным группам:
   - BTC_HIGH (>0.75 к BTC)
   - ETH_HIGH (>0.75 к ETH)
   - SOL_HIGH (>0.75 к SOL)
4. **Запускает годовой бектест** (365 дней) для каждой монеты
5. **Выбирает топ-5 монет** из каждой группы по комбинированному скору

## ⏱️ ОЖИДАЕМОЕ ВРЕМЯ

- **~2-4 часа** (зависит от количества монет и производительности)
- Каждая монета: ~1-2 минуты на годовой бектест

## 📊 КАК ОТСЛЕЖИВАТЬ ПРОГРЕСС

### 1. Проверить процесс:
```bash
ps aux | grep mass_screening
```

### 2. Проверить результаты:
```bash
python3 scripts/monitor_screening_progress.py
```

### 3. Проверить логи (если есть):
```bash
tail -f logs/screening.log  # если логирование настроено
```

### 4. Проверить файлы результатов:
```bash
ls -lt data/reports/correlation_groups_* | head -5
```

## 📁 РЕЗУЛЬТАТЫ

После завершения будут созданы файлы:

1. **`data/reports/correlation_groups_screening_YYYYMMDD_HHMMSS.json`**
   - Полные результаты скрининга
   - Все монеты с их метриками
   - Группировка по корреляционным группам

2. **`data/reports/correlation_groups_top5_YYYYMMDD_HHMMSS.csv`**
   - Топ-5 монет из каждой группы
   - Готово для анализа и формирования портфеля

## 🎯 СЛЕДУЮЩИЕ ШАГИ

После завершения скрининга:

1. ✅ Проверить результаты через `monitor_screening_progress.py`
2. ✅ Выбрать топ-5 монет из каждой группы
3. ✅ Проверить/оптимизировать параметры для новых монет
4. ✅ Запустить финальный годовой бектест на портфеле из 15 монет

## ⚠️ ВАЖНО

- Скрининг работает в фоновом режиме
- Не прерывайте процесс
- Результаты сохраняются автоматически
- Можно проверить прогресс в любое время

---

**Обновлено**: 2025-01-XX

