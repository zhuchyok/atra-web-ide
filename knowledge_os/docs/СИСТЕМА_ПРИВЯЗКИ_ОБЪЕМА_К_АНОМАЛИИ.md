# 🎯 СИСТЕМА ПРИВЯЗКИ ОБЪЕМА К АНОМАЛИИ

## 📋 ОПИСАНИЕ

Система привязки объема входа к индикатору "Аномалия" автоматически корректирует размер позиции и риск на основе рыночной активности. Чем больше кружков аномалии, тем выше подтверждение сигнала и тем больше объем входа.

## 🔧 ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

### Основные функции:

```python
def calculate_anomaly_based_volume(base_volume, circles_count, user_deposit=1000):
    """Рассчитывает объем входа на основе индикатора аномалии"""

def calculate_anomaly_based_risk(base_risk, circles_count):
    """Рассчитывает риск на основе индикатора аномалии"""
```

### Модифицированная функция DCA:

```python
def dca_calculate_next_qty_and_tp(
    entry_prices, qtys, price, dca_count, deposit, risk_pct,
    leverage=1, side="long", df=None, current_index=None,
    anomaly_circles_count=0
):
    """Расчет объема с учетом аномалии"""
```

## 📊 ТАБЛИЦА МНОЖИТЕЛЕЙ

| Кружки | Описание | Объем | Риск | Стратегия |
|--------|----------|-------|------|-----------|
| ⚪ | Нормальный объем | 0.5x | 0.7x | Консервативный |
| 🟢 | Повышенная активность | 0.8x | 0.9x | Умеренный |
| 🟢🟢 | Высокая активность | 1.0x | 1.0x | Стандартный |
| 🟢🟢🟢 | Аномальная активность | 1.5x | 1.3x | Агрессивный |
| 🟢🟢🟢🟢 | Критическая активность | 2.0x | 1.6x | Максимальный |
| 🟢🟢🟢🟢🟢 | Максимальная аномалия | 3.0x | 2.0x | Экстремальный |

## 🎯 ЛОГИКА СИСТЕМЫ

### Консервативный подход (0-1 кружок):
- **Объем**: Уменьшается до 50-80% от базового
- **Риск**: Снижается до 70-90% от базового
- **Применение**: Нормальная рыночная активность

### Стандартный подход (2 кружка):
- **Объем**: Базовый объем (100%)
- **Риск**: Базовый риск (100%)
- **Применение**: Высокая активность

### Агрессивный подход (3+ кружка):
- **Объем**: Увеличивается до 150-300% от базового
- **Риск**: Увеличивается до 130-200% от базового
- **Применение**: Аномальная активность

## 💡 ПРАКТИЧЕСКИЕ ПРИМЕРЫ

### BTCUSDT (Нормальная активность):
```
📊 Базовый объем: $100
🔢 Множитель объема: x0.50
💵 Скорректированный объем: $50.00
⚠️ Скорректированный риск: 1.40%
📝 Стратегия: ⚠️ Консервативный вход - низкая активность
```

### PEPEUSDT (Критическая активность):
```
📊 Базовый объем: $100
🔢 Множитель объема: x2.00
💵 Скорректированный объем: $200.00
⚠️ Скорректированный риск: 3.20%
📝 Стратегия: 🔥 Максимальный вход - критическая активность
```

### DOGEUSDT (Максимальная аномалия):
```
📊 Базовый объем: $100
🔢 Множитель объема: x3.00
💵 Скорректированный объем: $300.00
⚠️ Скорректированный риск: 4.00%
📝 Стратегия: 💥 Экстремальный вход - максимальная аномалия
```

## 🚀 ПРЕИМУЩЕСТВА

### ✅ Автоматическая корректировка:
- Объем входа автоматически корректируется на основе аномалии
- Риск автоматически корректируется на основе аномалии
- Количество монет пересчитывается с учетом аномалии

### 🎯 Адаптивная стратегия:
- Система адаптируется к рыночным условиям
- Консервативный подход при низкой активности
- Агрессивный подход при высокой активности

### 📊 Улучшенное качество сигналов:
- Чем больше кружков = тем выше подтверждение сигнала
- Автоматическое увеличение объема при высокой активности
- Автоматическое снижение риска при низкой активности

## 🔄 ИНТЕГРАЦИЯ В СИГНАЛЫ

### Новые торговые сигналы:
```python
# Получаем данные аномалии
anomaly_data = await get_anomaly_indicator(symbol)
circles_count = anomaly_data["circles_count"]

# Рассчитываем скорректированные параметры
adjusted_volume, volume_multiplier, recommendation = calculate_anomaly_based_volume(
    base_volume, circles_count, user_deposit
)
adjusted_risk, risk_multiplier = calculate_anomaly_based_risk(base_risk, circles_count)

# Добавляем в сообщение
msg += f"🎯 <b>СКОРРЕКТИРОВАННЫЕ ПАРАМЕТРЫ:</b>\n"
msg += f"• 💰 Объем входа: ${adjusted_volume:.2f} (x{volume_multiplier:.2f})\n"
msg += f"• ⚠️ Риск: {adjusted_risk:.2f}% (x{risk_multiplier:.2f})\n"
msg += f"• 📝 Стратегия: {recommendation}"
```

### DCA сигналы:
```python
# Передаем количество кружков в функцию расчета
new_qty, avg_price_new, tp1, tp2, limit_reached = dca_calculate_next_qty_and_tp(
    entry_prices, qtys, last['close'], dca_count, deposit, risk_pct,
    leverage, side='long', df=df, current_index=len(df)-1,
    anomaly_circles_count=circles_count
)
```

## ⚠️ ОГРАНИЧЕНИЯ И БЕЗОПАСНОСТЬ

### Ограничения объема:
- Максимум 95% от депозита пользователя
- Автоматическое ограничение при превышении

### Ограничения риска:
- Максимум 10% риск на сделку
- Автоматическое ограничение при превышении

### Безопасность:
- Система работает только при наличии данных аномалии
- Fallback на базовые значения при ошибках
- Логирование всех корректировок

## 📈 РЕЗУЛЬТАТЫ ВНЕДРЕНИЯ

### Ожидаемые улучшения:
1. **Повышение качества сигналов** - автоматическая адаптация к рыночным условиям
2. **Оптимизация риска** - снижение риска при низкой активности
3. **Максимизация прибыли** - увеличение объема при высокой активности
4. **Автоматизация** - система работает без вмешательства пользователя

### Метрики успеха:
- Увеличение win rate на 5-15%
- Снижение максимальной просадки на 10-20%
- Повышение среднего профита на 20-40%

## 🔧 НАСТРОЙКА И КОНФИГУРАЦИЯ

### Настройка множителей:
```python
# В файле signal_live.py можно изменить множители
anomaly_multipliers = {
    0: 0.5,    # Нормальный объем
    1: 0.8,    # Повышенная активность
    2: 1.0,    # Высокая активность
    3: 1.5,    # Аномальная активность
    4: 2.0,    # Критическая активность
    5: 3.0     # Максимальная аномалия
}
```

### Настройка ограничений:
```python
# Максимальный объем от депозита
max_volume_percent = 0.95

# Максимальный риск
max_risk_percent = 10.0
```

## 📝 ЗАКЛЮЧЕНИЕ

Система привязки объема к аномалии представляет собой революционное решение для автоматической адаптации торговых стратегий к рыночным условиям. Она обеспечивает:

- **Автоматическую корректировку** объема и риска
- **Адаптивную стратегию** на основе рыночной активности
- **Улучшенное качество** торговых сигналов
- **Безопасность** с встроенными ограничениями

Система полностью интегрирована в существующую архитектуру и работает автоматически без необходимости дополнительной настройки.
