# ВРЕМЕННЫЕ НАСТРОЙКИ ДЛЯ ТЕСТИРОВАНИЯ СИГНАЛОВ

## Изменения в настройках (временно для тестирования)

### 1. Фильтры риска (config.py)
- `min_volume_24h`: 100_000 → 10_000 (снижен в 10 раз)
- `max_spread_pct`: 1.5 → 5.0 (увеличен в 3 раза)
- `min_price`: 0.001 → 0.0001 (снижен в 10 раз)
- `max_price`: 100_000 → 1_000_000 (увеличен в 10 раз)
- `max_volatility_pct`: 5.0 → 20.0 (увеличен в 4 раза)
- `min_profit_pct`: 0.5 → 0.1 (снижен в 5 раз)
- `max_profit_pct`: 2.5 → 10.0 (увеличен в 4 раза)
- `enable_risk_filters`: True → False (отключены)

### 2. Улучшенные фильтры (shared_utils.py)
- `USE_BTC_TREND_FILTER`: True → False (отключен)
- `use_rsi_filter`: True → False (отключен)
- `use_volume_filter`: False (остался отключен)
- `use_adx_filter`: False (остался отключен)
- `use_bb_squeeze_filter`: False (остался отключен)
- `use_time_filter`: False (остался отключен)
- `use_correlation_filter`: False (остался отключен)

### 3. Параметры стратегии (signal_live.py)
- `BB_WINDOW`: 20 → 10 (уменьшен в 2 раза)
- `BB_STD`: 2.0 → 1.5 (уменьшен)
- `SL_ATR_MULT`: 2.0 → 1.0 (уменьшен в 2 раза)
- `MIN_VOLATILITY`: 0.01 → 0.001 (снижен в 10 раз)
- `MAX_VOLATILITY`: 0.2 → 1.0 (увеличен в 5 раз)
- `FINAL_LIMIT`: 12 → 20 (увеличен)
- `MAX_POSITIONS`: 4 → 10 (увеличен в 2.5 раза)
- `MAX_CORR`: 0.8 → 0.95 (увеличен)
- `CYCLE_MINUTES`: 5 → 1 (уменьшен в 5 раз)
- `DCA_BELOW_SL_PCT`: 2.0 → 1.0 (уменьшен в 2 раза)
- `MAX_DCA`: 5 → 10 (увеличен в 2 раза)
- `ALPHA`: 2 → 1 (уменьшен в 2 раза)
- `MAX_RISK_PCT`: 50 → 100 (увеличен в 2 раза)
- `TP_PCT`: 1 → 0.5 (уменьшен в 2 раза)

### 4. Условия входа в позицию (signal_live.py)
Все условия значительно смягчены:

#### Строгий режим:
- Касание BB: 1.0100 → 1.1000 (увеличено)
- Тренд EMA: 1.002 → 0.990 (смягчено)
- RSI: 42 → 80 (смягчено)
- Объем: 1.2 → 0.1 (снижен)
- Волатильность: 2.4 → 0.1 (снижена)
- Momentum: 0.0 → -1.0 (смягчено)
- Сила тренда: 0.40 → 0.01 (снижена)

#### Мягкий режим:
- Касание BB: 1.0500 → 1.2000 (увеличено)
- Тренд EMA: 0.999 → 0.980 (смягчено)
- RSI: 58 → 90 (смягчено)
- Объем: 0.5 → 0.01 (снижен)
- Волатильность: 0.6 → 0.01 (снижена)
- Momentum: -0.1 → -2.0 (смягчено)
- Сила тренда: 0.04 → 0.001 (снижена)

### 5. Фильтры объема (exchange_api.py)
- Минимальный оборот: 10000 → 1000 (снижен в 10 раз)
- Минимальный объем USDT: 1_000_000 → 10_000 (снижен в 100 раз)

### 6. Новостные фильтры
- `NEWS_FILTER_ACTIVE`: False (остался отключен)

## Результат
С этими настройками система должна генерировать **значительно больше сигналов** для тестирования. Все фильтры максимально смягчены, что позволит проверить работу системы генерации и отправки сигналов.

## ВАЖНО!
Эти настройки предназначены **ТОЛЬКО ДЛЯ ТЕСТИРОВАНИЯ**. Не используйте их для реальной торговли!

Для возврата к нормальным настройкам нужно будет откатить все изменения.
