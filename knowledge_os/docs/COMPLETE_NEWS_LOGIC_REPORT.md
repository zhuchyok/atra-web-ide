# 📰 ПОЛНАЯ ЛОГИКА НОВОСТНЫХ ФИЛЬТРОВ ДЛЯ SPOT И FUTURES

## 🎯 **ОБЩАЯ СХЕМА:**

### **📊 Режимы новостей:**

```python
NEWS_FILTER_MODES = {
    "conservative": {
        "enable_negative_news": True,    # Блокировать по негативным новостям
        "enable_positive_news": True,    # Генерировать LONG по позитивным
        "block_short_on_positive": True, # Блокировать SHORT при позитивных
    },
    "aggressive": {
        "enable_negative_news": False,   # НЕ блокировать по негативным новостям
        "enable_positive_news": True,    # Генерировать LONG по позитивным
        "block_short_on_positive": False, # НЕ блокировать SHORT при позитивных
    }
}
```

### **💰 Режимы торговли:**

- **SPOT** - только LONG позиции, без плеча (1x)
- **FUTURES** - LONG и SHORT позиции, динамическое плечо (0.5x-5x)

---

## 🔍 **ДЕТАЛЬНАЯ ЛОГИКА ПО РЕЖИМАМ:**

### **1️⃣ SPOT ПОЛЬЗОВАТЕЛИ:**

#### **📰 Conservative режим новостей:**

```
✅ ПОЗИТИВНЫЕ НОВОСТИ:
   • Генерируют LONG сигналы
   • Усиливают технические сигналы
   • Добавляют новостное обоснование

❌ НЕГАТИВНЫЕ НОВОСТИ:
   • БЛОКИРУЮТ все сигналы на 2 часа
   • Пользователь не получает никаких сигналов
   • Защита от убыточных сделок
```

#### **📰 Aggressive режим новостей:**

```
✅ ПОЗИТИВНЫЕ НОВОСТИ:
   • Генерируют LONG сигналы
   • Усиливают технические сигналы
   • Добавляют новостное обоснование

✅ НЕГАТИВНЫЕ НОВОСТИ:
   • НЕ блокируют сигналы
   • Пользователь получает технические сигналы
   • Более активная торговля
```

### **2️⃣ FUTURES ПОЛЬЗОВАТЕЛИ:**

#### **📰 Conservative режим новостей:**

```
✅ ПОЗИТИВНЫЕ НОВОСТИ:
   • Генерируют LONG сигналы
   • Усиливают технические LONG сигналы
   • БЛОКИРУЮТ SHORT сигналы на 2 часа
   • Добавляют новостное обоснование

✅ НЕГАТИВНЫЕ НОВОСТИ:
   • Генерируют SHORT сигналы
   • Усиливают технические SHORT сигналы
   • БЛОКИРУЮТ LONG сигналы на 2 часа
   • Добавляют новостное обоснование
```

#### **📰 Aggressive режим новостей:**

```
✅ ПОЗИТИВНЫЕ НОВОСТИ:
   • Генерируют LONG сигналы
   • Усиливают технические LONG сигналы
   • НЕ блокируют SHORT сигналы
   • Добавляют новостное обоснование

✅ НЕГАТИВНЫЕ НОВОСТИ:
   • Генерируют SHORT сигналы
   • Усиливают технические SHORT сигналы
   • НЕ блокируют LONG сигналы
   • Добавляют новостное обоснование
```

---

## 🔄 **АЛГОРИТМ ОБРАБОТКИ НОВОСТЕЙ:**

### **1. Проверка негативных новостей:**

```python
if news_blocked is True:  # Обнаружены негативные новости
    for user_id, user_data in user_data_dict.items():
        trade_mode = user_data.get('trade_mode', 'spot')
        news_mode = user_data.get('news_filter_mode', 'conservative')

        if trade_mode == 'futures':
            # ВСЕГДА: негативные новости = SHORT сигнал
            negative_news_flag = True
            print("Негативная новость - потенциальный SHORT сигнал для фьючерсов")

        elif news_mode == 'conservative':
            # SPOT + Conservative: блокируем все сигналы
            print("Заблокирован по новостям до {block_until}")
            continue

        else:  # SPOT + Aggressive
            # SPOT + Aggressive: не блокируем
            print("Негативные новости отключены для spot пользователя")
```

### **2. Проверка позитивных новостей:**

```python
positive_news_found, positive_news_post = await is_positive_news(symbol)
if positive_news_found:
    print("Обнаружена позитивная новость - потенциальный LONG сигнал")
    positive_news_flag = True
```

### **3. Генерация сигналов:**

```python
# Для каждого пользователя:
if positive_news_flag:
    # Генерируем LONG сигнал с новостным усилением
    if user_data.get('trade_mode') == 'futures' and news_mode == 'conservative':
        # Блокируем SHORT сигналы при позитивных новостях
        block_short_signals = True

if negative_news_flag and user_data.get('trade_mode') == 'futures':
    # Генерируем SHORT сигнал с новостным усилением
    if news_mode == 'conservative':
        # Блокируем LONG сигналы при негативных новостях
        block_long_signals = True
```

---

## 📋 **ТАБЛИЦА ПОВЕДЕНИЯ:**

| Режим торговли | Режим новостей | Позитивные новости | Негативные новости     |
| -------------- | -------------- | ------------------ | ---------------------- |
| **SPOT**       | Conservative   | ✅ LONG сигналы    | ❌ Блокирует все       |
| **SPOT**       | Aggressive     | ✅ LONG сигналы    | ✅ Технические сигналы |
| **FUTURES**    | Conservative   | ✅ LONG, ❌ SHORT  | ✅ SHORT, ❌ LONG      |
| **FUTURES**    | Aggressive     | ✅ LONG, ✅ SHORT  | ✅ SHORT, ✅ LONG      |

---

## 🎯 **ПРИМЕРЫ СЦЕНАРИЕВ:**

### **Сценарий 1: XRPUSDT + Негативные новости**

```
Пользователь 958930260:
• Режим торговли: FUTURES
• Режим новостей: Aggressive
• Результат: ✅ Получает SHORT сигнал с новостным усилением
```

### **Сценарий 2: BTCUSDT + Позитивные новости**

```
Пользователь 556251171:
• Режим торговли: SPOT
• Режим новостей: Soft (Aggressive)
• Результат: ✅ Получает LONG сигнал с новостным усилением
```

### **Сценарий 3: ETHUSDT + Негативные новости**

```
Пользователь 556251171:
• Режим торговли: SPOT
• Режим новостей: Conservative
• Результат: ❌ Блокируется на 2 часа
```

---

## 🔧 **ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ:**

### **Ключевые переменные:**

- `negative_news_flag` - флаг негативных новостей
- `positive_news_flag` - флаг позитивных новостей
- `news_blocked` - статус блокировки по новостям
- `block_until` - время до снятия блокировки

### **Функции новостей:**

- `is_negative_news(symbol)` - проверка негативных новостей
- `is_positive_news(symbol)` - проверка позитивных новостей
- `create_news_summary(title)` - создание краткого описания
- `get_news_impact_emoji(side)` - эмодзи для типа сигнала

---

## 📊 **ИТОГ:**

### **✅ SPOT пользователи:**

- **Conservative**: защита от негативных новостей
- **Aggressive**: активная торговля без блокировок

### **✅ FUTURES пользователи:**

- **Conservative**: строгий контроль по новостям
- **Aggressive**: максимальная активность торговли

### **🎯 Главный принцип:**

**Для фьючерсов негативные новости ВСЕГДА усиливают SHORT сигналы, независимо от режима новостей!**
