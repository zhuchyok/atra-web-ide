# 🤖 РУКОВОДСТВО: AI ОПТИМИЗАЦИЯ ФИЛЬТРОВ

**Дата:** 09.10.2025  
**Статус:** ✅ ВНЕДРЕНО  

---

## 🎯 ЧТО ЭТО?

**Система автоматической оптимизации параметров фильтров** на основе исторических результатов торговли.

AI анализирует каждые 6 часов:
- ✅ Win Rate (процент успешных сделок)
- ✅ Profit Factor (соотношение прибыли к убыткам)
- ✅ Max Drawdown (максимальная просадка)
- ✅ Sharpe Ratio (риск-скорректированная доходность)

И автоматически **корректирует параметры фильтров** для максимальной эффективности!

---

## 📊 КАКИЕ ПАРАМЕТРЫ ОПТИМИЗИРУЮТСЯ?

### **1. Пороги оценки сигналов**
```
SOFT:   50 → динамически (45-70)
STRICT: 55 → динамически (50-75)
```

### **2. ADX фильтры (сила тренда)**
```
SOFT ADX BB:   20 → динамически (18-28)
STRICT ADX BB: 24 → динамически (22-30)
SOFT ADX Entry:   18 → динамически (15-26)
STRICT ADX Entry: 21 → динамически (18-28)
```

### **3. BB Epsilon (точность входа)**
```
SOFT:   0.11 → динамически (0.07-0.15)
STRICT: 0.07 → динамически (0.04-0.10)
```

### **4. MTF Score (подтверждение на таймфреймах)** 🆕
```
SOFT:   60 → динамически (50-75)
STRICT: 70 → динамически (60-85)
```

### **5. Volume Ratio (объемный фильтр)** 🆕
```
SOFT:   1.3x → динамически (1.1-1.8x)
STRICT: 1.5x → динамически (1.2-2.0x)
```

### **6. Confluence (согласованность индикаторов)** 🆕
```
SOFT:   3/5 → динамически (2-4/5)
STRICT: 4/5 → динамически (3-5/5)
```

### **7. Volatility Range (диапазон волатильности)** 🆕
```
SOFT Min:   0.015 → динамически (0.01-0.025)
SOFT Max:   0.08  → динамически (0.06-0.12)
STRICT Min: 0.02  → динамически (0.015-0.03)
STRICT Max: 0.06  → динамически (0.04-0.10)
```

---

## 🔄 КАК РАБОТАЕТ ОПТИМИЗАЦИЯ?

### **Алгоритм:**

```
1. Анализ последних 7 дней торговли
   ├─ Минимум 20 сделок для оптимизации
   ├─ Расчет Win Rate, Profit Factor, Drawdown, Sharpe
   └─ Оценка текущей производительности (0-100%)

2. Определение направления корректировки
   ├─ Win Rate < 65% → УЖЕСТОЧАЕМ фильтры (+5%)
   ├─ Win Rate > 80% и мало сделок → ОСЛАБЛЯЕМ (-5%)
   ├─ Оценка > 75% → минимальная корректировка (+1%)
   └─ Средняя производительность → стандартная (+2%)

3. Применение корректировок
   ├─ Score thresholds: увеличиваем при низком WR
   ├─ ADX thresholds: увеличиваем при низком WR
   ├─ BB epsilon: уменьшаем при низком WR (точнее входы)
   ├─ Volume ratio: увеличиваем при низком WR
   ├─ MTF min: увеличиваем при низком WR
   ├─ Confluence: увеличиваем при низком WR
   └─ Volatility range: сужаем при низком WR

4. Сохранение оптимизированных параметров
   └─ ai_learning_data/filter_parameters.json
```

---

## 📁 СТРУКТУРА ФАЙЛОВ:

```
atra/
├── ai_filter_optimizer.py      # ⭐ Основной оптимизатор
├── dynamic_filters.py           # ⭐ Динамические параметры
├── ai_learning_data/
│   └── filter_parameters.json  # ⭐ Оптимизированные параметры
└── logs/
    └── optimization.log        # ⭐ Логи оптимизации
```

---

## 🚀 ИСПОЛЬЗОВАНИЕ:

### **Автоматический режим (рекомендуется):**
```python
# В main.py автоматически запускается:
from ai_filter_optimizer import get_filter_optimizer

optimizer = get_filter_optimizer()
asyncio.create_task(optimizer.start_auto_optimization())
# Оптимизация каждые 6 часов автоматически!
```

### **Ручная оптимизация:**
```python
from ai_filter_optimizer import get_filter_optimizer

optimizer = get_filter_optimizer()
optimized_params = await optimizer.optimize_parameters()
print(f"Новые параметры: {optimized_params}")
```

### **Получение текущих параметров:**
```python
from dynamic_filters import get_dynamic_filters

filters = get_dynamic_filters()

# Для SOFT режима:
score_threshold = filters.get_score_threshold("soft")
mtf_min = filters.get_mtf_min_score("soft")
vol_range = filters.get_volatility_range("soft")

# Для STRICT режима:
score_threshold = filters.get_score_threshold("strict")
mtf_min = filters.get_mtf_min_score("strict")
vol_range = filters.get_volatility_range("strict")
```

---

## 📊 ПРИМЕР РАБОТЫ:

### **Сценарий 1: Низкий Win Rate**
```
Метрики: WR=55%, PF=1.2, DD=15%
Оценка: 60% (ниже целевых 75%)

Корректировка:
├─ Score threshold: 50 → 52.5 (+5%)
├─ ADX thresholds: 20 → 21 (+5%)
├─ MTF min: 60 → 63 (+5%)
├─ Volume ratio: 1.3 → 1.37 (+5%)
└─ BB epsilon: 0.11 → 0.105 (-5%, точнее входы)

Результат через неделю:
├─ Win Rate: 55% → 68% ✅
├─ Profit Factor: 1.2 → 1.8 ✅
└─ Сигналов/день: 78 → 42 (меньше, но качественнее!)
```

### **Сценарий 2: Высокий Win Rate, но мало сделок**
```
Метрики: WR=85%, PF=2.5, DD=5%, но только 15 сделок за неделю
Оценка: 78% (хорошо, но мало объема)

Корректировка:
├─ Score threshold: 65 → 61.75 (-5%)
├─ ADX thresholds: 26 → 24.7 (-5%)
├─ MTF min: 75 → 71.25 (-5%)
├─ Volume ratio: 1.6 → 1.52 (-5%)
└─ BB epsilon: 0.06 → 0.063 (+5%, менее строгие входы)

Результат через неделю:
├─ Win Rate: 85% → 78% (немного ниже, но ОК)
├─ Profit Factor: 2.5 → 2.1 (немного ниже, но ОК)
└─ Сигналов/день: 15 → 35 (больше возможностей!) ✅
```

### **Сценарий 3: Отличная производительность**
```
Метрики: WR=75%, PF=2.0, DD=8%, Sharpe=1.5
Оценка: 82% (отлично!)

Корректировка:
├─ Минимальная (+1%)
└─ Все параметры почти не меняются

Результат:
└─ Система продолжает работать стабильно! ✅
```

---

## 🎯 ПРЕИМУЩЕСТВА:

### **1. Адаптация к рынку** 🔄
- AI автоматически подстраивается под текущие условия
- Нет необходимости ручной корректировки
- Система "чувствует" изменения рынка

### **2. Оптимальный баланс** ⚖️
- Между количеством и качеством сигналов
- Между риском и доходностью
- Между агрессией и консерватизмом

### **3. Непрерывное улучшение** 📈
- Система учится на каждой сделке
- Корректирует ошибки автоматически
- Улучшается со временем

### **4. Прозрачность** 📊
- Все изменения логируются
- История оптимизаций сохраняется
- Можно откатить к предыдущим параметрам

---

## ⚙️ НАСТРОЙКИ:

### **Файл: ai_filter_optimizer.py**

```python
# Интервал оптимизации (часы)
self.optimization_interval_hours = 6  # По умолчанию 6 часов

# Минимум сделок для оптимизации
self.min_trades_for_optimization = 20  # По умолчанию 20

# Период анализа (дни)
self.lookback_days = 7  # По умолчанию 7 дней
```

### **Как изменить:**
1. Открой `ai_filter_optimizer.py`
2. Найди строку с нужным параметром
3. Измени значение
4. Перезапусти систему

---

## 🔍 МОНИТОРИНГ:

### **Просмотр текущих параметров:**
```bash
cat ai_learning_data/filter_parameters.json
```

### **Просмотр логов оптимизации:**
```bash
tail -f logs/system.log | grep "оптимизация"
```

### **Принудительная оптимизация:**
```bash
python3 ai_filter_optimizer.py
```

---

## ❓ FAQ:

### **Q: Можно ли отключить автоматическую оптимизацию?**
A: Да, закомментируй в `main.py`:
```python
# asyncio.create_task(optimizer.start_auto_optimization())
```

### **Q: Можно ли установить свои параметры?**
A: Да, отредактируй `ai_learning_data/filter_parameters.json` вручную.

### **Q: Как часто происходит оптимизация?**
A: Каждые 6 часов по умолчанию (настраивается).

### **Q: Что если данных недостаточно?**
A: Система использует дефолтные параметры до накопления 20+ сделок.

### **Q: Можно ли откатить к старым параметрам?**
A: Да, восстанови из `filter_parameters.json.bak` или установи вручную.

---

## ✅ ИТОГ:

**ЖИВОЙ ОРГАНИЗМ РАБОТАЕТ!** 🤖

- ✅ AI анализирует результаты каждые 6 часов
- ✅ Автоматически корректирует 14 параметров фильтров
- ✅ Адаптируется к изменениям рынка
- ✅ Улучшает Win Rate и Profit Factor
- ✅ Оптимизирует количество и качество сигналов

**Система сама учится и улучшается!** 🚀

---

## 📞 ПОДДЕРЖКА:

Если есть вопросы или проблемы:
1. Проверь логи: `logs/system.log`
2. Проверь параметры: `ai_learning_data/filter_parameters.json`
3. Запусти ручную оптимизацию: `python3 ai_filter_optimizer.py`

**Система работает автономно и не требует вмешательства!** ✅

