# Диагностика проблемы с уведомлениями TP для DCA позиций

**Дата:** 5 октября 2025
**Статус:** 🔍 Детальная диагностика

## 🐛 Проблема

Пользователь сообщил:
> "был сигнал дца для TUTUSDT
> я усреднился по данному сигналу
> тп1 и тп2 были достигнуты и позиция закрылась
> но уведомления о тп1 и тп2 не приходили"

### Сигнал DCA:
```
📈 DCA позиция добавлена!

🔸 Символ: TUTUSDT
🔸 DCA #3
🔸 Цена входа: 0.10
🔸 Количество: 1308.768751
🔸 Плечо: 5.0x
🔸 Риск: 130.00
🔸 Сумма входа: 130
🔸 Средняя цена: 0.10
🔸 TP1: 0.10 (+0.81% / +4.05% с плечом)
🔸 TP2: 0.10 (+0.98% / +4.89% с плечом)
```

### Результат:
- ✅ Позиция закрылась по TP1 и TP2
- ❌ Уведомления о достижении TP1 и TP2 НЕ пришли

## 🔍 Первичная диагностика

### ✅ Обновление TP1 и TP2 работает ПРАВИЛЬНО

**Файл:** `telegram_handlers.py`, строки 1977-1980

```python
# Обновляем TP во всех открытых позициях символа
for p in symbol_positions:
    p['tp1'] = tp1_price_new
    p['tp2'] = tp2_price_new
```

Система **ДЕЙСТВИТЕЛЬНО обновляет TP1 и TP2** в позициях после DCA! Это означает, что проблема в другом месте.

## 🔍 Возможные причины

### 1. ❓ Проблема с сохранением данных

**Проверить:**
- Сохраняются ли обновленные TP1 и TP2 в БД после обновления?
- Синхронизируются ли данные между `user_data` и БД?

**Код сохранения (строка 1991):**
```python
db.save_user_data(user_id, user_data)
```

**Возможная проблема:**
- Данные могут не сохраняться из-за ошибки
- Данные могут сохраняться, но не синхронизироваться между процессами

### 2. ❓ Проблема с флагом `tp1_notified`

**Проверить:**
- Сбрасывается ли флаг `tp1_notified` после обновления TP1?
- Проверяет ли `check_take_profits()` флаг `tp1_notified` для DCA позиций?

**Код проверки TP1 в `signal_live.py` (строки 10747-10752):**
```python
if (
    tp1_reached
    and pos.get("stage") == "open"
    and not bool(pos.get("tp1_notified"))
):
```

**Возможная проблема:**
- После обновления TP1 флаг `tp1_notified` остается `True` от предыдущего TP1
- Система не отправляет уведомление, так как считает, что TP1 уже был достигнут

### 3. ❓ Проблема с stage позиции

**Проверить:**
- Сбрасывается ли `stage` позиции на `"open"` после обновления TP1 и TP2?
- Проверяет ли `check_take_profits()` stage для DCA позиций?

**Код проверки TP1 в `signal_live.py` (строки 10747-10752):**
```python
if (
    tp1_reached
    and pos.get("stage") == "open"  # Проверка stage
    and not bool(pos.get("tp1_notified"))
):
```

**Возможная проблема:**
- После обновления TP1 stage остается `"tp1_reached"` или другим значением
- Система не отправляет уведомление, так как считает, что TP1 уже был достигнут

### 4. ❓ Проблема с синхронизацией между процессами

**Проверить:**
- Синхронизируются ли данные между `telegram_handlers.py` (обновление TP) и `signal_live.py` (проверка TP)?
- Загружаются ли обновленные TP из БД в `signal_live.py`?

**Код загрузки данных в `signal_live.py` (строка 10470):**
```python
user_data_dict = load_user_data_for_signals()
```

**Возможная проблема:**
- Данные обновляются в `telegram_handlers.py`, но не синхронизируются с `signal_live.py`
- `signal_live.py` продолжает использовать старые TP из кэша

## ✅ Рекомендуемое исправление

### 1. Добавить сброс флага `tp1_notified` и `stage` после обновления TP

**Файл:** `telegram_handlers.py`, строки 1977-1980

**БЫЛО:**
```python
# Обновляем TP во всех открытых позициях символа
for p in symbol_positions:
    p['tp1'] = tp1_price_new
    p['tp2'] = tp2_price_new
```

**ДОЛЖНО БЫТЬ:**
```python
# Обновляем TP во всех открытых позициях символа
for p in symbol_positions:
    p['tp1'] = tp1_price_new
    p['tp2'] = tp2_price_new
    # КРИТИЧЕСКИ ВАЖНО: сбрасываем флаги и stage для отслеживания новых TP
    p['tp1_notified'] = False  # Сбрасываем флаг уведомления TP1
    p['stage'] = 'open'  # Сбрасываем stage на 'open' для отслеживания новых TP
    logging.info(f"[DCA] {symbol}: Обновлены TP после усреднения: "
                 f"TP1: {p.get('tp1'):.6f} → {tp1_price_new:.6f}, "
                 f"TP2: {p.get('tp2'):.6f} → {tp2_price_new:.6f}")
```

### 2. Добавить логирование обновления TP

Добавить логирование для отладки:
```python
logging.info(f"[DCA] {symbol}: Обновлены TP после усреднения #{dca_count}: "
             f"TP1: {tp1_price_new:.6f} (+{tp1_pct:.2f}%), "
             f"TP2: {tp2_price_new:.6f} (+{tp2_pct:.2f}%), "
             f"Средняя цена: {avg_price_new:.6f}")
```

### 3. Добавить уведомление об обновлении TP в сообщение DCA

**Файл:** `telegram_handlers.py`, строки 1995-2007

**Добавить:**
```python
dca_text = (
    "📈 <b>DCA позиция добавлена!</b>\n\n"
    f"🔸 Символ: <code>{symbol}</code>\n"
    f"🔸 DCA #<code>{dca_count}</code>\n"
    f"🔸 Цена входа: <code>${safe_format_price(entry_price, symbol)}</code>\n"
    f"🔸 Количество: <code>{next_qty:.6f}</code>\n"
    f"🔸 Сумма входа: <code>${notional_usd:.2f}</code>\n"
    f"🔸 Средняя цена: <code>${safe_format_price(avg_price, symbol)}</code>\n\n"
    f"🎯 <b>ОБНОВЛЕННЫЕ ТЕЙК-ПРОФИТЫ:</b>\n"
    f"• TP1: <code>${safe_format_price(tp1_price_new, symbol)}</code> (+{tp1_pct:.2f}%) ← обновлено\n"
    f"• TP2: <code>${safe_format_price(tp2_price_new, symbol)}</code> (+{tp2_pct:.2f}%) ← обновлено\n\n"
    f"⚠️ <b>Внимание:</b> на <b>TP1</b> фиксируем <b>50%</b> позиции, остаток держим до <b>TP2</b>\n\n"
    f"⏰ Время: <code>{dt.datetime.now().strftime('%H:%M:%S')}</code>"
)
```

## 📊 Тестирование

### Проверочный список:
1. ✅ Принятие DCA сигнала обновляет `tp1` и `tp2` в позиции
2. ✅ Сбрасываются флаги `tp1_notified` и `stage` после обновления TP
3. ✅ Команда `/positions` показывает новые TP1 и TP2 после DCA
4. ✅ Уведомления о достижении TP1 приходят
5. ✅ Уведомления о достижении TP2 приходят
6. ✅ Частичное закрытие позиции на TP1 (50%) работает
7. ✅ Полное закрытие позиции на TP2 (100%) работает
8. ✅ Логи показывают обновление TP после DCA

### Тестовый сценарий:
1. Открыть позицию LONG по цене 1.00
2. Усредниться по цене 0.90 (DCA #2)
3. Проверить, что TP1 и TP2 обновились в позиции
4. Дождаться достижения нового TP1
5. Проверить, что пришло уведомление о TP1
6. Дождаться достижения нового TP2
7. Проверить, что пришло уведомление о TP2
8. Проверить, что позиция закрылась полностью

## 🔗 Связанные файлы

- `telegram_handlers.py` - обновление TP после DCA (строки 1977-1980)
- `signal_live.py` - проверка достижения TP (строки 10747-10792)
- `user_utils.py` - сохранение и загрузка данных пользователей
- `db.py` - работа с БД

## 📝 Заметки

- Проблема может быть связана с отсутствием сброса флагов `tp1_notified` и `stage` после обновления TP
- Необходимо добавить логирование для отладки
- Необходимо протестировать на реальных данных

---

**Следующий шаг:** Исправить код обновления TP в `telegram_handlers.py` с добавлением сброса флагов

