# 🎯 Динамические параметры торговой системы ATRA

## 📊 Обзор системы

Торговая система ATRA использует **динамические параметры**, которые автоматически адаптируются к рыночным условиям:

- 🎯 **Динамический риск** - 1-5% в зависимости от волатильности и тренда
- ⚡ **Динамическое плечо** - 1-20x на основе базового плеча, тренда и волатильности
- 📈 **Динамические TP/SL** - адаптируются под волатильность рынка
- 🔄 **Автоматическая оптимизация** - параметры обновляются в реальном времени

## 🧮 Алгоритмы расчета

### 1. Динамический риск (`get_dynamic_risk_pct`)

```python
def get_dynamic_risk_pct(df, i):
    """
    Динамический расчет процента риска на основе волатильности и тренда
    """
    # Базовый риск: 2%
    # При сильном тренде: увеличиваем риск
    # При высокой волатильности: уменьшаем риск
    # Диапазон: 1-5%
```

**Формула:**
```
dynamic_risk = base_risk * (1 + 2 * trend) / (1 + 5 * volatility)
```

**Примеры:**
- Сильный восходящий тренд + низкая волатильность = 4-5% риск
- Боковой рынок + средняя волатильность = 2% риск
- Сильная волатильность + слабый тренд = 1% риск

### 2. Динамическое плечо (`get_dynamic_leverage`)

```python
def get_dynamic_leverage(df, i, base_leverage=1):
    """
    Динамический расчет плеча на основе волатильности и тренда
    """
    # При сильном тренде: увеличиваем плечо
    # При высокой волатильности: уменьшаем плечо
    # Диапазон: 1-20x
```

**Формула:**
```
trend_factor = 1 + abs(trend) * 2  # до 3x при сильном тренде
volatility_factor = 1 / (1 + volatility * 3)  # уменьшаем при высокой волатильности
dynamic_leverage = base_leverage * trend_factor * volatility_factor
```

**Примеры:**
- Базовое плечо 5x + сильный тренд + низкая волатильность = 15x
- Базовое плечо 3x + боковой рынок + средняя волатильность = 3x
- Базовое плечо 10x + высокая волатильность = 2-3x

### 3. Динамические TP/SL (`get_dynamic_tp_levels`, `get_dynamic_sl_level`)

```python
def get_dynamic_tp_levels(df, i, side="long", base_tp1_pct=1.0, base_tp2_pct=2.0):
    """
    Динамический расчет уровней Take Profit на основе волатильности
    """
    # При высокой волатильности: увеличиваем TP
    # Диапазон TP1: 0.5-10%
    # Диапазон TP2: 1-15%
```

```python
def get_dynamic_sl_level(df, i, side="long", base_sl_pct=2.0):
    """
    Динамический расчет уровня Stop Loss на основе волатильности
    """
    # При высокой волатильности: увеличиваем SL
    # Диапазон: 1-8%
```

## 📈 Практические примеры

### Пример 1: Консервативный трейдер
```json
{
  "user_id": "123456789",
  "deposit": 1000,
  "trade_mode": "spot",
  "base_leverage": 1,
  "filter_mode": "strict",
  "news_filter_mode": "conservative"
}
```

**Результат:**
- Динамический риск: 1-2%
- Динамическое плечо: 1-2x
- TP1: 0.5-2%
- TP2: 1-3%
- SL: 1-3%

### Пример 2: Умеренный трейдер
```json
{
  "user_id": "987654321",
  "deposit": 5000,
  "trade_mode": "futures",
  "base_leverage": 5,
  "filter_mode": "soft",
  "news_filter_mode": "aggressive"
}
```

**Результат:**
- Динамический риск: 2-4%
- Динамическое плечо: 3-12x
- TP1: 1-4%
- TP2: 2-6%
- SL: 2-5%

### Пример 3: Агрессивный трейдер
```json
{
  "user_id": "555666777",
  "deposit": 10000,
  "trade_mode": "futures",
  "base_leverage": 10,
  "filter_mode": "enhanced",
  "news_filter_mode": "technical_only"
}
```

**Результат:**
- Динамический риск: 3-5%
- Динамическое плечо: 5-20x
- TP1: 2-8%
- TP2: 3-12%
- SL: 3-8%

## 🔧 Настройка базовых параметров

### Базовое плечо
```bash
# Консервативный подход
python3 manage_users.py add 123456789 1000 spot strict conservative 1

# Умеренный подход
python3 manage_users.py add 987654321 5000 futures soft aggressive 5

# Агрессивный подход
python3 manage_users.py add 555666777 10000 futures enhanced technical_only 10
```

### Рекомендации по базовому плечу:

#### 🟢 Безопасный уровень (1-3x):
- Начинающие трейдеры
- Консервативная стратегия
- Небольшие депозиты (< 2000 USDT)

#### 🟡 Умеренный уровень (3-7x):
- Опытные трейдеры
- Сбалансированная стратегия
- Средние депозиты (2000-10000 USDT)

#### 🔴 Агрессивный уровень (7-20x):
- Профессиональные трейдеры
- Агрессивная стратегия
- Крупные депозиты (> 10000 USDT)

## 📊 Мониторинг динамических параметров

### Логи системы
```
[DEBUG] Динамический риск для BTCUSDT: 2.3%
[DEBUG] Динамическое плечо для BTCUSDT: 4.2x
[DEBUG] Динамические TP для BTCUSDT: TP1=1.8%, TP2=3.2%
[DEBUG] Динамический SL для BTCUSDT: 2.1%
```

### Проверка текущих параметров
```bash
# Просмотр настроек пользователя
python3 manage_users.py list

# Обновление базового плеча
python3 manage_users.py update 123456789 base_leverage 5
```

## ⚠️ Важные замечания

### 1. Безопасность
- Система автоматически ограничивает максимальные значения
- При экстремальной волатильности параметры становятся консервативными
- Всегда начинайте с низкого базового плеча

### 2. Адаптация
- Параметры обновляются каждые 5 минут
- Учитывается последние 20 свечей для расчета
- Система учитывает как локальную, так и глобальную волатильность

### 3. Ограничения
- Максимальное плечо: 20x
- Максимальный риск: 5%
- Максимальный TP: 15%
- Максимальный SL: 8%

## 🎯 Преимущества динамических параметров

### ✅ Адаптивность
- Автоматическая подстройка под рыночные условия
- Снижение рисков в волатильные периоды
- Увеличение прибыли в трендовых движениях

### ✅ Безопасность
- Ограничения максимальных значений
- Консервативные настройки при неопределенности
- Защита от экстремальных рыночных условий

### ✅ Эффективность
- Оптимизация под каждый актив
- Учет рыночной микроструктуры
- Максимальное использование рыночных возможностей

## 🔄 Обновления системы

Система динамических параметров постоянно улучшается:

- 📊 Добавление новых индикаторов
- 🧠 Машинное обучение для оптимизации
- 📈 Анализ исторических данных
- 🔧 Fine-tuning алгоритмов

---

**💡 Совет:** Начните с консервативных настроек и постепенно увеличивайте базовое плечо по мере накопления опыта и прибыли.