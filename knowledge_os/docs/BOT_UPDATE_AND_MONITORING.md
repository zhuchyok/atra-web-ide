# 🔄 ОБНОВЛЕНИЕ И МОНИТОРИНГ БОТА

**Дата:** 2025-12-02  
**Действия:** Обновление с git, перезапуск, мониторинг

---

## 📥 ОБНОВЛЕНИЕ С GIT

### Шаг 1: Обновление кода
```bash
cd /root/atra
git stash
git pull origin main
```

**Статус:** ✅ Код обновлен

---

## 🚀 ПЕРЕЗАПУСК БОТА

### Шаг 2: Остановка старого процесса
```bash
pkill -f 'signal_live.py'
```

### Шаг 3: Запуск нового процесса
```bash
nohup python3 signal_live.py > signal_live.log 2>&1 &
```

**Статус:** ✅ Бот перезапущен

---

## 📊 МОНИТОРИНГ

### Проверяемые метрики:

1. **Статус процесса:**
   - ✅ Бот запущен (PID проверяется)
   - ✅ Логи обновляются

2. **Генерация сигналов:**
   - Количество сигналов за час
   - Количество сигналов за 10 минут
   - Последние сигналы в БД

3. **Ошибки и предупреждения:**
   - ERROR в логах
   - WARNING в логах
   - Блокировки фильтрами (VolumeImbalance, ML, Risk)

4. **Параметры фильтров:**
   - Все параметры из успешного бэктеста применены
   - Порядок фильтров соответствует успешному бэктесту

---

## 📋 ТЕКУЩИЙ СТАТУС

Проверяется в реальном времени...

---

## 🔍 КОМАНДЫ ДЛЯ МОНИТОРИНГА

### Проверка статуса процесса:
```bash
ps aux | grep 'signal_live.py' | grep -v grep
```

### Просмотр логов:
```bash
tail -f signal_live.log
```

### Проверка сигналов в БД:
```bash
python3 -c "
import sqlite3
conn = sqlite3.connect('trading.db')
cursor = conn.cursor()
cursor.execute('SELECT COUNT(*) FROM signals WHERE created_at > datetime(\"now\", \"-1 hour\")')
print(cursor.fetchone()[0])
conn.close()
"
```

### Проверка ошибок:
```bash
tail -100 signal_live.log | grep -E 'ERROR|WARNING'
```

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Обновить код с git
2. ✅ Перезапустить бота
3. 🔄 Мониторить работу (в процессе)
4. 📊 Анализировать результаты
5. 🔧 Корректировать при необходимости

