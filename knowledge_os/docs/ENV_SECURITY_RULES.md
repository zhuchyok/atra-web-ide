# 🔐 ПРАВИЛА БЕЗОПАСНОСТИ: ФАЙЛЫ ОКРУЖЕНИЯ

**Дата создания:** 2025-01-27  
**Статус:** ✅ **ОБЯЗАТЕЛЬНО К СОБЛЮДЕНИЮ**

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО

### ❌ НИКОГДА НЕ КОММИТЬТЕ:
- ❌ Файл `env` с реальными API ключами
- ❌ Файл `.env` с реальными ключами (хотя он в .gitignore)
- ❌ Любые файлы с секретами и паролями

### ✅ ВСЕГДА ИСПОЛЬЗУЙТЕ:
- ✅ Файл `.env` для реальных ключей (он в `.gitignore`)
- ✅ Файл `env` только как шаблон (без реальных ключей)
- ✅ Зашифрованные ключи в базе данных для продакшена

---

## 📁 СТРУКТУРА ФАЙЛОВ

### 1. `.env` (РЕАЛЬНЫЕ КЛЮЧИ) ✅
- **Статус:** В `.gitignore` - НЕ коммитится
- **Назначение:** Хранить реальные API ключи и секреты
- **Содержимое:** Реальные значения всех ключей
- **Безопасность:** ✅ Защищен от коммита

### 2. `env` (ШАБЛОН) ✅
- **Статус:** Может быть закоммичен (без реальных ключей)
- **Назначение:** Шаблон конфигурации
- **Содержимое:** Все переменные с пустыми значениями или примерами
- **Безопасность:** ✅ Не содержит реальных ключей

### 3. `env.prod` / `env.dev` (ШАБЛОНЫ) ✅
- **Статус:** Могут быть закоммичены (без реальных ключей)
- **Назначение:** Шаблоны для разных окружений
- **Содержимое:** Настройки без реальных ключей
- **Безопасность:** ✅ Не содержат реальных ключей

---

## 🔧 ПРАВИЛЬНАЯ НАСТРОЙКА

### Шаг 1: Создайте `.env` файл
```bash
# Скопируйте шаблон
cp env .env

# Или создайте вручную
touch .env
```

### Шаг 2: Добавьте реальные ключи в `.env`
```bash
# Отредактируйте .env
nano .env

# Добавьте реальные значения:
BITGET_API_KEY=your_real_api_key_here
BITGET_SECRET_KEY=your_real_secret_key_here
BITGET_PASSPHRASE=your_real_passphrase_here
TELEGRAM_TOKEN=your_real_telegram_token_here
```

### Шаг 3: Убедитесь, что `.env` в `.gitignore`
```bash
# Проверьте .gitignore
grep "\.env" .gitignore

# Должно быть:
# *.env
# env
```

---

## 🚫 ЧТО НЕЛЬЗЯ ДЕЛАТЬ

### ❌ НЕ КОММИТЬТЕ:
```bash
# ❌ НЕ ДЕЛАЙТЕ ЭТОГО:
git add env          # Если в нем есть реальные ключи
git add .env         # Никогда не коммитьте .env
git commit -m "..."  # С реальными ключами
```

### ❌ НЕ ХРАНИТЕ:
- Реальные ключи в файле `env`
- Реальные ключи в файлах `env.prod` / `env.dev`
- Реальные ключи в любых файлах, которые коммитятся в git

---

## ✅ ЧТО МОЖНО ДЕЛАТЬ

### ✅ МОЖНО КОММИТЬТЬ:
```bash
# ✅ МОЖНО:
git add env          # Если в нем НЕТ реальных ключей (только шаблон)
git add env.prod     # Если в нем НЕТ реальных ключей
git add env.dev      # Если в нем НЕТ реальных ключей
git add .gitignore   # Всегда можно
```

### ✅ ПРАВИЛЬНОЕ ХРАНЕНИЕ:
- Реальные ключи только в `.env` (который в `.gitignore`)
- Реальные ключи в базе данных (зашифрованные)
- Реальные ключи в переменных окружения системы

---

## 🔍 ПРОВЕРКА БЕЗОПАСНОСТИ

### Перед коммитом проверьте:
```bash
# 1. Проверьте, что .env в .gitignore
grep "\.env\|^env$" .gitignore

# 2. Проверьте, что в env нет реальных ключей
grep -E "BITGET_API_KEY=|TELEGRAM_TOKEN=" env | grep -v "^#"

# 3. Проверьте статус git
git status

# Убедитесь, что .env НЕ в списке измененных файлов
```

---

## 📝 ПРИОРИТЕТ ЗАГРУЗКИ ПЕРЕМЕННЫХ

Система загружает переменные в следующем порядке:

1. **`.env`** (приоритет) - реальные ключи
2. `ATRA_ENV_FILE` (переменная окружения)
3. `env.prod` / `env.dev` (в зависимости от `ATRA_ENV`)
4. `env` (шаблон, fallback)

Это означает, что `.env` всегда имеет приоритет над другими файлами.

---

## 🎯 ПРАВИЛО ПРОЕКТА

### Обязательное правило:
> **НИКОГДА не коммитьте файл `env` с реальными ключами в git.**
> **ВСЕГДА используйте `.env` (который в `.gitignore`) для реальных ключей.**

### Проверка перед коммитом:
1. ✅ Убедитесь, что `.env` в `.gitignore`
2. ✅ Убедитесь, что файл `env` не содержит реальных ключей
3. ✅ Проверьте `git status` - `.env` не должен быть в списке
4. ✅ Если нужно, добавьте `env` в `.gitignore` (уже добавлено)

---

## 🔐 ДОПОЛНИТЕЛЬНАЯ БЕЗОПАСНОСТЬ

### Для продакшена:
- Используйте зашифрованные ключи в базе данных
- Храните ключи в переменных окружения системы
- Используйте секретные менеджеры (если доступны)

### Для разработки:
- Используйте `.env` для локальных ключей
- Не делитесь `.env` файлом
- Используйте разные ключи для dev и prod

---

## ✅ СТАТУС: ПРАВИЛО УСТАНОВЛЕНО

**Правило безопасности установлено и применено:**

1. ✅ Файл `env` добавлен в `.gitignore`
2. ✅ Файл `.env` уже в `.gitignore` (через `*.env`)
3. ✅ Ключи удалены из файла `env` (оставлены пустыми)
4. ✅ Ключи добавлены в файл `.env`
5. ✅ `config.py` обновлен для загрузки `.env` в первую очередь
6. ✅ Документация создана

---

*Правило создано автоматически системой безопасности ATRA*

