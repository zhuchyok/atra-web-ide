# Только зависимости для Streamlit дашборда (код монтируется при запуске)
FROM python:3.11-slim

WORKDIR /app
COPY dashboard/requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

EXPOSE 8501
