# 🔧 PRIORITY 2: План рефакторинга signal_live.py

**Дата:** November 23, 2025  
**Команда:** Игорь (Backend), Анна (QA), Мария (Technical Writer)  
**Статус:** 🚀 **В ПРОЦЕССЕ**

---

## 🎯 ЦЕЛЬ

Разбить большой файл `signal_live.py` (6728 строк) на логические модули для улучшения поддерживаемости.

---

## 📊 АНАЛИЗ ТЕКУЩЕЙ СТРУКТУРЫ

### **Основные компоненты:**

1. **Валидация и фильтры** (~1500 строк)
   - `SignalQualityValidator`
   - `PatternConfidenceScorer`
   - `DynamicSymbolBlocker`
   - `PipelineMonitor`
   - `SmartRSIFilter`
   - Различные функции проверки фильтров

2. **Генерация сигналов** (~3000 строк)
   - `_generate_signal_impl()` - основная функция
   - Различные паттерны (classic, alternative_1, alternative_2, alternative_3)
   - LONG и SHORT сигналы

3. **ML интеграция** (~500 строк)
   - `check_ml_filter()`
   - Интеграция с LightGBM

4. **MTF Confirmation** (~300 строк)
   - `_run_mtf_confirmation_with_logging()`
   - Гибридная MTF система

5. **Утилиты и хелперы** (~1000 строк)
   - Функции для работы с данными
   - Функции для работы с API
   - Различные вспомогательные функции

6. **Основной цикл** (~500 строк)
   - `check_and_send_signals()`
   - Обработка пользователей
   - Отправка сигналов

---

## 📁 ПЛАН РЕФАКТОРИНГА

### **Новая структура:**

```
src/
├── signals/
│   ├── __init__.py
│   ├── generation.py          # Основная логика генерации сигналов
│   ├── patterns.py             # Паттерны (classic, alternative_1, etc.)
│   ├── filters.py              # Все фильтры и валидация
│   ├── ml_integration.py       # ML фильтр и интеграция
│   ├── mtf_confirmation.py     # MTF подтверждение
│   └── signal_processor.py     # Основной цикл обработки
```

---

## 🔧 ПЛАН ДЕЙСТВИЙ

### **Этап 1: Создание модулей (Игорь)**
1. Создать `src/signals/filters.py` - все фильтры
2. Создать `src/signals/ml_integration.py` - ML интеграция
3. Создать `src/signals/mtf_confirmation.py` - MTF
4. Создать `src/signals/patterns.py` - паттерны
5. Создать `src/signals/generation.py` - основная логика
6. Создать `src/signals/signal_processor.py` - основной цикл

### **Этап 2: Миграция кода (Игорь)**
1. Перенести классы фильтров
2. Перенести ML интеграцию
3. Перенести MTF confirmation
4. Перенести паттерны
5. Перенести основную логику
6. Обновить импорты в signal_live.py

### **Этап 3: Тесты (Анна)**
1. Создать тесты для filters.py
2. Создать тесты для ml_integration.py
3. Создать тесты для patterns.py
4. Создать тесты для generation.py

### **Этап 4: Документация (Мария)**
1. Создать диаграмму архитектуры
2. Обновить API документацию
3. Добавить примеры использования

---

## ⚠️ ВАЖНО

- Сохранить обратную совместимость
- Не ломать существующий функционал
- Тестировать после каждого этапа
- Делать постепенно, не все сразу

---

## ✅ СТАТУС

**План создан:** ✅  
**Начало работы:** 🚀

---

*План подготовлен: Виктор (Team Lead) + Игорь, Анна, Мария*

