#!/bin/bash
# Скрипт для деплоя на сервер через SSH

set -e

SERVER="root@185.177.216.15"
REMOTE_DIR="/root/atra"
SCRIPT_NAME="update_and_check_bot.sh"

echo "=================================================================================="
echo "👥 КОМАНДА ИЗ 13 ЭКСПЕРТОВ - ДЕПЛОЙ НА СЕРВЕР"
echo "=================================================================================="
echo ""

# Копируем скрипт на сервер
echo "📤 Копируем скрипт на сервер..."
scp scripts/$SCRIPT_NAME $SERVER:$REMOTE_DIR/

# Запускаем скрипт на сервере
echo ""
echo "🚀 Запускаем обновление и проверку на сервере..."
ssh $SERVER "cd $REMOTE_DIR && chmod +x $SCRIPT_NAME && ./$SCRIPT_NAME"

echo ""
echo "=================================================================================="
echo "✅ ДЕПЛОЙ ЗАВЕРШЕН"
echo "=================================================================================="
