# Анализ падения MLX Server 26.01.2026

## 📊 Хронология падения

### Время падения: 20:05:26 - 20:05:31

1. **20:05:26** - MLX Server начал отвечать `404 Not Found` на запросы к модели
   - Модель `deepseek-r1-distill-llama:70b` не найдена
   - Множественные параллельные запросы (видно по логам)

2. **20:05:29** - `Server disconnected without sending a response`
   - Сервер начал падать, но еще не полностью упал
   - ExtendedThinking переключился на fallback (tinyllama)

3. **20:05:31** - `All connection attempts failed`
   - Полное падение сервера
   - Код завершения: **-6 (SIGABRT)** - аварийное завершение

## 🔍 Причины падения (SIGABRT)

SIGABRT (-6) означает аварийное завершение процесса. Возможные причины:

1. **Нехватка памяти (OOM)** - наиболее вероятно
   - Модель `deepseek-r1-distill-llama:70b` очень большая (~70B параметров)
   - Множественные параллельные запросы
   - Система могла убить процесс из-за нехватки памяти

2. **Assertion failed** - критическая ошибка в коде
   - Возможно, проверка в MLX библиотеке не прошла
   - Или внутренняя ошибка в FastAPI/uvicorn

3. **Критическая ошибка Python**
   - Необработанное исключение, которое привело к abort()

## 📈 Нагрузка перед падением

### Из логов видно:
- **Множественные параллельные запросы** к MLX Server
- **Попытки загрузить модель** `deepseek-r1-distill-llama:70b`
- **404 ошибки** - модель не найдена или не загружена
- **Быстрое переключение на fallback** (Ollama tinyllama)

### Проблема:
- MLX Server не успел обработать запросы
- Возможно, превышен лимит параллельных запросов (5)
- Или нехватка памяти при загрузке модели

## ❌ Проблема с логированием

**До исправления:**
- MLX Server не логировал в файл
- Логи терялись (только stdout/stderr в Supervisor)
- Невозможно было увидеть причину падения

**После исправления:**
- ✅ Логирование в `logs/mlx_api_server.log`
- ✅ stdout в `logs/mlx_server_stdout.log`
- ✅ stderr в `logs/mlx_server_stderr.log`

## 🔧 Что нужно проверить в следующий раз

1. **Логи MLX Server** (`logs/mlx_api_server.log`)
   - Ошибки перед падением
   - Использование памяти
   - Активные запросы

2. **stderr лог** (`logs/mlx_server_stderr.log`)
   - Python traceback
   - Assertion errors
   - OOM сообщения

3. **Системные логи** (если доступны)
   - `dmesg` или `syslog` для OOM killer
   - Использование памяти системой

4. **Нагрузка в момент падения**
   - Количество активных запросов
   - Использование памяти
   - Загруженные модели

## 📝 Рекомендации

1. **Улучшить мониторинг памяти**
   - Более агрессивная очистка моделей
   - Предупреждения при высоком использовании памяти

2. **Ограничить параллельные запросы**
   - Уменьшить `_max_concurrent_requests` если память ограничена
   - Добавить очередь запросов

3. **Улучшить обработку ошибок**
   - Graceful degradation вместо падения
   - Возврат ошибки вместо SIGABRT

4. **Предзагрузка моделей**
   - Загружать модели заранее
   - Проверять доступность памяти перед загрузкой

## ✅ Исправления применены

1. ✅ Добавлено логирование MLX Server в файл
2. ✅ Supervisor сохраняет stdout/stderr в файлы
3. ✅ Улучшена диагностика падений

## 🔄 Следующие шаги

1. Перезапустить MLX Server с новым логированием
2. Запустить тест снова
3. Проверить логи при следующем падении (если произойдет)
4. Анализировать причину по логам
