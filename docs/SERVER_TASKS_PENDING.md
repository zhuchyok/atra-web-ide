# 🖥️ ЗАДАЧИ ДЛЯ СЕРВЕРА MAC STUDIO

**Дата:** 2026-01-21  
**Фокус:** Инфраструктура, мониторинг, миграция данных

---

## ✅ УЖЕ СДЕЛАНО

1. ✅ **Система бэкапов Knowledge OS**
   - Локальные бэкапы (03:00, launchd)
   - Синхронизация в Google Drive (03:10, cron)
   - Мониторинг здоровья (04:00, cron)
   - DR-тесты

2. ✅ **Docker инфраструктура**
   - docker-compose.yml настроен
   - Все сервисы определены

3. ✅ **Настройка агентов**
   - Nightly Learner настроен (Redis, MLX URL)
   - Victoria и Veronica готовы

---

## 🔥 ПРИОРИТЕТНЫЕ ЗАДАЧИ

### 1. **МИГРАЦИЯ ДАННЫХ С ВНЕШНЕГО СЕРВЕРА** 🔥🔥🔥

**Статус:** ⚠️ Не выполнена  
**Приоритет:** КРИТИЧЕСКИЙ

**Что нужно:**

- Миграция всех данных Knowledge OS с сервера `185.177.216.15`
- Проверка целостности данных после миграции
- Обновление конфигураций для работы с Mac Studio

**Скрипт:** `scripts/migration/migrate_to_mac_studio.py`

**Шаги:**

```bash
# 1. Проверка подключения к серверу
ssh root@185.177.216.15

# 2. Создание бэкапа на сервере
# (если еще не создан)

# 3. Запуск миграции
python3 scripts/migration/migrate_to_mac_studio.py

# 4. Верификация данных
python3 scripts/verify_migration.py
```

---

### 2. **НАСТРОЙКА GRAFANA ДАШБОРДОВ** 🔥🔥

**Статус:** ⚠️ Частично (есть конфигурация, но дашборды не настроены)  
**Приоритет:** ВЫСОКИЙ

**Что нужно:**

- Настройка дашбордов для мониторинга:
  - Knowledge OS метрики
  - Использование моделей MLX
  - Статус агентов (Victoria, Veronica, Nightly)
  - Использование ресурсов (CPU, память, диск)
  - Статус бэкапов

**Файлы:**

- `knowledge_os/dashboard/grafana_dashboard.json` - нужно обновить
- `infrastructure/monitoring/prometheus.yml` - проверить метрики

**Шаги:**

```bash
# 1. Проверка Prometheus метрик
curl http://localhost:9090/api/v1/targets

# 2. Импорт дашбордов в Grafana
# Открыть http://localhost:3000
# Импортировать дашборды из knowledge_os/dashboard/
```

---

### 3. **НАСТРОЙКА АЛЕРТОВ** 🔥🔥

**Статус:** ❌ Отсутствует  
**Приоритет:** ВЫСОКИЙ

**Что нужно:**

- Алерты для критических событий:
  - Сервис недоступен (Knowledge OS API, MLX Server)
  - Бэкап не создался
  - Диск заполнен (>80%)
  - Агент упал (Victoria, Veronica, Nightly)
  - Ошибки в логах

**Где настроить:**

- Prometheus Alertmanager
- Или простые скрипты с уведомлениями (Telegram, email)

---

### 4. **ПРОВЕРКА РАБОТЫ ВСЕХ СЕРВИСОВ** 🔥

**Статус:** ⚠️ Нужна проверка  
**Приоритет:** ВЫСОКИЙ

**Что проверить:**

- ✅ Knowledge OS API работает
- ✅ Knowledge OS Worker обрабатывает задачи
- ✅ Victoria Agent работает
- ✅ Veronica Agent работает
- ✅ Nightly Learner работает (уже настроен)
- ✅ MLX API Server доступен
- ✅ Prometheus собирает метрики
- ✅ Grafana отображает данные

**Скрипт:** `scripts/check_all_services.sh` (если есть)

**Команды:**

```bash
# Проверка всех сервисов
docker-compose ps

# Health checks
curl http://localhost:8000/health
curl http://localhost:11434/
curl http://localhost:9090/-/healthy
curl http://localhost:3000/api/health

# Логи агентов
docker-compose logs victoria-agent --tail=50
docker-compose logs veronica-agent --tail=50
docker-compose logs nightly-learner --tail=50
```

---

### 5. **НАСТРОЙКА АВТОМАТИЧЕСКИХ ЗАДАЧ** 🔥

**Статус:** ⚠️ Частично (бэкапы настроены)  
**Приоритет:** СРЕДНИЙ

**Что нужно:**

- ✅ Бэкапы Knowledge OS (уже настроено)
- ⚠️ Автоматический перезапуск упавших сервисов (Docker restart policies)
- ⚠️ Очистка старых логов
- ⚠️ Мониторинг использования диска
- ⚠️ Автоматическое обновление моделей (опционально)

**Docker restart policies:**

```yaml
# В docker-compose.yml добавить:
restart: unless-stopped # для всех сервисов
```

---

### 6. **ОПТИМИЗАЦИЯ ИСПОЛЬЗОВАНИЯ РЕСУРСОВ**

**Статус:** ⚠️ Нужна проверка  
**Приоритет:** СРЕДНИЙ

**Что проверить:**

- Использование памяти (128GB)
- Использование диска (2TB)
- Нагрузка на CPU
- Оптимизация загрузки моделей (lazy loading)

**Команды:**

```bash
# Использование ресурсов
docker stats

# Использование диска
df -h
du -sh ~/.ollama/models/blobs
du -sh ~/.cache/huggingface

# Проверка памяти
vm_stat
```

---

### 7. **НАСТРОЙКА ЛОГИРОВАНИЯ И РОТАЦИИ ЛОГОВ**

**Статус:** ⚠️ Частично  
**Приоритет:** СРЕДНИЙ

**Что нужно:**

- Централизованное логирование (если еще нет)
- Ротация логов (чтобы не заполняли диск)
- Структурированные логи (JSON)

**Где:**

- Docker logs (уже есть)
- Файловые логи в `logs/`
- Возможно добавить ELK или Loki

---

### 8. **БЕЗОПАСНОСТЬ И ДОСТУП**

**Статус:** ⚠️ Нужна проверка  
**Приоритет:** СРЕДНИЙ

**Что проверить:**

- Порты не открыты наружу (только localhost)
- Пароли и ключи в .env (не в git)
- Ограничение доступа к Grafana/Prometheus
- Firewall настройки (если нужно)

---

## 📋 РЕКОМЕНДУЕМЫЙ ПОРЯДОК

### Фаза 1 (Критическое - 1-2 дня):

1. Миграция данных с сервера
2. Проверка работы всех сервисов
3. Настройка алертов

### Фаза 2 (Важное - 2-3 дня):

4. Настройка Grafana дашбордов
5. Автоматические задачи (restart policies, очистка логов)
6. Оптимизация ресурсов

### Фаза 3 (Оптимизация - по необходимости):

7. Централизованное логирование
8. Безопасность и доступ

---

## 🔍 БЫСТРАЯ ПРОВЕРКА ТЕКУЩЕГО СОСТОЯНИЯ

```bash
# 1. Статус всех контейнеров
docker-compose ps

# 2. Health checks
./scripts/check_all_services.sh  # если есть

# 3. Логи последних ошибок
docker-compose logs --tail=100 | grep -i error

# 4. Использование ресурсов
docker stats --no-stream

# 5. Проверка бэкапов
bash scripts/check_backups_health.sh
```

---

**Последнее обновление:** 2026-01-21
