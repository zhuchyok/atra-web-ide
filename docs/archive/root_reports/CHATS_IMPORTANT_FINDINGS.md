# 📚 ВАЖНЫЕ НАХОДКИ ИЗ ИЗУЧЕНИЯ ЧАТОВ

**Дата:** 2026-01-26  
**Статус:** ✅ **ИЗУЧАЮ ТРАНСКРИПТЫ**

---

## ⚠️ КРИТИЧЕСКИ ВАЖНОЕ ИЗ ЧАТОВ

### 1. База данных - использование существующей БД

**Из транскриптов:**

- НЕ создавать новую БД в `knowledge_os/docker-compose.yml`
- Использовать существующую БД `knowledge_postgres`
- Подключение: `DATABASE_URL: postgresql://admin:secret@knowledge_postgres:5432/knowledge_os`

**Что сделано:**

- ✅ В `knowledge_os/docker-compose.yml` БД закомментирована
- ✅ `DATABASE_URL` изменен на `knowledge_postgres:5432`
- ✅ `depends_on: db` удалены

---

### 2. Knowledge OS - не изучена полностью

**Из транскрипта `bed9cffa-a24d-4ca3-958b-17209d7b7ade.txt`:**

- "папку `knowledge_os/` я **не изучила полностью**"
- "не проходила _все_ ~150 модулей `knowledge_os/app/*.py`"
- "критический путь и интеграционные точки я разобрала"

**Важные компоненты:**

- `ai_core.py` - ядро системы
- `telegram_simple.py`, `main.py`/`main_enhanced.py` - UI/интеграции
- Таск-система на Postgres (`tasks`, `experts`, `knowledge_nodes`)
- Автономные компоненты (`singularity_autonomous.py`, мониторинг, self-healing)

---

### 3. Мультиагентная система

**Из транскрипта `eeb837fd-d8cd-40c8-a583-f5c6fcc92067.txt`:**

- Обсуждение: "похоже ли текущее решение на мультиагентную систему"
- Что не хватает для "настоящей" MAS:
  - Единый интерфейс агента
  - Единый протокол сообщений
  - Диспетчеризация/шина событий
  - Память/blackboard
  - Политики координации

**Что было сделано:**

- Добавлен MAS-core в `src/agents/mas/`
- Протокол сообщений (`protocol.py`)
- Реестр агентов (`registry.py`)
- Event-bus (`event_bus.py`)
- Оркестратор с routing + audit + blackboard
- Blackboard (shared memory)
- Audit-log с редактированием секретов
- Идемпотентность для side-effect tool calls

---

### 4. Вероника - две роли

**Из транскрипта `4e83128d-29f9-49c5-9f08-a04aec292e87.txt`:**

- **Вероника-Research** (web + локальные модели) - всегда включена
- **Вероника-Operator** (tools: команды/ssh/патчи) - отдельно, выключена по умолчанию

**Безопасность:**

- Tool-executor = удаленное выполнение действий
- Повышенный риск для "корпоративного мозга"
- Нужна аутентификация/авторизация
- Allowlist команд/операций
- Полный audit-log всех действий

---

### 5. Конфликты портов

**Важно:**

- НЕ запускать одновременно `atra` и `atra-web-ide`
- Конфликты портов: 5432, 8010, 8011
- Для торговой системы: используйте `atra`
- Для Web IDE: используйте `atra-web-ide`

---

## 📊 КЛЮЧЕВЫЕ РЕШЕНИЯ ИЗ ЧАТОВ

### Архитектурные решения:

1. Использование существующей БД `knowledge_postgres`
2. Разделение ролей Вероники (Research vs Operator)
3. MAS-core для мультиагентной системы
4. Event-bus для слабосвязанных агентов
5. Blackboard для общей памяти

### Конфигурационные решения:

1. `USE_VICTORIA_ENHANCED=true` и `USE_VERONICA_ENHANCED=true`
2. Использование внешней сети `atra-network`
3. Подключение к `knowledge_postgres` вместо создания новой БД
4. Volume mapping для `knowledge_os`

### Безопасность:

1. Tool-executor выключен по умолчанию
2. Allowlist команд/операций
3. Audit-log всех действий
4. Идемпотентность для side-effect операций

---

## 🔍 ЧТО НУЖНО ПРОВЕРИТЬ

1. ✅ Использование `knowledge_postgres` вместо создания новой БД
2. ⏳ Полное изучение `knowledge_os/app/*.py` (150+ модулей)
3. ⏳ Проверка MAS-core компонентов
4. ⏳ Проверка разделения ролей Вероники
5. ⏳ Проверка безопасности tool-executor

---

_Отчет создан: 2026-01-26_  
_Изучение продолжается..._
