# 💻 Claude Code + Ollama: Полное объяснение для ATRA

**Дата:** 2026-01-26  
**Для кого:** Разработчики ATRA Web IDE

---

## 🎯 Что такое Claude Code?

**Claude Code** - это **агентный инструмент для кодирования** от компании Anthropic (создатели Claude AI). Это не просто чат-бот, а полноценный **AI-ассистент в терминале**, который может:

### ✨ Что умеет Claude Code:

1. **📖 Читать код** - Анализировать весь проект, понимать структуру
2. **✏️ Модифицировать код** - Редактировать файлы, добавлять функции
3. **▶️ Выполнять код** - Запускать команды, тесты, скрипты
4. **🔍 Анализировать проекты** - Находить баги, предлагать улучшения
5. **📝 Писать код** - Создавать новые файлы, функции, модули

---

## 🤔 Зачем это НАМ в ATRA?

### Проблема сейчас:

**Victoria работает через API:**
- Вы отправляете задачу → Victoria думает → Возвращает ответ
- Нет прямого доступа к файлам проекта
- Нет возможности "видеть" весь контекст проекта сразу
- Нет интерактивного режима разработки

### Решение с Claude Code:

**Claude Code работает локально:**
- Открываете терминал в директории проекта
- Claude Code "видит" ВСЕ файлы проекта
- Может сразу читать, редактировать, выполнять
- Интерактивный режим - как работа с коллегой рядом

---

## 🔗 Как работает Claude Code + Ollama/MLX?

### Обычно Claude Code работает так:

```
Claude Code → Anthropic API (платно) → Claude Sonnet → Ответ
```

### С Ollama работает так:

```
Claude Code → Ollama (локально/бесплатно) → Ollama модель → Ответ
```

**Ollama эмулирует Anthropic API**, поэтому Claude Code думает, что работает с Anthropic, но на самом деле использует Ollama модели.

### С MLX API Server работает так (НОВОЕ!):

```
Claude Code → MLX API Server (локально/быстро) → MLX модель → Ответ
```

**MLX API Server теперь тоже эмулирует Anthropic API!** Теперь Claude Code может работать напрямую с нашими MLX моделями, которые быстрее на Apple Silicon!

---

## 🚀 Практические примеры использования

### Пример 1: Быстрый рефакторинг

**Без Claude Code:**
```
1. Открываете файл вручную
2. Ищете проблемные места
3. Редактируете
4. Тестируете
5. Коммитите
```

**С Claude Code:**
```bash
cd /Users/bikos/Documents/atra-web-ide
claude --model qwen3-coder

# В Claude Code:
"Найди все места где используется устаревший API и обнови их"
→ Claude Code автоматически:
  - Находит все вхождения
  - Обновляет код
  - Показывает diff
  - Предлагает протестировать
```

### Пример 2: Добавление новой функции

**Без Claude Code:**
```
1. Планируете структуру
2. Создаете файлы
3. Пишете код
4. Тестируете
5. Интегрируете
```

**С Claude Code:**
```bash
claude --model qwen3-coder

# В Claude Code:
"Добавь функцию для обработки PDF в Telegram боте, используя llava:7b"
→ Claude Code:
  - Изучает существующий код бота
  - Понимает архитектуру
  - Добавляет функцию в правильное место
  - Интегрирует с существующим кодом
  - Показывает что изменилось
```

### Пример 3: Отладка багов

**Без Claude Code:**
```
1. Читаете логи
2. Ищете проблемный код
3. Догадываетесь в чем проблема
4. Исправляете
5. Тестируете
```

**С Claude Code:**
```bash
claude --model qwen3-coder

# В Claude Code:
"Почему Victoria не отвечает в Telegram? Посмотри логи и код"
→ Claude Code:
  - Читает логи
  - Анализирует код бота
  - Находит проблему
  - Предлагает решение
  - Может сразу исправить
```

---

## 💡 Преимущества для ATRA

### 1. **Локальная разработка**
- Не нужно отправлять код в облако
- Всё работает на вашем Mac Studio
- Приватность и безопасность

### 2. **Использование наших моделей**
- **MLX модели** (быстрее на Apple Silicon):
  - `qwen2.5-coder:32b` - специализированная coding модель
  - `phi3.5:3.8b` - быстрая модель
  - `tinyllama:1.1b-chat` - очень быстрая модель
- **Ollama модели**:
  - `qwen3-coder` - специализированная coding модель
  - `glm-4.7` - мощная reasoning модель
  - `gpt-oss:20b/120b` - максимальное качество (Cloud)
- **Бесплатно** (локально) или через Cloud

### 3. **Полный контекст проекта**
- Claude Code видит ВСЮ структуру проекта
- Понимает зависимости между файлами
- Может работать с несколькими файлами одновременно

### 4. **Интерактивный режим**
- Как работа с коллегой-программистом
- Задаете вопрос → Получаете ответ + код
- Можете уточнять, просить изменить

### 5. **Интеграция с Victoria**
- Victoria может планировать задачи
- Claude Code может выполнять их локально
- Комбинация: стратегия (Victoria) + тактика (Claude Code)

---

## 🛠️ Как настроить и использовать

### Шаг 1: Установить Claude Code

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

### Шаг 2: Настроить для работы с Ollama или MLX

**Вариант А: С MLX API Server (рекомендуется для Mac Studio)**
```bash
# Запустить MLX API Server
python3 -m uvicorn knowledge_os.app.mlx_api_server:app --host 0.0.0.0 --port 11435

# Настроить Claude Code для MLX
export ANTHROPIC_AUTH_TOKEN=ollama
export ANTHROPIC_API_KEY=""
export ANTHROPIC_BASE_URL=http://localhost:11435  # MLX API Server!
```

**Вариант Б: С Ollama (автоматически)**
```bash
ollama launch claude
```

**Вариант В: С Ollama (вручную)**
```bash
export ANTHROPIC_AUTH_TOKEN=ollama
export ANTHROPIC_API_KEY=""
export ANTHROPIC_BASE_URL=http://localhost:11434  # Ollama
```

### Шаг 3: Запустить Claude Code

```bash
# Перейти в директорию проекта
cd /Users/bikos/Documents/atra-web-ide

# С MLX моделью (быстрее на Mac Studio)
claude --model qwen2.5-coder:32b

# Или с Ollama моделью
claude --model qwen3-coder
```

### Шаг 4: Использовать!

```
Claude Code готов к работе!

Вы можете:
- Задать вопрос о коде
- Попросить добавить функцию
- Попросить исправить баг
- Попросить рефакторить код
- Попросить написать тесты
```

---

## 📊 Сравнение: Victoria vs Claude Code

| Функция | Victoria (API) | Claude Code (Локально) |
|---------|----------------|------------------------|
| **Доступ к файлам** | Через API | Прямой доступ |
| **Контекст проекта** | Ограниченный | Полный |
| **Интерактивность** | Запрос-ответ | Диалог |
| **Выполнение команд** | Через API | Напрямую |
| **Скорость** | Зависит от сети | Мгновенно |
| **Приватность** | Данные уходят на сервер | Всё локально |
| **Использование** | Через Telegram/Web | В терминале |

---

## 🎯 Когда использовать что?

### Используйте **Victoria** когда:
- ✅ Нужно планирование и координация
- ✅ Работаете через Telegram/Web интерфейс
- ✅ Нужна стратегия и архитектурные решения
- ✅ Работаете удаленно

### Используйте **Claude Code** когда:
- ✅ Нужно быстро исправить баг
- ✅ Работаете локально над кодом
- ✅ Нужен полный контекст проекта
- ✅ Хотите интерактивную разработку

---

## 🔄 Интеграция Victoria + Claude Code

### Сценарий 1: Планирование + Выполнение

```
1. Victoria (Telegram): "Добавь обработку PDF в бот"
2. Victoria анализирует задачу, планирует
3. Victoria создает план действий
4. Вы открываете Claude Code
5. Claude Code выполняет план локально
6. Результат отправляется обратно в Victoria
```

### Сценарий 2: Отладка

```
1. Victoria (Telegram): "Бот не отвечает"
2. Victoria анализирует логи (если доступны)
3. Вы открываете Claude Code в директории проекта
4. Claude Code: "Почему бот не отвечает?"
5. Claude Code находит проблему, исправляет
6. Тестируете, коммитите
```

---

## 💰 Стоимость

### Claude Code + Anthropic API:
- 💸 **Платно** - ~$3-15 за 1M токенов
- 🌐 Требует интернет
- ⚡ Быстро, но дорого

### Claude Code + Ollama (локально):
- 🆓 **Бесплатно** - используем наши модели
- 🏠 Работает локально
- ⚡ Быстро и бесплатно

### Claude Code + Ollama Cloud:
- 💰 **Платно** - но дешевле чем Anthropic
- 🌐 Требует интернет
- ⚡ Очень мощные модели (120B параметров)

---

## 🚀 Рекомендации для ATRA

### Для ежедневной разработки (Mac Studio):
```bash
# Используйте MLX модель (быстрее!)
export ANTHROPIC_BASE_URL=http://localhost:11435
claude --model qwen2.5-coder:32b
```

### Для ежедневной разработки (Ollama):
```bash
# Используйте Ollama модель
export ANTHROPIC_BASE_URL=http://localhost:11434
claude --model qwen3-coder
```

### Для сложных задач:
```bash
# Используйте Cloud модель (если есть API ключ)
export OLLAMA_API_KEY=your_key
export ANTHROPIC_BASE_URL=http://localhost:11434
claude --model gpt-oss:120b-cloud
```

### Для быстрых правок:
```bash
# Используйте быструю MLX модель
export ANTHROPIC_BASE_URL=http://localhost:11435
claude --model phi3.5:3.8b
```

---

## 📝 Примеры команд для Claude Code

### Анализ кода:
```
"Проанализируй victoria_telegram_bot.py и найди потенциальные проблемы"
```

### Рефакторинг:
```
"Рефактори функцию handle_telegram_media, сделай её более читаемой"
```

### Добавление функции:
```
"Добавь обработку видео файлов в Telegram бот, аналогично обработке фото"
```

### Исправление бага:
```
"Почему Victoria не отвечает? Найди проблему и исправь"
```

### Написание тестов:
```
"Напиши unit тесты для функции process_pdf"
```

### Документация:
```
"Добавь docstrings ко всем функциям в victoria_telegram_bot.py"
```

---

## ✅ Итого: Зачем нам Claude Code?

1. **🚀 Быстрая разработка** - Интерактивная работа с кодом
2. **🔍 Полный контекст** - Видит весь проект сразу
3. **💰 Бесплатно** - Используем наши локальные модели
4. **🔒 Приватно** - Всё работает локально
5. **🤝 Дополняет Victoria** - Victoria планирует, Claude Code выполняет
6. **📈 Продуктивность** - Меньше времени на рутинные задачи

---

## 🎓 Следующие шаги

1. ✅ Установите Claude Code: `curl -fsSL https://claude.ai/install.sh | bash`
2. ✅ Настройте для Ollama: `ollama launch claude`
3. ✅ Попробуйте на простой задаче
4. ✅ Интегрируйте в свой workflow

**Claude Code - это как иметь опытного коллегу-программиста, который всегда готов помочь!** 🚀