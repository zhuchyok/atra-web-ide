# Анализ предложенных улучшений архитектуры

Документ сопоставляет детальный план улучшений с текущей реализацией: что уже есть, что не нужно или низкий приоритет, что стоит внедрить.

---

## 1. Уже реализовано

### 1.1 Кэширование

| Предложение | Реализация |
|-------------|------------|
| Кэширование промптов/ответов | **Enhanced Cache** (`knowledge_os/app/enhanced_cache.py`) — кэш для Victoria Enhanced по (method, goal, context). **PromptCache** опционально. **semantic_ai_cache** в ai_core для экспертов. **Redis** — поиск в main.py (search cache). Кэш здоровья узлов и скана моделей в local_router / available_models_scanner. |
| Многоуровневый кэш (L1/L2/L3) | Нет единого класса MultiLayerCache. Есть разрозненно: in-memory (Enhanced, health, scan), Redis (search, orchestrator), БД (semantic_ai_cache). Объединять в один слой не обязательно — текущая схема достаточна. |

**Вывод:** Кэширование уже есть на ключевых путях. При желании можно добавить кэш по хэшу (prompt + category) в LocalAIRouter для повторяющихся запросов — быстрая победа.

---

### 1.2 Circuit Breaker

| Предложение | Реализация |
|-------------|------------|
| Circuit Breaker для моделей/сервисов | **knowledge_os/app/circuit_breaker.py** — общий CB с записью в БД. Используется в **ai_core** для database, local_models, cloud; в **mlx_server_supervisor** для MLX. В **src** — свой CircuitBreaker для источников данных. |

**Вывод:** Реализовано. Дополнительно можно обернуть вызовы к конкретным узлам Ollama/MLX в local_router в CB по узлу (при необходимости).

---

### 1.3 Трассировка и correlation ID

| Предложение | Реализация |
|-------------|------------|
| Детальная трассировка, correlation ID | **observability.py** — OpenTelemetry, `trace_span`, атрибуты. **victoria_enhanced** оборачивает solve в span. **event_bus** — correlation_id для request/response. **task_distribution_system** — correlation_id в заданиях. |

**Вывод:** Трассировка и correlation_id есть. **Реализовано (2026-02-03):** Backend chat генерирует correlation_id, передаёт в VictoriaClient.run/run_stream → заголовок X-Correlation-ID → Victoria. Victoria уже читает X-Correlation-ID и прокидывает по цепочке. SSE step содержит correlation_id для отладки.

---

### 1.4 Умное планирование и оценка сложности

| Предложение | Реализация |
|-------------|------------|
| Контекстное планирование, переформулировка цели | **victoria_server.understand_goal()** — restated, category (simple|investigate|multi_step), first_step. Результат используется в plan и executor. |
| Оценка сложности задачи | **intelligent_model_router.estimate_task_complexity()** — по тексту и категории. **department_heads_system.determine_complexity()** — SIMPLE/MEDIUM/COMPLEX. Используются в victoria_enhanced и smart_worker. |

**Вывод:** Understand → Plan → Execute и оценка сложности уже встроены. Дальше — только тонкая настройка порогов и категорий.

---

### 1.5 Балансировка нагрузки и выбор модели

| Предложение | Реализация |
|-------------|------------|
| Балансировка Ollama/MLX по загрузке | **load_balancer.py** — NodeLoad, select_best_node. В **local_router** используется при выборе узла (если get_load_balancer доступен), плюс обновление load после запроса. |
| Умный выбор модели | **available_models_scanner** + выбор «лучшей» модели в victoria_server; **intelligent_model_router** — сложность задачи и fit модели по типу (coding/reasoning). |

**Вывод:** Балансировка и интеллектуальный выбор модели реализованы. При желании можно усилить веса по загрузке или latency.

---

### 1.6 Контекстная память и сессии

| Предложение | Реализация |
|-------------|------------|
| Контекст сессии, прошлые сообщения | **session_context_manager** — сессии по (user_id, expert_name), последние запросы/ответы из БД (session_context). **collective_memory** — долгосрочные следы (trace_id, action, result). |

**Вывод:** Краткосрочный и долгосрочный контекст есть. В пайплайне Victoria (victoria_server) контекст сессии пока не подмешивается в промпт — можно добавить вызов session_context при наличии user_id/session_id.

---

### 1.7 Обратная связь и обучение

| Предложение | Реализация |
|-------------|------------|
| Сбор feedback, обучение на ошибках | **feedback_collector** — implicit feedback в ai_core. **human_in_the_loop** — record_feedback, learn_from_feedback. **telegram_simple** — кнопки 👍/👎 и кэш feedback. **adaptive_prompter** — prompt_feedback в БД. **singularity_9_ab_tester** — сравнение по feedback_score. |

**Вывод:** Цепочка сбора обратной связи и использования в оценках/тестах есть. Полностью автоматическое «переобучение промптов» на основе валидатора не реализовано — можно добавить отдельным фоновым процессом при росте объёма данных.

---

### 1.8 A/B тестирование

| Предложение | Реализация |
|-------------|------------|
| A/B тестирование моделей/промптов | **singularity_9_ab_tester**, **prompt_ab_testing**. В local_router — опционально **ml_router_ab_test**. |

**Вывод:** База для A/B есть. Расширять только при явной потребности в экспериментах.

---

## 2. Частично реализовано или отсутствует

### 2.1 Прогресс длинных задач (WebSocket)

| Предложение | Реализация |
|-------------|------------|
| Прогресс-бар, промежуточные статусы по WebSocket | WebSocket есть для **терминала** (backend). Для чата с Victoria/Victoria Enhanced потока прогресса по WebSocket нет. |

**Рекомендация:** Имеет смысл для длинных сценариев (много шагов, делегирование): при появлении фронта с чатом добавить эндпоинт WebSocket и отправку событий типа `{ step, total, status }` из Victoria/оркестратора.

---

### 2.2 Уточняющие вопросы до выполнения

| Предложение | Реализация |
|-------------|------------|
| Если задача неясна — сгенерировать уточняющие вопросы | В **agent_evolver** есть clarification_questions, но в **victoria_server** перед plan/execute нет проверки «нужна ли репликация» и не возвращаются вопросы пользователю. |

**Рекомендация:** Добавить в victoria_server после understand_goal проверку по category/ambiguity: при высокой неоднозначности возвращать ответ с полем `needs_clarification` и списком вопросов, не запуская выполнение. Реализация — один вызов LLM или эвристики по длине/ключевым словам.

---

### 2.3 Предложения следующих шагов

| Предложение | Реализация |
|-------------|------------|
| После выполнения — предложить 2–3 логичных следующих действия | Отдельного suggestion_engine в контуре Victoria нет. |

**Рекомендация:** Низкий приоритет. Можно добавить постобработку: по типу выполненной задачи (из plan/category) и knowledge_base возвращать 1–3 предложения (например, «проверить тесты», «посмотреть логи»). Имеет смысл после стабилизации основного сценария.

---

### 2.4 Ретривал по прошлым успешным решениям

| Предложение | Реализация |
|-------------|------------|
| Векторный поиск по прошлым успешным решениям | **semantic_cache** / **semantic_ai_cache** используются для кэша ответов. Отдельного «ретривала по успешным решениям» для усиления промпта в Victoria нет. |

**Рекомендация:** При росте объёма успешных кейсов можно добавить шаг: по эмбеддингу текущей цели искать в БД/кэше похожие успешные ответы и подставлять 1–2 примера в промпт планировщика или исполнителя.

---

### 2.5 Пакетная обработка мелких задач

| Предложение | Реализация |
|-------------|------------|
| Батчинг однотипных мелких задач из БД | В Enhanced Orchestrator задачи обрабатываются по одной. Отдельного батчера для «мелких однотипных» нет. |

**Рекомендация:** Низкий приоритет, пока нет явной нагрузки из множества мелких задач. При появлении — можно группировать по domain_id/типу и вызывать LLM батчем.

---

### 2.6 Предиктивная загрузка моделей

| Предложение | Реализация |
|-------------|------------|
| Загружать часто используемые модели заранее по расписанию | В MLX есть кэш моделей и LRU-выгрузка. Отдельного «расписания использования» и предзагрузки по нему нет. |

**Рекомендация:** Имеет смысл только при выраженных паттернах (например, утром — одна модель, вечером — другая) и при нехватке памяти. Можно отложить.

---

## 3. Что не нужно или низкий приоритет

- **Мультимодальность (изображения/аудио)** — в плане помечено «если актуально». Сейчас фокус на коде и задачах; добавлять только при появлении требований.
- **Отдельный «умный роутер» с ML-моделью** — уже есть intelligent_model_router и ML-роутер в local_router; дублировать не нужно.
- **Полноценный MultiLayerCache в одном классе** — текущего набора кэшей достаточно; унификация — по желанию для единообразия кода.
- **Jaeger/Prometheus/Grafana** — при необходимости подключаются к существующему OpenTelemetry; отдельно не обязательно, если логи и метрики уже достаточны.

---

## 4. Что внедрить в первую очередь

### 4.1 Быстрые победы (1–2 недели)

1. **Correlation ID по всей цепочке**  
   - В `victoria_server`: принять опциональный `correlation_id` в теле запроса (или сгенерировать), передавать его в understand_goal, plan, executor, вызовы Veronica и в ответ.  
   - Пробросить в observability (атрибут span) и в логи.  
   - Эффект: проще отлаживать и анализировать запросы.

2. **Кэш по (prompt, category) в LocalAIRouter**  
   - Небольшой in-memory LRU (например, 500–1000 записей, TTL 1 час) для результата run_local_llm.  
   - Ключ: hash(prompt + category + model).  
   - Эффект: ускорение повторяющихся запросов без изменения контрактов.

3. **Уточняющие вопросы при неясной задаче**  
   - После understand_goal: если category == "multi_step" и длина/эвристика указывают на неоднозначность — один вызов LLM «сгенерировать 1–3 уточняющих вопроса» и ответ с `needs_clarification: true` и списком вопросов, без выполнения.  
   - Эффект: меньше ложных выполнений и переделок.

### 4.2 Средний срок (3–4 недели)

4. **Подмешивание контекста сессии в Victoria**  
   - При наличии user_id/session_id вызывать session_context_manager.get_session_context и добавлять в системный/пользовательский промпт краткий контекст (последние 2–3 пары вопрос–ответ).  
   - Эффект: более связный диалог без смены архитектуры.

5. **WebSocket для прогресса длинных задач**  
   - После появления чата с Victoria: эндпоинт WebSocket, в оркестраторе/Victoria при смене шага отправлять событие progress.  
   - Эффект: лучший UX при долгих сценариях.

### 4.3 По мере необходимости

6. **Ретривал по успешным решениям** — когда накопится достаточно успешных кейсов в БД/кэше.  
7. **Автообучение промптов по валидатору** — фоновый джоб по отклонённым результатам и обновлению шаблонов.  
8. **Пакетная обработка** — только при реальной нагрузке множеством мелких однотипных задач.

---

## 5. Краткая сводка

| Категория | Статус | Действие |
|-----------|--------|----------|
| Кэширование | Есть | Опционально: LRU в LocalAIRouter по (prompt, category, model). |
| Circuit Breaker | Есть | Оставить как есть; при необходимости — CB по узлам в local_router. |
| Трассировка / correlation_id | Частично | Добавить единый correlation_id по цепочке и в логи/span. |
| Understand + сложность | Есть | Только тонкая настройка. |
| Балансировка / выбор модели | Есть | При желании — усилить веса по загрузке. |
| Контекст сессии | Есть в БД | Подключить в victoria_server при наличии user_id/session_id. |
| Feedback / обучение | Есть | Опционально: автообновление промптов по валидатору. |
| Уточняющие вопросы | Нет | Внедрить после understand_goal при высокой неоднозначности. |
| Прогресс по WebSocket | Нет | Внедрить при появлении чата с длинными сценариями. |
| Предложения следующих шагов | Нет | Низкий приоритет. |
| Пакетная обработка / предзагрузка моделей | Нет | По мере появления потребности. |

Архитектура уже закрывает большую часть предложенного плана. Наиболее полезные следующие шаги: **correlation_id по цепочке**, **кэш в LocalAIRouter** и **уточняющие вопросы** — они дают быстрый эффект при небольшом объёме работ.

---

## 6. Реализовано (быстрые победы)

- **Correlation ID**: в `victoria_server.py` — заголовок `X-Correlation-ID`, поле в `TaskResponse`, в `_run_task_store` и ответе 202/GET status. Логи содержат короткий префикс correlation_id.
- **Кэш в LocalAIRouter**: в `knowledge_os/app/local_router.py` — кэш по ключу (prompt, category, model), TTL 30 мин, max 500 записей, возвращается кортеж (result, routing_source). Статистика: `_prompt_cache_hits` / `_prompt_cache_misses`.
- **Уточняющие вопросы**: в `victoria_server.py` — `_check_ambiguity`, `_generate_clarification_questions`, `_understand_goal_with_clarification`; при неоднозначности возвращается `status: needs_clarification` и список вопросов, выполнение не запускается. Для выполнения используется переформулированная цель (`restated_goal`) и в Enhanced, и в стандартном режиме.
- **Исправлен баг отступа** в `_ensure_best_available_models` (блок `if best:`).
