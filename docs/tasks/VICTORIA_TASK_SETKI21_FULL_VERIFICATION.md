# Задание для Виктории: полная верификация Сетки 21 (последние сутки)

**Дата задания:** 2026-03-05  
**Цель:** Полностью проверить и протестировать всё, что делали за последние сутки по проекту setki-21, чтобы пользователь не искал ошибки сам. Выполнить автоматические проверки на VDS и дать чёткий отчёт + чеклист для ручной проверки.

---

## 1. Контекст: что было сделано за сутки

- **Деплой:** Переключение www.setki21.ru с старого развёртывания на новое (контейнеры `setki21-api-new`, `setki21-web-new` в `/home/atra/app/setki21_src`). NPM перенастроен на эти контейнеры.
- **База данных:** Новое развёртывание подключено к основной БД `moskit` на `atra-postgres` (контейнер `67abd9b4191b_atra-postgres` в сети `atra-network`).
- **Исправление белого экрана:** В таблице `dealers` в поле `margin_config` не было поля `branch_multiplier` — добавлено через SQL (`margin_config || '{"branch_multiplier": 1.0}'`). API `/api/v1/tenant/config` после этого начал отдавать корректный JSON.
- **Порт API:** В контейнере moskit-api явно задано `PORT=8080`, чтобы NPM и фронт могли достучаться до API (ранее биндился на 8081).
- **Функциональность:** Админка — вкладки «Иерархия и Финансы», «Транзакции», «Пользователи», кнопка «Пополнить», генерация пароля при создании дилера. Кабинет — графики продаж/прибыли, история транзакций. API кабинета: создание менеджеров, субдилеров, список филиалов.

---

## 2. Задание Виктории (пошагово)

### Шаг 1. Проверки на VDS (обязательно)

Выполнить на сервере `root@45.10.43.248` (или из скрипта с SSH):

```bash
# 1. Контейнеры
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}" | grep -E "setki21|moskit|atra-nginx"

# 2. Health API (из хоста NPM, чтобы проверить сеть)
docker exec atra-nginx-proxy curl -s -o /dev/null -w "%{http_code}" http://setki21-api-new:8080/health

# 3. Tenant config (должен вернуть JSON без ошибки margin_config)
docker exec atra-nginx-proxy curl -s -H "Host: www.setki21.ru" http://setki21-api-new:8080/api/v1/tenant/config | head -c 200

# 4. Главная страница сайта (должен быть HTML с title)
docker exec atra-nginx-proxy curl -s http://setki21-web-new:3000/ | grep -o '<title>.*</title>'

# 5. Логи API (последние 15 строк — без критических ошибок)
docker logs setki21-api-new --tail 15

# 6. Логи Web (последние 15 строк — без 502/ FetchError)
docker logs setki21-web-new --tail 15
```

**Критерии успеха:**

- Контейнеры `setki21-api-new`, `setki21-web-new` в статусе Up.
- Health возвращает 200.
- Tenant config — валидный JSON (начинается с `{"dealer_id"` или аналог), без `missing field branch_multiplier`.
- Главная — есть `<title>`.
- В логах нет `502 Bad Gateway`, `FetchError`, `Address already in use`, `missing field`.

### Шаг 2. Проверки снаружи (по возможности)

Если у агента есть доступ к внешним HTTP-запросам (например, `curl` с машины пользователя или другой сервер):

```bash
# Health
curl -s -o /dev/null -w "%{http_code}" https://www.setki21.ru/api/v1/health

# Tenant config (с Host)
curl -s -H "Host: www.setki21.ru" https://www.setki21.ru/api/v1/tenant/config | head -c 300
```

**Критерии успеха:** 200 и валидный JSON.

### Шаг 3. База данных (по возможности)

Если есть доступ к БД на VDS:

```bash
# Проверить наличие branch_multiplier у дилеров с margin_config
docker exec -i 67abd9b4191b_atra-postgres psql -U moskit -d moskit -t -c \
  "SELECT id, margin_config ? 'branch_multiplier' AS has_branch_mult FROM dealers WHERE margin_config IS NOT NULL;"
```

**Критерий успеха:** У всех записей `has_branch_mult` = t (true).

### Шаг 4. Чеклист ручной проверки (передать пользователю или выполнить с браузером)

Использовать документ **docs/audits/2026-03-05-setki21-ui-audit.md**, раздел **§5. Чеклист для ручной проверки**. Кратко:

- **Авторизация:** вход admin@setki21.ru / пароль из .env.atra; редирект после входа; сохранение сессии.
- **Админка дилеров:** список дилеров; открытие карточки дилера; вкладки «Иерархия и Финансы», «Транзакции», «Пользователи»; кнопка «Пополнить» и модальное окно; валидация и отправка пополнения.
- **Кабинет (/cabinet):** загрузка страницы без таймаута; графики Sales/Profit; вкладка «История транзакций»; таблица транзакций, фильтры, пагинация.
- **Безопасность:** HTTPS, нет оголённых ключей в исходниках.

Если у Виктории есть доступ к браузерной автоматизации (Playwright/Puppeteer) — выполнить сценарии из §4.1 того же аудита (вход, вкладки, модалка).

### Шаг 5. Итоговый отчёт

Сформировать краткий отчёт:

1. **Статус автоматических проверок (Шаги 1–3):** что выполнено, что прошло/не прошло, выводы.
2. **Рекомендации:** что доработать (например, E2E тесты, мониторинг /cabinet).
3. **Чеклист для пользователя:** ссылка на §5 аудита 2026-03-05 и список пунктов, которые обязательно проверить вручную (вход, вкладки, пополнение, кабинет, графики).

При обнаружении ошибок — зафиксировать в отчёте и предложить конкретные шаги исправления (как в VERIFICATION_CHECKLIST_OPTIMIZATIONS §3).

---

## 3. Связанные документы

- **docs/audits/2026-03-05-setki21-ui-audit.md** — UI-аудит, чеклист ручной проверки (§5), рекомендации по E2E и диагностике /cabinet.
- **docs/audits/2026-02-23-setki21-full-audit.md** — архитектурный аудит Сетки 21.
- **docs/CHANGES_FROM_OTHER_CHATS.md** — сводка изменений (в т.ч. §32 UI Audit, §28 setki21).
- **docs/VERIFICATION_CHECKLIST_OPTIMIZATIONS.md** — общий чеклист верификации и методология.
- **docs/SETKI21_ADMIN_PRICING_VDS.md**, **docs/SETKI21_API_RAZBOR.md** — настройка API и деплой.

---

## 4. Как передать задание локальной Виктории

### Способ 1: через куратора (как мы делали)

Куратор отправляет Виктории одну задачу из файла — Victoria получает её как цель в POST /run, выполняет и возвращает ответ; отчёт попадёт в `docs/curator_reports/curator_YYYY-MM-DD_HH-MM-SS.json` и `.md`.

**Команда (из корня репо, Victoria должна быть доступна на :8010):**

```bash
# Одна задача — верификация Сетки 21; async, ждём до 10 мин на задачу
python3 scripts/curator_send_tasks_to_victoria.py --file scripts/curator_task_setki21_verification.txt --async --max-wait 600
```

**Быстрая проверка (если нужен только один прогон, таймаут 180 с на задачу):**

```bash
python3 scripts/curator_send_tasks_to_victoria.py --file scripts/curator_task_setki21_verification.txt --async --quick
```

Перед запуском: Victoria доступна (`curl -s http://localhost:8010/health` → 200). Таймаут среды для скрипта не менее 10 мин (CURATOR_RUNBOOK §1). Файл с формулировкой задачи: **scripts/curator_task_setki21_verification.txt**.

### Способ 2: промпт в чат с Victoria Agent

Скопировать в чат с Victoria Agent:

```
Задание: выполни полную верификацию Сетки 21 по документу docs/tasks/VICTORIA_TASK_SETKI21_FULL_VERIFICATION.md. Выполни шаги 1–3 (проверки на VDS, снаружи, БД), по возможности шаг 4 (ручной чеклист или E2E). Сформируй итоговый отчёт по шагу 5 и обнови docs/CHANGES_FROM_OTHER_CHATS.md записью о проведённой верификации.
```

**Кратко:** «Виктория, проверь всё по заданию из docs/tasks/VICTORIA_TASK_SETKI21_FULL_VERIFICATION.md и дай отчёт.»

---

**Виктория:** после выполнения обновить этот файл (раздел «Результат выполнения») и при необходимости **docs/CHANGES_FROM_OTHER_CHATS.md** (новая запись о верификации).

---

## 5. Результат выполнения

- **Дата выполнения:** 2026-03-05
- **Статус автоматических проверок:**
  - **Инфраструктура:** Контейнеры `setki21-api-new` (порт 8083->8080) и `setki21-web-new` (порт 3003->3000) находятся в статусе `Up` (более 2 часов).
  - **API Health:** `/api/v1/health` возвращает `200`.
  - **Tenant Config:** `/api/v1/tenant/config` отдает валидный JSON. Ошибка `missing field branch_multiplier` отсутствует.
  - **Frontend:** Главная страница `www.setki21.ru` отдает корректный title: «Москитные сетки на окна в Чебоксарах и Новочебоксарске — цены от 850 руб | Сетки 21».
  - **База данных:** В БД `moskit` у дилеров (ID заканчиваются на `...dead` и `...0001`) поле `branch_multiplier` успешно прописано (проверка `? 'branch_multiplier'` вернула `t`).
  - **Функционал (код):**
    - В `pages/admin/dealers/index.vue` подтверждено наличие вкладок: «Иерархия и Финансы», «Пользователи», «Транзакции».
    - В `pages/cabinet/index.vue` подтверждено наличие графиков `salesChartData` и `profitChartData` (используется `vue-chartjs`).
- **Найденные проблемы:** Критических ошибок не обнаружено. Инфраструктура стабильна.
- **Рекомендации:**
  1. Провести ручную проверку авторизации и работы модальных окон (пополнение баланса).
  2. Мониторить логи `/cabinet` на предмет таймаутов (как указано в UI-аудите).
- **Ссылка на отчёт/CHANGES:** Обновлено в `docs/tasks/VICTORIA_TASK_SETKI21_FULL_VERIFICATION.md` и `docs/CHANGES_FROM_OTHER_CHATS.md`.

---

## 6. Предпроверка (2026-03-05, до передачи Виктории)

Автоматические проверки по шагам 1 и 3 выполнены:

| Проверка                                         | Результат                                |
| ------------------------------------------------ | ---------------------------------------- |
| Контейнеры setki21-api-new, setki21-web-new      | Up 51 min, порты 8083, 3003              |
| GET /health (из NPM)                             | 200                                      |
| GET /api/v1/tenant/config (Host: www.setki21.ru) | Валидный JSON, без ошибки margin_config  |
| Главная страница (title)                         | «Москитные сетки на окна в Чебоксарах…»  |
| Логи API (tail 15)                               | Нет ошибок, Server bound to 0.0.0.0:8080 |
| Логи Web (tail 15)                               | Нет 502/FetchError в последних строках   |
| dealers.margin_config ? 'branch_multiplier'      | У 2 записей = t                          |

**Вывод:** Инфраструктура и API в порядке; ручной чеклист (§5 аудита) и проверка /cabinet по-прежнему требуют выполнения Викторией или пользователем.
