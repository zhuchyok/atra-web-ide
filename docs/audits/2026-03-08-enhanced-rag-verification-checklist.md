# Чеклист верификации: Enhanced RAG (Singularity 21.5)

**Дата:** 2026-03-08
**Ответственный:** Анна (QA Engineer), Дмитрий (ML)

## 1. Hybrid Search v2 (BM25 + Vector)

- [ ] **Точность по ключам:** Запрос "Docker port 8010" должен находить точное упоминание порта в `VICTORIA.md` (BM25 приоритет).
- [ ] **Семантическая глубина:** Запрос "Как работает оркестрация?" должен находить концептуально близкие узлы, даже если слова не совпадают (Vector приоритет).
- [ ] **Комбинированный скоринг:** Проверить, что `similarity` в ответе корректно рассчитывается по формуле `0.7*V + 0.3*B`.
- [ ] **Доменные фильтры:** Поиск с `domain='AI Research'` не должен возвращать узлы из других доменов.

## 2. External Docs Indexer

- [ ] **Загрузка контента:** Проверить, что `ExternalIndexer` успешно скачивает текст с внешних URL (например, документация Anthropic).
- [ ] **Чанкование:** Убедиться, что длинные документы разбиваются на перекрывающиеся чанки (overlap) без потери контекста на границах.
- [ ] **Индексация:** Проверить появление новых узлов в таблице `knowledge_nodes` с корректными эмбеддингами.
- [ ] **Метаданные:** Наличие `source_url` в метаданных узла для обратной ссылки.

## 3. Re-ranking (Phase 2)

- [ ] **Качество топ-1:** Проверить, что после переранжировки наиболее точный ответ всегда оказывается на первом месте.
- [ ] **Latency:** Время отработки Re-ranking на 10-20 чанках не должно превышать 1.5 сек (на Mac Studio).

## 4. Регрессия и Безопасность

- [ ] **Нагрузка на RAM:** Мониторинг Mac Studio при массовой индексации (не более +2GB RAM).
- [ ] **Синтаксис:** Отсутствие ошибок в `enhanced_search.py` после правок.
- [ ] **SQL Injection:** Проверка экранирования параметров в новых SQL-запросах с `tsvector`.

---

**Статус:** ⏳ Ожидание завершения задач локальной Викторией.
