# 🔍 ПОЧЕМУ ЭКСПЕРТЫ НЕ ВИДЕЛИ РЕАЛЬНЫЙ КОД ДАШБОРДА

**Дата:** 2026-01-28  
**Проблема:** Эксперты вернули примеры кода для Qdrant/KServe/Triton, которых нет в системе

---

## ❌ ПРОБЛЕМА: ЭКСПЕРТЫ РАБОТАЮТ "ВСЛЕПУЮ"

### Что получают эксперты:

Из `smart_worker_autonomous.py` (строка 114):
```python
prompt = f"""{expert_config['system_prompt']}\nRole: {expert_config['role']}\nDept: {expert_config['department']}\nTASK: {task['title']}\nDESC: {task['description']}"""
```

**Эксперты получают ТОЛЬКО:**
1. ✅ `system_prompt` - их роль и инструкции
2. ✅ `title` - название задачи
3. ✅ `description` - описание задачи

**Пример описания задачи dashboard_audit:**
```
Найдена ошибка: Проверить все try/except блоки в дашборде
Местоположение: app.py - все табы
Требуется исправление.
```

### Что НЕ получают эксперты:

❌ **Код файлов** (например, `knowledge_os/dashboard/app.py`)  
❌ **Пути к файлам**  
❌ **Контекст проекта**  
❌ **Доступ к файловой системе** через инструменты (`read_file`, `list_directory`)  
❌ **Содержимое файлов** в промпте

---

## 🔍 КАК ЭТО РАБОТАЕТ СЕЙЧАС

### Поток обработки задачи:

```
1. Задача создается в БД
   └─> title: "🔴 ИСПРАВИТЬ: Ошибки в дашборде"
   └─> description: "Местоположение: app.py - все табы"

2. Smart Worker берет задачу
   └─> Получает expert_config (system_prompt, role, department)
   └─> Формирует промпт: system_prompt + title + description

3. Вызывается run_cursor_agent_smart()
   └─> Который вызывает run_smart_agent_async() из ai_core.py
   └─> Агент работает ТОЛЬКО с текстовым промптом
   └─> НЕ имеет доступа к файлам проекта

4. Эксперт видит только:
   "Ты - эксперт по дашбордам. 
    Задача: Исправить ошибки в дашборде
    Описание: Местоположение: app.py - все табы"
   
   ❌ НЕ видит сам файл app.py!
```

---

## 🎯 ПОЧЕМУ ЭКСПЕРТЫ ДАЛИ QDRANT/KSERVE/TRITON

### Логика экспертов:

1. **Эксперт получил задачу:**
   - "Исправить ошибки в дашборде"
   - "Местоположение: app.py"
   - Но НЕ видит сам `app.py`

2. **Эксперт предполагает:**
   - "Дашборд использует векторный поиск"
   - "Популярные технологии для этого: [предполагает на основе общих знаний]"
   - "Дам примеры кода для этих технологий"

3. **Реальность:**
   - Система использует **pgvector + VectorCore + MLX API Server**
   - Но эксперт этого не знает, т.к. не видел код
   - Эксперт предполагает популярные технологии, которых нет в системе

---

## ✅ РЕШЕНИЕ: ПЕРЕДАВАТЬ КОД В КОНТЕКСТЕ

### Вариант 1: Добавить код в description задачи

При создании задачи dashboard_audit:
```python
# Читаем файл
with open('knowledge_os/dashboard/app.py', 'r') as f:
    code = f.read()

# Добавляем в description
description = f"""
Найдена ошибка: Проверить все try/except блоки в дашборде
Местоположение: app.py - все табы

КОД ФАЙЛА:
```python
{code[:5000]}  # Первые 5000 символов
```

Требуется исправление.
"""
```

### Вариант 2: Передавать путь к файлу в metadata

```python
metadata = {
    "file_path": "knowledge_os/dashboard/app.py",
    "file_type": "python",
    "context": "dashboard"
}
```

И в `smart_worker_autonomous.py`:
```python
# Читаем файл если указан путь
if task.get('metadata', {}).get('file_path'):
    file_path = task['metadata']['file_path']
    try:
        with open(file_path, 'r') as f:
            file_content = f.read()
        prompt += f"\n\nКОД ФАЙЛА {file_path}:\n```python\n{file_content}\n```"
    except Exception as e:
        prompt += f"\n\n⚠️ Не удалось прочитать файл {file_path}: {e}"
```

### Вариант 3: Дать экспертам доступ к инструментам

Изменить `run_smart_agent_async` чтобы агенты могли использовать:
- `read_file` - чтение файлов
- `list_directory` - просмотр директорий
- `grep` - поиск в коде

Но это требует изменения архитектуры агентов.

---

## 📋 РЕКОМЕНДАЦИИ

### Краткосрочные (быстрое решение):

1. **При создании задач dashboard_audit:**
   - Читать файл `app.py`
   - Добавлять код в `description` (первые 5000-10000 символов)

2. **В `smart_worker_autonomous.py`:**
   - Проверять `metadata.file_path`
   - Автоматически читать файл и добавлять в промпт

### Среднесрочные (улучшение архитектуры):

1. **Добавить поле `file_context` в таблицу `tasks`:**
   ```sql
   ALTER TABLE tasks ADD COLUMN file_context TEXT;
   ```

2. **Создать функцию для автоматического чтения файлов:**
   ```python
   async def enrich_task_with_code(task):
       if task.get('metadata', {}).get('file_path'):
           # Читаем файл
           # Добавляем в file_context
           # Возвращаем обогащенную задачу
   ```

3. **Передавать код в промпт эксперта:**
   ```python
   prompt = f"""{expert_config['system_prompt']}
   TASK: {task['title']}
   DESC: {task['description']}
   CODE: {task.get('file_context', 'N/A')}"""
   ```

### Долгосрочные (полная перестройка):

1. **Дать экспертам доступ к инструментам:**
   - `read_file`, `list_directory`, `grep`
   - Эксперты сами читают нужные файлы

2. **Контекстный поиск:**
   - Автоматически находить релевантные файлы
   - Добавлять их в контекст задачи

---

## 🎯 ВЫВОДЫ

1. **Эксперты не видели код** потому что система не передает файлы в контексте задачи
2. **Они работают "вслепую"** - только по описанию, без доступа к реальному коду
3. **Решение:** Передавать код файлов в `description` или `metadata` задачи
4. **Идеальное решение:** Дать экспертам доступ к инструментам для чтения файлов

---

**Дата анализа:** 2026-01-28  
**Автор:** ATRA Corporation (Victoria Agent)
